VERDICT: APPROVED
PR: 1241
Round: 1
Repo: dcetlin/Lobster

---

## Round 1 — 2026-05-19

### Stage 1: Vision Alignment

**Prior:** This implementation is solving the wrong problem, or solving the right problem in a direction that forecloses better paths.

**Theory of change check:** This PR removes broken imports referencing `TTL_EXCEEDED_HOURS` and `recover_ttl_exceeded_uows` — symbols removed from `executor.py` in PR #1216. The vision demands a system that reduces cognitive load and routes work reliably. Broken test-collection errors corrupt the feedback loop the WOS executor depends on. Removing them is the right structural act.

**What would have to be true for this to be wrong:** The symbols would have to still exist somewhere in executor.py, or the removed tests would have to be covering live behavior. Neither is true — `git grep` on the PR branch confirms zero references to these symbols in src/, and executor.py contains only `reset_expired_claims()` (different semantics, different test).

**What does this foreclose:** Nothing. The PR explicitly acknowledges the replacement API belongs in a new dedicated test. No path is narrowed.

**Opportunity cost:** Near-zero. This is cleanup discovered during oracle review of PR #1233. Leaving it would silently break test collection on 3 files.

**Alignment verdict:** Confirmed. This serves the vision's reliability requirements without introducing any premature closure.

**Learnings check (oracle/learnings.md):** No known failure pattern from learnings.md applies here. This is not a state-machine transition, classification, or contract-design issue. It is dead-code removal.

**Golden patterns check (oracle/golden-patterns.md):** No golden pattern is being extended or violated. This is below the threshold for a pattern entry — it is a correction, not a structural win.

---

### Stage 2: Quality Review

**Does it do what it claims to do?**

Yes. Three files are cleaned:

1. `tests/integration/test_wos_ttl_recovery.py` — top-level import block for `TTL_EXCEEDED_HOURS` and `recover_ttl_exceeded_uows` removed; two dead TTL test functions and `_seed_and_activate` helper deleted; docstring updated to reflect single-scenario scope.
2. `tests/unit/test_orchestration/test_executor_false_complete_fix.py` — two dead symbols removed from import block; `TestTtlRecoveryCoversExecuting` class (2 methods) deleted.
3. `tests/integration/test_wos_e2e_harness.py` — `TestHarness003TtlRecovery` class deleted; `HARNESS_003_ISSUE_NUMBER` constant removed.

**Verification:** `git grep "TTL_EXCEEDED_HOURS\|recover_ttl_exceeded_uows"` on `origin/fix/remove-dead-ttl-imports-uow-4f7a81` across tests/ returns no matches (exit code 1). Dead symbols fully excised.

**Surviving imports clean:** The surviving import block in `test_executor_false_complete_fix.py` imports only `Executor`, `ExecutorOutcome`, `_dispatch_via_inbox`, `_noop_dispatcher`, `_dispatcher_is_async`, `_ASYNC_EXECUTOR_TYPES` — all live symbols.

**Encoded Orientation check (constraint-3):** This PR does not change behavioral defaults, system constraints, agent identity, or decision-making rules. No Encoded Orientation gate applies.

**Failure modes:** None identified. This is a subtraction. The only failure mode would be accidentally deleting a live test — the diff shows that the surviving test functions (`test_startup_sweep_detects_executor_orphan`, `test_startup_sweep_ignores_fresh_ready_for_executor_uow`) remain intact and their import chain is unbroken.

**Patterns introduced:** None. No new structural pattern.

**What this makes easier:** Test collection on these 3 files no longer fails with ImportError. CI feedback is reliable again.

**What this makes harder:** Nothing.

**VERDICT: APPROVED**
