VERDICT: APPROVED

**PR:** #1333 — refactor(wos): promote _WOS_UOW_EXECUTOR_PREFIX from function-local to module scope in wos_completion.py
**Repo:** dcetlin/Lobster
**Reviewed:** 2026-05-26
**Oracle round:** 1

---

## Stage 1 — Premise Check

**Prior:** This implementation is solving the wrong problem, or solving it in a direction that forecloses better paths.

**Disconfirmed.** The oracle finding from PR #1331 explicitly named this constant's function-local placement as a structural defect (learnings.md PR #967 pattern: frozenset/constant defined inside a function body is re-allocated on every call). This PR implements the exact correction: promotes `_WOS_UOW_EXECUTOR_PREFIX` to module scope alongside its sibling `WOS_TASK_ID_PREFIX`. No alternative framing is more correct — the fix is precise and the scope is correct.

Vision constraint check: pure structural improvement, no screen-dependency increase, no cognitive-extension regression. Premise is sound.

---

## Stage 2 — Implementation Review

**Placement:** Module-scope constant sits between `WOS_TASK_ID_PREFIX` and `WRITE_RESULT_SUCCESS_STATUS` (lines 67–75 of PR branch). Logically grouped with its public sibling. Correct.

**Docstring:** Names the narrower scope ("excludes wos-diagnose-*, wos-surface-*, and other non-executor wos- prefixed tasks"), the guard purpose, and the consuming function. Accurate and complete.

**Function body:** `maybe_fail_wos_uow` correctly references module-scope `_WOS_UOW_EXECUTOR_PREFIX` — the function-local declaration removed cleanly.

**Behavioral delta:** Zero. Same string value `"wos-uow_"`, same call sites, same behavior.

**Tests:** PR description cites 4 tests passing in `test_wos_completion.py -k "maybe_fail"`. Tests confirmed present. The test `test_maybe_fail_wos_uow_skips_non_executor_prefix` uses the literal string `"wos-diagnose-abc123"` and a docstring referencing `"wos-uow_"` rather than importing `_WOS_UOW_EXECUTOR_PREFIX`. This is a minor instance of the learnings.md PR #967 mirror-constant pattern. Severity: note only (the test exercises the behavioral outcome — zero UoW records created — rather than asserting the constant's string value; silent divergence risk is lower than the named examples). No fix required before merge.

**Patterns applied:**
- **learnings.md PR #967** (gate frozenset/constant in function body): this PR is the direct remediation of that learning as applied to `_WOS_UOW_EXECUTOR_PREFIX`. The correction matches the named pattern exactly — module-scope constant replaces per-call allocation.

**No golden pattern concerns raised.**

---

## Finding Summary

| Severity | Finding |
|----------|---------|
| Note | Test `test_maybe_fail_wos_uow_skips_non_executor_prefix` does not import `_WOS_UOW_EXECUTOR_PREFIX` — uses literal string in docstring. Minor mirror-constant risk. Not a blocker; behavioral assertions are prefix-independent. |

No blockers. No NEEDS_CHANGES items.

---

## Audit

oracle_event: oracle_approved
pr: 1333
repo: dcetlin/Lobster
uow_id: uow_20260526_f13643
round: 1
