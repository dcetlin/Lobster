VERDICT: APPROVED
PR: 1233
Round: 2

## Round 2 — 2026-05-19

### Prior Gap Status

One gap from Round 1:

**Gap 1 (BLOCKER): Stale run_ttl_recovery mock in test file** — ADDRESSED.

The three `patch.object(_hb_fresh, "run_ttl_recovery")` lines at lines 178, 215, and 253 of `tests/unit/test_orchestration/test_github_rate_limit_check.py` have been removed in commit 4bd79bec. The diff confirms the exact three lines were deleted, one per test function (`test_dispatch_skipped_when_rate_limit_low`, `test_dispatch_proceeds_when_rate_limit_ok`, `test_rate_limit_check_called_once_per_cycle`).

Full-repo grep for `run_ttl_recovery` in `~/lobster/` still returns hits in `scheduled-tasks/executor-heartbeat.py` and the test file on main — but this is the current `main` branch state before merge, as expected. The PR diff shows all three function definition lines, the call site, and the three mock lines all removed. After merge, `grep -r "run_ttl_recovery" ~/lobster/ --include="*.py"` will return zero results in the canonical repo.

**Round 1 Stage 1 findings: unchanged.** The vision alignment verdict (Confirmed) was formed before seeing the implementation and is not revised. The removal remains traceable to constraint-4 (minimize metabolic cost of cybernetic engagement) and principle-1 (proactive resilience over reactive recovery). No new vision-alignment concerns arise from the fix commit.

---

### Round 2 Stage 2: Fix Verification

**Does the fix do what it claims?** Yes. The diff at `tests/unit/test_orchestration/test_github_rate_limit_check.py` shows exactly three line deletions, each removing a `patch.object(_hb_fresh, "run_ttl_recovery")` entry from the `ExitStack` context managers in the three affected test functions. No other lines are modified in the test file. The fix is minimal and correctly scoped.

**Encoded Orientation check (constraint-3):** The fix commit removes test lines only. No behavioral defaults, agent identity, or decision-making rules are affected. Constraint-3 does not trigger.

**Pattern citation — stale-assertion (learnings.md, 2026-04-22, #827):** The Round 1 blocking gap was the exact pattern named in learnings.md: "when a PR removes a behavior, any test that patches or asserts the removed behavior must be updated before merge." The fix correctly resolves this by removing the stale patches rather than adding a stub or suppressing the failure. The stale-assertion pattern's behavioral effect in Round 1: it caused me to flag the test file gap as a blocker rather than a note, which is what triggered the NEEDS_CHANGES verdict. The fix satisfies that exact requirement.

**No new issues introduced:** The fix commit makes no changes to production code, adds no new mocks, and does not alter any other test assertions. The three deleted lines were the only blocking gap.

---

## Round 1 — 2026-05-19

### Stage 1: Vision Alignment (formed before seeing implementation)

**Prior:** This implementation is solving the wrong problem, or solving the right problem in a direction that forecloses better paths.

**Theory of change:** vision.yaml current_focus confirms the WOS executor went live on 2026-05-18. The executor is now producing real work. Any mechanism generating crash alerts every 3 minutes is directly competing with genuine signals for Dan's attention — violating constraint-4 (minimize metabolic cost of cybernetic engagement). The theory of change is: remove the dead code path that generates the noise; confirm the replacement path is intact; restore signal integrity.

**What would have to be true:** `reset_expired_claims()` must cover the recovery territory that `run_ttl_recovery()` was supposed to cover. If `reset_expired_claims()` has a coverage gap that the TTL path filled, removing `run_ttl_recovery()` forecloses that recovery territory. Stage 2 must verify coverage.

**Cheaper test of assumption:** Restoring `TTL_EXCEEDED_HOURS` to `executor.py` is a plausible alternative. But `executor.py`'s module docstring explicitly states the visibility-timeout model (`reset_expired_claims()`) supersedes TTL-based recovery. Restoring a symbol the design intentionally removed would be reviving a deprecated path against the architecture's grain. Removal of `run_ttl_recovery()` is the structurally correct path.

**What this forecloses:** TTL-based stall recovery as a fallback for UoWs with `claimed_until IS NULL`. This is a theoretical gap — `reset_expired_claims()` only recovers UoWs where `claimed_until IS NOT NULL`. However, this gap was already present since PR #1220's introduction of `reset_expired_claims()` and the `try/except ImportError` making `run_ttl_recovery()` return `[]` on every call. The removal makes the existing functional reality explicit structurally.

**Alignment verdict: Confirmed.** The removal is traceable to constraint-4 (minimize metabolic cost of cybernetic engagement — 480 crash alerts per day are direct metabolic cost) and principle-1 (proactive resilience — removing a broken recovery path prevents false confidence in a degraded system).

**Pattern citation — full-repo grep as precondition for dead-code removal (golden-patterns.md, 2026-04-25):** This pattern requires running `grep -r 'run_ttl_recovery' ~/lobster/ --include='*.py'` before accepting the removal. Applying this pattern changed my Stage 2 behavior: rather than stopping at the heartbeat file, I ran the full-repo grep before forming any conclusion. The grep returned 3 hits in `tests/unit/test_orchestration/test_github_rate_limit_check.py`. Without this pattern, those call sites would have been missed. The pattern's behavioral effect was preventing a false APPROVED verdict — the single-hit rule ("one import anywhere in the repo is as binding as ten imports") applied directly here.

---

### Stage 2: Quality Review

**Does it do what it claims to do?** Partially. The function body and call site are cleanly removed. The module docstring update and the inline comment cleanup in `_main_inner()` are both included and correct. `reset_expired_claims()` in the Phase 1 stall recovery block is present and unaffected. The PR title claim is accurate: crash alert spam stops because the `try/except ImportError → _write_crash_alert()` block is gone.

**What the key decisions are:** The function is removed completely rather than replaced with a stub or disabled by comment. This is the correct approach — a stub would preserve the call site and its mock surface but provide no value. The `reset_expired_claims()` path remains the sole canonical path.

**Encoded Orientation check (constraint-3):** This PR removes behavioral code — it does not add a new behavioral default, agent identity change, or decision-making rule. This is not an Encoded Orientation decision. No prior logged decision or vision.yaml anchor is required for a removal of dead code.

**Failure modes after merge:**

**Finding 1 (BLOCKER): Stale mock in test suite will fail after merge.**

Full-repo grep for `run_ttl_recovery` returns 3 hits in `tests/unit/test_orchestration/test_github_rate_limit_check.py`:

- Line 178: `patch.object(_hb_fresh, "run_ttl_recovery"),`
- Line 215: `patch.object(_hb_fresh, "run_ttl_recovery"),`
- Line 253: `patch.object(_hb_fresh, "run_ttl_recovery"),`

`patch.object` raises `AttributeError` when the named attribute does not exist on the target object (verified: `patch.object(obj, "nonexistent")` raises `AttributeError: ... does not have the attribute 'nonexistent'`). After this PR merges, `run_ttl_recovery` will not exist as a module-level name in `executor-heartbeat.py`. All three tests will fail with `AttributeError`.

This is the exact failure pattern named in the learnings.md stale-assertion entry (2026-04-22, #827): when a PR removes a behavior, any test that patches or asserts the removed behavior must be updated before merge. The mock is suppressing a call to a function that will no longer exist; the mechanism (`patch.object`) requires the attribute to exist.

The PR description's test plan includes "No `run_ttl_recovery` references remain in the file" — but the scope was the heartbeat file only. The full-repo grep requirement from golden-patterns.md catches this gap.

**Resolution:** Remove the three `patch.object(_hb_fresh, "run_ttl_recovery")` lines from `tests/unit/test_orchestration/test_github_rate_limit_check.py`. The tests should pass without it because the function is gone and cannot be called.

**Finding 2 (Pre-existing, not introduced by this PR):** Two test files import `TTL_EXCEEDED_HOURS` and `recover_ttl_exceeded_uows` from `orchestration.executor` — symbols removed in PR #1216:
- `tests/unit/test_orchestration/test_executor_false_complete_fix.py` lines 38-39
- `tests/integration/test_wos_ttl_recovery.py` line 47

Both imports fail today on main (confirmed). These are pre-existing CI failures not introduced by PR #1233. They are flagged as observations for tracking and are out of scope for this PR's revision contract.

**Coverage gap analysis (not a blocker):** `reset_expired_claims()` requires `claimed_until IS NOT NULL`. UoWs stuck in `active` with `claimed_until IS NULL` would not be recovered. However: (a) this gap has existed since PR #1220; (b) `run_ttl_recovery()` was already returning `[]` on every call; (c) `executor.py`'s module docstring explicitly states "No separate TTL recovery job is needed — the visibility-timeout model handles it." The gap is structural design intent, not a regression introduced here.

---

### Revision Contract

**Gap 1 (BLOCKER): Stale run_ttl_recovery mock in test file.**

The three `patch.object(_hb_fresh, "run_ttl_recovery")` lines in `tests/unit/test_orchestration/test_github_rate_limit_check.py` (lines 178, 215, 253) must be removed before merge. Resolution options:
- (a) **Addressed:** Remove the three lines and verify the tests pass.
- (b) **Disputed:** Author states why `patch.object` will not raise `AttributeError` on a missing attribute in this testing framework configuration, with a specific cited reason.
- (c) **Deferred:** Not applicable — this is a CI correctness issue that will cause the test suite to fail on merge.
