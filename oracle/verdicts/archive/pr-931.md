VERDICT: APPROVED
PR: 931
Round: 2

## Round 2 — 2026-04-25

### Prior gap tracking

**Gap 1 (Round 1): Feature bundling — two undescribed substantive changes** — ADDRESSED.

The blocking gap required either removing the undescribed steward.py/dispatcher_handlers.py changes or updating the PR description to cover all changed files explicitly. The PR was rebased onto origin/main and both bundles are gone. The round 2 diff contains only: `oracle/golden-patterns.md` (pattern housekeeping), `oracle/verdicts/archive/pr-924.md` and `oracle/verdicts/archive/pr-930.md` (verdict archiving), `src/ooda/fast_thorough_selector.py`, `src/orchestration/shard_dispatch.py`, and `tests/ooda/test_fast_thorough_selector.py`. Every changed file is either the stated StrEnum scope or acceptable oracle housekeeping. The PR description correctly describes the oracle housekeeping entries and notes that the non-StrEnum files (verdict archives, golden-patterns additions) were present in the branch before the StrEnum fix. The scope is now unambiguous.

**Gap 2 (advisory, Round 1): Stated test plan item not fulfilled — backward-compat assertions** — ADDRESSED.

`TestPathSelectionBackwardCompat` is added to `test_fast_thorough_selector.py` with exactly the two explicit assertions the round 1 gap named:
- `assert PathSelection.FAST == "fast"`
- `assert PathSelection.THOROUGH == "thorough"`

The PR's stated test plan item is now satisfied by executable tests, not the Python StrEnum specification alone.

---

## Stage 1 — Vision Alignment (locked from Round 1)

**Adversarial prior:** This implementation is solving the wrong problem, or solving the right problem in a direction that forecloses better paths.

The round 1 Stage 1 finding is locked. The PR serves the StrEnum golden pattern (golden-patterns.md [2026-04-24], Rule 3) applied to a named violation — `shard_dispatch.py`'s `select_path()` returning raw strings was identified by name as Issue #926. The golden pattern entry names `shard_dispatch.py` and Issue #926 explicitly. No cheaper test of the assumption exists; the violation was already named and filed. The work is consistent with vision.yaml `principle-3` (Determinism over judgment for conditionals) and constraint-3's direct reference to `fast_thorough_selector.py` as part of the OODA decision loop structure.

**Golden pattern citation — StrEnum constants (golden-patterns.md [2026-04-24]):** Rule 3 states "Any function that returns one of N fixed string choices should define a StrEnum." This pattern constrained my Stage 1 analysis by confirming the fix layer (define at the dispatch action layer, import at the selector layer) and confirming backward-compatibility is structural, not advisory. Without this pattern in scope, I would have needed to independently reason about whether the typing investment was warranted — the pattern makes it a non-question.

**Alignment verdict: Confirmed.** The adversarial prior is not confirmed.

---

## Stage 2 — Quality Review (Round 2)

### Scope verification (mechanical)

| File changed | In PR description or acceptable housekeeping? |
|---|---|
| `oracle/golden-patterns.md` | Yes — two pattern entries added; acceptable housekeeping |
| `oracle/verdicts/archive/pr-924.md` | Yes — verdict archive; acceptable housekeeping |
| `oracle/verdicts/archive/pr-930.md` | Yes — verdict archive; acceptable housekeeping |
| `src/ooda/fast_thorough_selector.py` | Yes — core StrEnum change |
| `src/orchestration/shard_dispatch.py` | Yes — core StrEnum change |
| `tests/ooda/test_fast_thorough_selector.py` | Yes — test update + backward-compat class |

Scope is clean. Feature-bundling failure pattern (learnings.md, cited in Round 1) does not apply to this revision. The bundled changes have been removed; the remaining scope is correctly described.

### Core StrEnum implementation

The implementation is correct and minimal. `PathSelection(StrEnum)` with `FAST = "fast"` and `THOROUGH = "thorough"` is defined in `shard_dispatch.py` at module scope, immediately after the imports. `fast_thorough_selector.py` imports `PathSelection` from `src.orchestration.shard_dispatch` and returns `PathSelection.FAST` / `PathSelection.THOROUGH` from both branches of `select_path()`. The return type annotation is updated to `PathSelection`.

The cross-package import direction (`ooda/` → `orchestration/`) was flagged as advisory in Round 1. No change was required and none was made. The advisory concern stands but does not block approval.

### Test verification

All 10 original selector assertions in `test_fast_thorough_selector.py` are updated to use `PathSelection.FAST` / `PathSelection.THOROUGH`. The `TestPathSelectionBackwardCompat` class adds two explicit string-equality assertions. The test file correctly imports `PathSelection` from `src.orchestration.shard_dispatch`. No regressions are introduced.

### Golden-patterns additions

Two entries are added to `oracle/golden-patterns.md`:
- `[2026-04-24] Pattern: StrEnum constants in all enum-backed SQL and routing logic` — this is the pattern this PR implements. Correct and appropriate.
- `[2026-04-25] Principle: canonical placement must be obvious from first encounter` — a structural principle for file organization. Appropriate housekeeping.

Both entries match the format and standards of existing golden-patterns entries. They are additions, not modifications to existing content.

### Encoded Orientation check (constraint-3)

The StrEnum change does not alter behavioral defaults — it is a type annotation change with backward-compatible values. Not an Encoded Orientation decision. No prior logged decision or vision.yaml anchor required. Constraint-3 is satisfied.

### What this makes easier / harder

Easier: `select_path()` return type is now typed and greppable. Every caller comparing against `'fast'`/`'thorough'` continues to work via StrEnum equality. Future enforcement of the StrEnum pattern has a clean, working example in a central routing function.

Harder: Nothing. The migration is backward-compatible by construction.

---

**VERDICT: APPROVED**

---

## Round 1 — 2026-04-24

VERDICT: NEEDS_CHANGES
PR: 931
Round: 1

---

## Stage 1 — Vision Alignment (formed before reading implementation)

**Adversarial prior:** This implementation is solving the wrong problem, or solving the right problem in a direction that forecloses better paths.

**Theory of change:** The StrEnum golden pattern (golden-patterns.md [2026-04-24]) was introduced precisely because `shard_dispatch.py`'s `select_path()` returning raw `'fast'`/`'thorough'` strings was identified as a named failure case. The PR is the direct remediation of that named case (Issue #926). The golden pattern entry names `shard_dispatch.py` and Issue #926 explicitly. No cheaper test of the underlying assumption exists — the violation was already identified by name and is a correctness investment rather than gold-plating.

**What this forecloses:** Nothing significant. StrEnum values compare equal to their string counterparts, making this a backward-compatible migration. No downstream direction is foreclosed.

**What would have to be true for this to be right:** The StrEnum enforcement is architecturally consistent with vision.yaml `principle-3` (Determinism over judgment for conditionals) — using enum constants in routing logic is exactly this principle applied. constraint-3 in vision.yaml references `fast_thorough_selector.py` directly as part of the OODA decision loop. Having the selector return untyped strings is a structural inconsistency in the decision-making system this vision depends on.

**Golden pattern citation — StrEnum constants (golden-patterns.md [2026-04-24]):** Rule 3 states "Any function that returns one of N fixed string choices should define a StrEnum." The PR satisfies Rule 3 exactly for `select_path()`. This pattern constrained my Stage 1 analysis by confirming the right layer for the fix (define the enum at the dispatch layer where the concept lives; import it at the selector layer) and confirming backward-compatibility is structural, not advisory.

**Alignment verdict: Confirmed.** The adversarial prior is not confirmed. This work is solving the right problem at the right layer.

---

## Stage 2 — Quality Review

### Feature-bundling check (mechanical, first)

The PR description states it is a narrow StrEnum enforcement fix for Issue #926, touching `shard_dispatch.py`, `fast_thorough_selector.py`, and the corresponding selector tests. Comparing against the actual changed files:

| File | In PR description? |
|------|--------------------|
| `src/orchestration/shard_dispatch.py` | Yes — core change |
| `src/ooda/fast_thorough_selector.py` | Yes — core change |
| `tests/ooda/test_fast_thorough_selector.py` | Yes — test update |
| `oracle/golden-patterns.md` | No — two new golden pattern entries added |
| `oracle/verdicts/archive/pr-924.md` | No — archiving prior verdict |
| `oracle/verdicts/archive/pr-930.md` | No — archiving prior verdict |
| `src/orchestration/steward.py` | No — adds `executing_orphan` short-circuit (+39 lines) |
| `tests/unit/test_orchestration/test_steward.py` | No — tests for executing_orphan short-circuit (+107 lines) |
| `src/orchestration/dispatcher_handlers.py` | No — adds `_load_instructions_from_artifact` fallback (+23 lines) |
| `tests/unit/test_orchestration/test_dispatcher_integration.py` | No — tests for artifact fallback (+80 lines) |

The oracle archive files and golden-patterns additions are housekeeping and acceptable. The three substantive bundles are the concern:

**Bundle 1 (stated scope):** StrEnum enforcement — `shard_dispatch.py`, `fast_thorough_selector.py`, `test_fast_thorough_selector.py`. This is the correct scope.

**Bundle 2 (unstated):** `executing_orphan` short-circuit — `steward.py` (+39 lines) and `test_steward.py` (+107 lines). This is the executing_orphan short-circuit feature from PR #924. The PR #924 verdict is being archived in this same PR (oracle/verdicts/archive/pr-924.md), indicating these changes are deliberate carry-forwards of an already-APPROVED feature. The changes are substantial and entirely unrelated to StrEnum enforcement.

**Bundle 3 (unstated):** `_load_instructions_from_artifact` fallback — `dispatcher_handlers.py` (+23 lines) and `test_dispatcher_integration.py` (+80 lines). This addresses Issue #922 (wos_execute messages without an `instructions` field raising KeyError). Zero mention in the PR description. No prior oracle verdict visible for this change.

The feature-bundling pattern from learnings.md (PRs #882, #717, #712) has been flagged three times in this codebase as a recurring failure mode. The test is mechanical: count changed files, compare against PR description, flag any file not mentioned. This PR contains two undescribed substantive code changes.

This is the dominant gap requiring changes.

### Core StrEnum change quality

The StrEnum implementation is correct. `PathSelection(StrEnum)` is defined in `shard_dispatch.py` with `FAST = "fast"` and `THOROUGH = "thorough"`. `fast_thorough_selector.py` imports `PathSelection` from `shard_dispatch` and returns `PathSelection.FAST` / `PathSelection.THOROUGH`. The type annotation on `select_path()` is updated to `PathSelection`.

**Cross-package import direction:** `fast_thorough_selector.py` is in `src/ooda/` and now imports from `src/orchestration/shard_dispatch.py`. This creates a dependency from `ooda/` → `orchestration/`. `PathSelection` is conceptually an OODA-layer concept (it is the output type of the Fast/Thorough meta-selector), but it is defined in the orchestration layer where it is acted upon. The canonical placement principle (golden-patterns.md [2026-04-25]) raises a question: could a new contributor infer that `PathSelection` lives in `shard_dispatch.py` without help? The answer is arguable but not clearly yes. However, this is an advisory concern, not a blocking one — the enum is consumed by both layers, and defining it at the action layer is defensible. If this direction causes import pain in the future, the migration path is a shared types module.

**Backward-compatibility:** The StrEnum contract guarantees `PathSelection.FAST == "fast"` is `True`. The PR description claims this as a test plan item ("Verify PathSelection.FAST == 'fast' and PathSelection.THOROUGH == 'thorough' for backward compat") but no explicit test for this property appears in `test_fast_thorough_selector.py`. The test assertions are updated to use `PathSelection.FAST` / `PathSelection.THOROUGH`, which verifies the returned value equals the enum member, but does not verify the enum member equals the raw string. This is a minor gap — the Python StrEnum specification guarantees it — but the PR's own stated test plan is not fully satisfied.

**Test correctness:** All 10 assertions in `test_fast_thorough_selector.py` correctly use `PathSelection.FAST` / `PathSelection.THOROUGH`. The import is added at the top of the test file. No regressions are introduced in the selector tests.

### Bundle 2 quality: executing_orphan short-circuit

The `steward.py` change adds an early-return check before the classification lookup (`if return_reason == "executing_orphan": return "executing_orphan"`) and the 4b-orphan guard in `_process_uow`. This is the exact fix required by PR #924 Round 1. The archived pr-924.md verdict shows Round 2 was APPROVED. The implementation here matches the required early-return pattern from the Round 1 NEEDS_CHANGES verdict. The three tests in `TestExecutingOrphanShortCircuit` are correct in structure and verify the path end-to-end.

This change is technically correct. The concern is not quality but bundling.

### Bundle 3 quality: `_load_instructions_from_artifact` fallback

`dispatcher_handlers.py` gains a new function `_load_instructions_from_artifact(uow_id)` that loads instructions from the workflow artifact file when a `wos_execute` message does not carry an `instructions` field. The call site changes from `msg["instructions"]` (which raised `KeyError` for missing field) to `msg.get("instructions") or _load_instructions_from_artifact(uow_id)`.

The function raises `ValueError` on missing or malformed artifact, which is caught by the existing spawn-gate and surfaces as a `send_reply` alert. The tests in `TestRouteWosMessageArtifactFallback` cover three cases: inline instructions used when present, artifact fallback when instructions absent, missing artifact returns send_reply with alert. The test design is correct.

**Behavioral change classification:** This changes the behavior of `route_wos_message` for a class of messages it previously rejected with a KeyError. This is an additive change to a routing function — it expands the set of valid inputs rather than changing how existing inputs are handled. It is not an Encoded Orientation decision in the constraint-3 sense (no new behavioral default, no new automation). However, it is an undescribed change bundled into a narrow-scope PR.

### Encoded Orientation check (constraint-3)

The StrEnum change (stated scope): not an Encoded Orientation decision. Type annotation change, no behavioral default change.

The executing_orphan short-circuit: Encoded Orientation decision (marks UoWs failed permanently based on classification). Prior logged decision: PR #924 oracle-approved (archived in this PR). Vision anchor: `principle-1` (Proactive resilience over reactive recovery). Constraint-3 is satisfied for this change.

The artifact fallback: not an Encoded Orientation decision. Additive input expansion.

### What this makes easier / harder

Easier after core StrEnum change: `select_path()` return type is now typed and greppable. Any caller that compares against `'fast'`/`'thorough'` continues to work via StrEnum equality. Future enforcement of the StrEnum pattern in routing logic is grounded by a working example.

Harder: The bundling of three independent changes into one PR makes the oracle-trail harder to trace. The executing_orphan feature now has two oracle records (pr-924.md approved → archived here; this PR's verdict). The artifact fallback feature has no independent oracle record.

---

## NEEDS_CHANGES — Required resolutions

**Gap 1: Feature bundling — two undescribed substantive changes**

The PR contains two independent changes not mentioned in the PR description:
- `steward.py` + `test_steward.py`: executing_orphan short-circuit (feature from PR #924, already oracle-approved)
- `dispatcher_handlers.py` + `test_dispatcher_integration.py`: artifact fallback for wos_execute without instructions (Issue #922)

Resolution (one of three options):
- **(a) Restrict PR to stated scope:** Remove the steward.py, test_steward.py, dispatcher_handlers.py, and test_dispatcher_integration.py changes. Open separate PRs for executing_orphan (with reference to pr-924 oracle approval) and the artifact fallback (with its own oracle review).
- **(b) Update PR description to cover all three changes:** The description must state what each bundled change does, why it is in this PR, and reference the relevant issue numbers. For the executing_orphan changes, cite pr-924 oracle approval. For the artifact fallback, cite Issue #922. This makes the bundling explicit rather than invisible.
- **(c) Split into three PRs:** Cleanest option. Consistent with the anti-bundling pattern in learnings.md.

A generic PR description update that does not trace each changed file to a specific issue or rationale is not sufficient resolution. The gap is addressed when the description (or the PR split) makes the scope unambiguous.

**Gap 2 (advisory, non-blocking): Stated test plan item not fulfilled**

The PR description states "Verify `PathSelection.FAST == 'fast'` and `PathSelection.THOROUGH == 'thorough'` for backward compat" as a test plan item. No test in `test_fast_thorough_selector.py` explicitly asserts these equalities. The Python StrEnum contract guarantees them, so this is not a correctness risk. However, the PR's own stated test plan is not satisfied. Adding two assertions (e.g., `assert PathSelection.FAST == "fast"` and `assert PathSelection.THOROUGH == "thorough"`) closes this gap. This is advisory — it does not block APPROVED — but should be addressed before merge.

---

### Revision contract

Gap 1 is the blocking gap. Resolution requires one of:
- (a) Remove the undescribed changes from this PR, or
- (b) Update the PR description to explicitly cover every changed file with its issue reference and rationale (the oracle will verify each file is accounted for in round 2).

Gap 2 is advisory. Addressed when the two backward-compat assertions are present in the test file, or the PR description explicitly states the test plan item is covered by the Python StrEnum specification and no test is needed.
