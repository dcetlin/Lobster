VERDICT: APPROVED
PR: 1210
Round: 2

## Round 2 — 2026-05-18

### Prior gap tracking (Round 1 → Round 2)

All three Round 1 blockers are now addressed. Status of each:

- **Issue 1 (Migration 118 broken grep pipe):** Addressed. The fix removes `-q` from the first grep, changing `grep -qF "0 9 * * *"` to `grep -F "0 9 * * *"`. Output now passes through the pipe to the second `grep -q "wos-metabolic-digest"`. The first conditional branch is live code.
- **Issue 2 (Feature bundling — /wos uow workstream):** Addressed. The PR was rebased onto main (which had already merged PR #1208 carrying the /wos uow commits). The current diff contains only the outcome_category digest work. `dispatcher_handlers.py` is not modified. Migration 117 appears only in unchanged context (it was already in main before this PR's changes begin).
- **Issue 3 (Dead `db_path` parameter in `format_digest`):** Addressed. The parameter has been removed from the `format_digest` signature. The new signature is `format_digest(groups, stalled, lookback_hours, now_dt: datetime, seeds_total: int) -> str | None`. All 14 test call sites have been updated to the new signature. No `db_path` argument appears in any call to `format_digest`.

Round 1 advisory issues (Issues 4 and 5) did not block merge and are not re-evaluated in this round.

---

## Stage 1: Vision Alignment (carried from Round 1, unchanged)

**Prior entering this review:** This implementation is solving the wrong problem, or solving the right problem in a direction that forecloses better paths.

**Theory of change test:** vision.yaml current_focus names "WOS bidirectional interface (#1118)" and "WOS completion notification layer" as the active constraints. A daily digest of completion outcomes is a downstream reporting surface — it converts classified completion data into a pulse message Dan sees in the morning. This is a legitimate integration task: outcome_category was wired in PR #759 but the digest was still using heuristic re-classification, meaning the architectural work of real-time classification had no surface expression. Replacing the heuristic with outcome_category is "wire what exists before building more" (vision.yaml principle-4) and directly reduces the divergence between architecture intent and the actual daily signal.

**Alignment verdict: Confirmed.** The digest outcome_category wiring is aligned with vision.yaml principle-4. The feature bundling concern from Round 1 (the /wos uow workstream) has been resolved by the rebase — this PR now contains only the digest work it was meant to contain.

---

## Stage 2: Quality Review (Round 2)

### Encoded Orientation check (OODA constraint-3)

No changes to behavioral defaults documents, gate tables, or agent identity in this round. The only modifications are to `scheduled-tasks/wos-metabolic-digest.py`, `scripts/upgrade.sh`, and `tests/unit/test_wos_metabolic_digest.py`. This check is not triggered.

### Verification of all three fixes

**Fix 1 — Migration 118 cron detection pipe (Issue 1):**

The fix is correct:
```bash
if crontab -l 2>/dev/null | grep -F "0 9 * * *" | grep -q "wos-metabolic-digest"; then
```
The `-q` flag is removed from the first grep. Output flows through the pipe. The first conditional branch is now reachable. The `elif` and `else` branches are unchanged and continue to handle the "no entry" and "entry present at correct schedule" cases correctly.

**Fix 2 — Feature bundling (Issue 2):**

Verified by inspecting the diff. No changes to `dispatcher_handlers.py`, no new task files for the /wos uow subcommand, and no Migration 117 additions. Migration 117 appears once in unchanged context (the surrounding block before the new Migration 118 insertion point). The diff is scoped to the digest implementation.

**Fix 3 — Dead `db_path` parameter (Issue 3):**

Verified. The new `format_digest` signature:
```python
def format_digest(
    groups: dict[str, list[dict]],
    stalled: list[dict],
    lookback_hours: int,
    now_dt: datetime,
    seeds_total: int,
) -> str | None:
```
No `db_path` parameter. All test call sites use the new signature: `md.format_digest(groups, stalled, lookback_hours, self._now_dt, seeds_total)`. The old `now_iso: str` parameter has been replaced with `now_dt: datetime`, which is a correct improvement — the function was receiving a string it had to parse when it could receive a datetime directly.

### Additional quality observations (Round 2)

No new issues were introduced by the rebase. The test refactoring is clean:

- `TestClassifyUow` (heuristic-based, 17 tests) replaced by `TestResolveOutcomeCategory` (DB-field-based, 9 tests). Coverage is appropriate for the new code path — null fallback, unrecognized value fallback, and terminal status override are all tested.
- `TestJobsJsonGate` updated to use the shared `src.utils.jobs.is_job_enabled` rather than the deleted local `_is_job_enabled`. The importlib-based test helper correctly exercises the module through its standard entry point.
- DB fixture correctly adds `outcome_category TEXT NULL`, `gate_fired TEXT NULL`, and `seeds_surfaced TEXT NULL` columns, ensuring tests match the real migration 0018/0019 schema.

The `aggregate_gate_churn` function correctly excludes the `none` gate label from the churn count (clean dispatch paths are signal-negative for churn). This is a behavioral correctness decision that is now test-covered.

The idle-day suppression pattern (`format_digest` returning `None` → caller suppresses send) is correctly implemented and tested. This applies the constraint-4 principle (minimize metabolic cost) structurally rather than procedurally.

### Patterns introduced (carry-forward from Round 1)

The idle-day suppression (returning None from a formatting function rather than an empty-string sentinel) and the pre-computed aggregates pattern for `format_digest` are both sound. These remain worth propagating to future digest-style formatters.

---

## Summary

All three Round 1 blockers resolved cleanly. No new issues introduced by the rebase or cleanup. The PR is scoped correctly to a single workstream. Implementation quality is consistent with Round 1 findings on what the PR does well. Verdict: APPROVED.

---

## Round 1 — 2026-05-18 (archived)

### Stage 1: Vision Alignment

**Prior entering this review:** This implementation is solving the wrong problem, or solving the right problem in a direction that forecloses better paths.

**Theory of change test:** vision.yaml current_focus names "WOS bidirectional interface (#1118)" and "WOS completion notification layer" as the active constraints. A daily digest of completion outcomes is a downstream reporting surface — it converts classified completion data into a pulse message Dan sees in the morning. This is a legitimate integration task: outcome_category was wired in PR #759 but the digest was still using heuristic re-classification, meaning the architectural work of real-time classification had no surface expression. Replacing the heuristic with outcome_category is "wire what exists before building more" (vision.yaml principle-4) and directly reduces the divergence between architecture intent and the actual daily signal.

**What would have to be true:** That Dan benefits from a morning pipeline health signal, and that the accuracy of outcome_category over heuristic classification is meaningful enough to justify the scope of this PR. Both are plausible. The heuristic classifier was known to be fragile (keyword matching against close_reason), and the real-time classification at write_result time is structurally more reliable.

**Foreclosure check:** The PR does not foreclose the bidirectional interface (#1118) or any structural WOS work. It is a leaf node on the output surface, not an architectural decision.

**Opportunity cost:** This is lower priority than the stated current constraint (#1118), but it is also not a blocking distraction — it can run in parallel without consuming Dan's attention. The metabolic cost is low.

**Feature bundling observation:** The PR contains three distinct workstreams across three commits:
1. `/wos uow <id>` dispatcher subcommand (commits 1 and 2)
2. `outcome_category` wiring into the daily digest (commit 3)

The learnings.md entry for 2026-05-14 (#1148) names feature-bundling as a NEEDS_CHANGES blocker when distinct workstreams are represented. However, the calibration note added 2026-05-18 (#1161) distinguishes: same-system non-behavioral cosmetics = note only; distinct UoW workstreams = block. These two changes likely belong to different issues and the PR title only describes the digest change. The `/wos uow` command is a non-trivial addition (198-line task file, 52 lines in dispatcher_handlers.py, a migration). This is not an incidental cosmetic bundled with the primary change — it is a distinct feature that adds observable dispatcher behavior. The 2026-05-14 threshold applies.

**Alignment verdict: Confirmed** — the digest outcome_category wiring is aligned with vision.yaml principle-4. The `/wos uow` command is also within scope of WOS operational tooling. However, the feature-bundling pattern requires either unbundling or explicit acknowledgment.

---

### Stage 2: Quality Review (Round 1)

#### Encoded Orientation check (OODA constraint-3)

The PR modifies `sys.dispatcher.bootup.md` (a behavioral defaults document). This constitutes an Encoded Orientation decision — specifically, adding a new dispatcher routing path (`/wos uow <id>`). The vision.yaml anchor is traceable: WOS operational tooling within current_focus. The dispatcher_handlers.py addition is a proper implementation with a corresponding routing entry. The orientation change is bounded and traceable. This check passes.

#### Issues requiring changes (Round 1)

**Issue 1: Migration 118 cron detection pipe is broken (BLOCKER)** — See Round 2 for resolution.

**Issue 2: Feature bundling — two distinct workstreams in one PR (BLOCKER per learnings.md 2026-05-14)** — See Round 2 for resolution.

**Issue 3: `format_digest` signature receives `db_path` but does not use it — dead parameter** — See Round 2 for resolution.

**Issue 4: `_avg_int` return type annotation is imprecise** — Advisory, not blocking.

**Issue 5: `count_seeds_surfaced` JSON parsing of `seeds_surfaced` column is speculative** — Advisory, not blocking.
