VERDICT: APPROVED
PR: 834
Round: 3

## Round 3 — 2026-05-23

### Prior gap tracking (Round 2 gaps)

All gaps from Rounds 1 and 2 enumerated before issuing new verdict.

**Gap 1 (Callback routing not wired):** ADDRESSED. `route_callback_message` in `dispatcher_handlers.py` now includes a `vision_accept:` / `vision_decline:` branch at line ~2539 that calls `handle_vision_callback` in the same file, which imports and delegates to `vision_inlet.handle_vision_callback`. The wiring is end-to-end and correctly integrated into the existing `route_callback_message` function on main (not redefined). This is the gap that drove Round 2's NEEDS_CHANGES; confirmed resolved.

**Gap 2 (yaml.dump destroys comments):** ADDRESSED. `handle_vision_accept` uses `_RUAMEL_YAML()` with `preserve_quotes=True`, loads via `_ry.load(vision_text)`, and writes back via `_ry.dump(vision_data, buf)` into a `StringIO` buffer before writing to disk. `ruamel.yaml>=0.18.0` added to `pyproject.toml`. Comment preservation is correct.

**Gap 3 (Hardcoded chat_id=6036):** ADDRESSED. All production function signatures now default to `int(os.environ.get("LOBSTER_ADMIN_CHAT_ID", "8075091586"))` — in both `vision_inlet.py` and `dispatcher_handlers.py`. The test files still pass `chat_id=6036` as an explicit argument in tests 4 and 5, which is a test-only override and not a production default.

**Gap 4 (Eligibility scope lacks logged prior for Dan-only fields):** ADDRESSED. `_ELIGIBLE_EXACT` is `frozenset()` (empty). `core.inviolable_constraints` and `active_project.phase_intent` are excluded entirely from the eligible set. The module docstring explicitly states these are Dan-only fields not eligible for agent proposals. Only `current_focus.*` remains proposable, anchored by od-1.

**Gap 5 (PR unmergeable — branch conflict with main):** ADDRESSED. PR now shows `mergeable: MERGEABLE, mergeStateStatus: CLEAN`. The rebase integrated `vision_accept:` / `vision_decline:` branches into the existing `route_callback_message` function and `CALLBACK_DATA_HANDLERS` frozenset on main — not as a redefinition, but as additions to the existing function at lines 2444 and 2521. The `handle_vision_callback` function is added after the existing function block. The original `vision_inlet.py` additions are intact.

---

### Stage 1: Vision Alignment (unchanged from Rounds 1 and 2)

**Prior entering this review:** This implementation is solving the wrong problem, or solving the right problem in a direction that forecloses better paths.

**Theory of change:** vision.yaml's `fundamental_intent` requires "structural access to intent, not just prose reconstruction." The Vision Object inlet closes the missing loop: observation → validated proposal → binary Dan confirm → field write. This is anchored by od-1 (resolved 2026-03-28) authorizing agent proposals to `current_focus.*` with Dan notification. The reflective-surface-queue observation (2026-03-27) named "frictionless inlet pathway" as the structural gap being addressed.

**Learning from golden-patterns.md applied:** The "adversarial prior seeding" pattern (2026-03-27) was applied — Stage 1 finding formed before reading the diff. The "oracle vocabulary as detection precondition" pattern (2026-03-29) was applied — both files read before the scan began. The adversarial prior is disconfirmed: this is the right problem, correctly scoped.

**Constraint-3 status:** Fully cleared in Round 2. With `_ELIGIBLE_EXACT = frozenset()`, no unanchored Encoded Orientation path exists. The only proposable fields are `current_focus.*`, anchored by od-1.

**Alignment verdict: Confirmed.**

---

### Stage 2: Quality Review

#### Gap 5 confirmation (Gap 5 from Round 2)

The Round 2 blocker was that the PR's branch was rebasing onto a stale main, introducing a duplicate `route_callback_message` definition. This is now resolved. The diff shows:

- `CALLBACK_DATA_HANDLERS` at line 2444 of `dispatcher_handlers.py` receives two additions (`"vision_accept:"`, `"vision_decline:"`) to the existing frozenset — not a redefinition.
- `route_callback_message` at line 2460 receives a new branch (approx. lines 2539-2550) that checks `data.startswith("vision_accept:") or data.startswith("vision_decline:")` before the fall-through return — correctly inserted after the `decide_close:` branch, before the unknown-callback fallback.
- `handle_vision_callback` in `dispatcher_handlers.py` is added as a new function after the existing routing block — no collision with anything on main.

No duplicate definitions. The integration is clean.

#### End-to-end path verification

The complete callback path is now wired:
1. Dan taps Accept/Decline → Telegram sends callback with `vision_accept:<field_path>:<hash>` or `vision_decline:<field_path>:<hash>`
2. Dispatcher calls `route_callback_message(msg)` from `dispatcher_handlers.py`
3. `route_callback_message` checks `CALLBACK_DATA_HANDLERS` (now includes vision prefixes) and enters the vision branch
4. Vision branch calls `handle_vision_callback(data, chat_id=effective_chat_id)` in `dispatcher_handlers.py`
5. `handle_vision_callback` imports `handle_vision_callback as _vi_callback` from `src.harvest.vision_inlet` and delegates
6. `vision_inlet.handle_vision_callback` parses `field_path` and `phash`, routes to `handle_vision_accept` or `handle_vision_decline`
7. `handle_vision_accept` loads vision.yaml with ruamel.yaml (comment-preserving), writes the new value, removes from pending, appends to accepted log
8. Returns reply string → dispatched back to Dan via Telegram

Each step is traceable in the diff. The path was broken in Round 1 (step 3→4 was missing); fixed in Round 2 (wired but unmergeable); now clean in Round 3.

#### Quality observations (non-blocking, notes only)

**`import io as _io` inside function body:** `handle_vision_accept` imports `io` locally as `_io` inside the function body. `io` is a stdlib module with no transitive dependencies — it should be imported at module scope. The local import is harmless but is inconsistent with the module's other top-level imports. Note only; does not affect correctness.

**Test-only `chat_id=6036` (Round 2 note, still applies):** Tests 4 and 5 pass `chat_id=6036` as an explicit argument. This is not a production path issue — the production defaults are correct. The chat_id 6036 is a valid test override even if it names a non-canonical chat. The test assertions do not depend on the chat_id routing. Note only.

**`_navigate_yaml` receives `ruamel.CommentedMap`:** `handle_vision_accept` loads vision.yaml with ruamel.yaml's round-trip loader (returns `CommentedMap`), then navigates with `_navigate_yaml`, which uses `dict`-style `__contains__` and `__getitem__`. ruamel's `CommentedMap` fully supports both, so this works correctly. The round-trip comment preservation operates at write time via `_ry.dump()`. No defect.

**Double-restore in test 4 (`finally: vi.PENDING_JSON = original_pending`):** Same note as Round 2: the `monkeypatch` fixture already handles restoration; the manual `finally` block is redundant. Harmless ordering overlap, not a functional defect.

#### Test coverage assessment

Five tests cover:
1. Valid proposal targeting `current_focus.this_week.primary` passes validation.
2. Ineligible field (`vision.long_term_direction`) is rejected with reason.
3. No-op proposal (identical to current value) is rejected.
4. Accept handler writes new value to temp vision.yaml copy using ruamel (round-trip preserving).
5. Decline handler removes from pending and appends to discard log.

The `route_callback_message` integration with vision callbacks is not directly tested in this PR's test file — tests 4 and 5 call `handle_vision_accept`/`handle_vision_decline` directly rather than through `route_callback_message`. This is the same test surface as Round 2. The task prompt states "24 callback routing tests pass" (from `test_dispatcher_integration.py`), which presumably covers the existing `route_callback_message` function. Those 24 tests are not in this PR's diff. The absence of a test for the new `vision_accept:` branch in `route_callback_message` is a minor gap — the branch is simple (two startswith checks → delegate to `handle_vision_callback`), and the coverage from tests 4/5 validates the handlers themselves. Not a blocking issue.

#### Encoded Orientation check (constraint-3)

The PR creates a mechanism by which agents can propose writes to `current_focus.*` via binary Telegram confirm. The eligible field set is restricted to `current_focus.*` only, anchored by od-1 (2026-03-28). No unanchored Encoded Orientation path exists. Constraint-3 is satisfied.

#### Pattern introduced

The binary confirm pattern (inline keyboard Accept/Decline with pending JSON storage + ruamel.yaml round-trip write) is a clean, reusable pattern for agent-originated structural changes to signed data files. This pattern is a golden pattern candidate: agent proposes → Dan confirms → comment-preserving field write. No other component in the codebase implements this pattern; this PR establishes it at a correct seam.

---

### Verdict summary

All five named gaps are addressed. The PR is MERGEABLE/CLEAN. The end-to-end callback path is correctly wired into the existing `route_callback_message` function on main without conflict or duplication. The ruamel.yaml comment-preservation is correctly implemented. The eligible field set is correctly restricted to `current_focus.*` with od-1 as the anchor. No new blocking gaps were found in Round 3.

---

## Round 2 — 2026-05-23

### Prior gap tracking

All four Round 1 gaps enumerated with current status before issuing new verdict.

**Gap 1 (Callback routing not wired):** ADDRESSED. `route_callback_message` in `dispatcher_handlers.py` now includes `vision_accept:` and `vision_decline:` branches that call `handle_vision_callback`. The wiring is end-to-end: callback_data → branch detection → `handle_vision_callback` in `dispatcher_handlers.py` → delegates to `handle_vision_callback` in `vision_inlet.py` via local import → parse field_path/hash → `handle_vision_accept` or `handle_vision_decline` → write vision.yaml.

**Gap 2 (yaml.dump destroys comments):** ADDRESSED. `handle_vision_accept` now uses the ruamel.yaml round-trip loader (`_RUAMEL_YAML()` with `preserve_quotes=True`). The authority model comment block is preserved on write. `ruamel.yaml>=0.18.0` added to `pyproject.toml`.

**Gap 3 (Hardcoded chat_id=6036):** ADDRESSED in all production function signatures. `dispatch_vision_proposals`, `handle_vision_accept`, `handle_vision_decline`, `handle_vision_callback` (both files) now default to `int(os.environ.get("LOBSTER_ADMIN_CHAT_ID", "8075091586"))`. The test file still passes `chat_id=6036` explicitly in tests 4 and 5 — this is a test-only explicit override, not a default, and does not affect production behavior. The gap as stated was about defaults; the defaults are fixed.

**Gap 4 (Eligibility scope lacks logged prior for Dan-only fields):** ADDRESSED. `_ELIGIBLE_EXACT` is now `frozenset()` (empty). `core.inviolable_constraints` and `active_project.phase_intent` are removed from the eligible set entirely. The module docstring explicitly notes these are Dan-only fields not eligible for agent proposals. Only `current_focus.*` remains proposable, anchored by od-1 (2026-03-28). The dead constraint-append branch has been removed.

---

### Stage 1: Vision Alignment (unchanged from Round 1)

**Prior entering this review:** This implementation is solving the wrong problem, or solving the right problem in a direction that forecloses better paths.

**Theory of change:** vision.yaml's fundamental_intent requires "structural access to intent, not just prose reconstruction." The Vision Object inlet closes the missing loop: observation → validated proposal → binary Dan confirm → field write. This is anchored by od-1 (resolved 2026-03-28) authorizing agent proposals to `current_focus.*` with Dan notification. The reflective-surface-queue observation (2026-03-27) named "frictionless inlet pathway" as the structural gap being addressed.

**Prior disconfirmed.** This is the right problem. Stage 1 finding unchanged from Round 1.

**Constraint-3 status:** With Gap 4 resolved (eligibility restricted to `current_focus.*` only), the constraint-3 flag from Round 1 is fully cleared. The eligible scope is now anchored entirely by od-1. No unanchored Encoded Orientation path remains.

**Alignment verdict: Confirmed.**

---

### Stage 2: Quality Review

#### New blocking gap discovered in Round 2

**Gap 5: PR is unmergeable — dirty conflict with main**

The PR's `mergeable_state` is "dirty" and `mergeable` is false. The PR was opened on 2026-04-22 against a version of `dispatcher_handlers.py` that was approximately 598 lines. The production `main` branch now has `dispatcher_handlers.py` at 3153 lines, including `route_callback_message` and `CALLBACK_DATA_HANDLERS` added by a subsequent PR (visible at lines 2444-2521 of the current production file).

The PR's diff adds 157 lines after what was line 595 of the base (after `route_wos_message`), including a full definition of `route_callback_message`. Merging this PR as-is would introduce a second definition of `route_callback_message` and `CALLBACK_DATA_HANDLERS` into `dispatcher_handlers.py`. Python would resolve this by using the latter definition (the one from this PR), silently overriding the existing production function that other callers depend on. Alternatively, GitHub cannot apply the diff cleanly and will block the merge.

**The required fix:** Rebase `feat/vision-inlet-mechanism` onto current main. After rebase:
- The `vision_accept:` / `vision_decline:` branch should be added to the existing `route_callback_message` function (which already has `decide_retry:` and `decide_close:` branches).
- `CALLBACK_DATA_HANDLERS` in the PR should add `vision_accept:` and `vision_decline:` to the existing frozenset, not redefine the entire frozenset.
- The new `handle_vision_callback` function in `dispatcher_handlers.py` should be retained as an addition after the existing function block.
- The new `vision_inlet.py` additions (the bulk of the PR) are not in conflict and will apply cleanly.

This is not a design gap — the gap-fixing logic in Round 2 commit 8161c005 is correct. The merge conflict is a branch staleness issue caused by main advancing while the PR was in oracle review.

#### Remaining quality observations (non-blocking, noted)

**Test-only `chat_id=6036` in tests 4 and 5:** Tests 4 (`test_accept_handler_writes_vision_yaml`) and 5 (`test_decline_handler_discards_proposal`) pass `chat_id=6036` as an explicit argument. This is not a production default issue (Gap 3 is resolved), but it is a test hygiene signal: a test using a chat_id that does not exist in the system is likely a copy-paste holdover from Round 1. Not a blocking gap — the tests exercise write correctness, not delivery routing. The `chat_id` parameter is accepted and stored but not used by `handle_vision_accept` or `handle_vision_decline` in any behavioral path that matters for the test assertion. Note only.

**ruamel.yaml navigation via `_navigate_yaml`:** `handle_vision_accept` loads vision.yaml with `_ry.load()` (ruamel CommentedMap) but navigates with `_navigate_yaml`, which uses normal `dict`-style access. ruamel.yaml's `CommentedMap` supports `__getitem__`/`__contains__` compatible with dict access, so this works correctly — the round-trip comment preservation happens at write time via `_ry.dump()`, not at navigate time. No defect; note for future reviewers.

**`_set_yaml_value` applied to ruamel CommentedMap:** `_set_yaml_value` does `current[path_parts[-1]] = value` where `current` is a `CommentedMap`. Assignment to a CommentedMap key works and does not strip comments from surrounding keys. Correct behavior confirmed by ruamel.yaml documentation.

**Double-restore in test 4 (`finally: vi.PENDING_JSON = original_pending`):** The Round 1 test hygiene note applies: the `finally` block manually resets `vi.PENDING_JSON = original_pending`, but `monkeypatch` in `tmp_data_dir` fixture already handles teardown. The double-restore is harmless but leaves a subtle ordering dependency. Not a blocking gap — same note as Round 1.

---

### Revision contract for Round 3

**Gap 5 (Unmergeable — branch conflict with main):** Address. Rebase `feat/vision-inlet-mechanism` onto current main. Integrate the vision callback branch into the existing `route_callback_message` function rather than redefining it. The test suite for `dispatcher_handlers.py` already has tests for `route_callback_message` at `tests/unit/test_orchestration/test_dispatcher_integration.py` — verify tests still pass after rebase. The vision-specific logic is correct and does not need to change; only the merge integration needs work.

---

## Round 1 — 2026-05-23

### Stage 1: Vision Alignment

**Prior entering this review:** This implementation is solving the wrong problem, or solving the right problem in a direction that forecloses better paths.

**Theory of change in vision.yaml this PR serves:**

The vision's fundamental intent is a system with "structural access to my intent, not just a prose reconstruction of it." Phase 1 success criterion sc-1 is: "removing vision.yaml would cause structurally different routing outcomes." The vision's active constraint is closing the observation-loop: the Vision Object solves routing but not the developmental ceiling — the inlet pathway (observation → field-change proposal → Dan confirm) was identified as the missing piece in a reflective-surface-queue observation dated 2026-03-27.

PR #834 builds exactly that missing piece. This is confirmed by: (1) the reflective-surface-queue.json entry naming "frictionless inlet pathway" as the structural gap; (2) vision.yaml active_project.phase_intent naming "build the substrate that lets every agent make intent-anchored decisions"; (3) od-1 (resolved 2026-03-28) establishing that agents may propose changes to current_focus.* with Dan confirmation.

**Is this solving the right problem?** Yes — confirmed. The vision inlet closes the loop from observation to field-change with Dan gating every write. The prior is disconfirmed for the core problem statement.

**What would have to be true for this to be the right path?** The binary confirm mechanism must actually be wired to the dispatcher callback routing path. If it is not, no proposal can ever be accepted or declined — the UI sends the button, Dan taps it, and nothing happens. This is the operative question for Stage 2.

**What does this foreclose?** The eligible field list encodes a permanent authority boundary via code rather than via the vision.yaml authority model header. Any change to which fields agents may propose now requires a code change to `_ELIGIBLE_EXACT` and `_CURRENT_FOCUS_PREFIX`, not a vision.yaml edit. This is a mild foreclosure but not a blocker — the field list is a reasonable seam for Phase 1.

**Encoded Orientation check (constraint-3):** This PR creates a mechanism by which agents can propose writes to `core.inviolable_constraints` and `active_project.phase_intent` via binary Telegram confirm. These are Dan-only fields per the vision.yaml authority model header. The mechanism is gated by Dan's explicit Accept tap — so it is not automated write. However, it establishes encoded orientation (a code path that can alter Dan-only structural fields) with a behavioral precedent. The od-1 decision (2026-03-28) authorized agent updates to `current_focus.*` and `life_domains.*` only. It did not authorize agent-originated proposals for `core.*` or `active_project.phase_intent`. This is a partially-unanchored Encoded Orientation decision: the `current_focus.*` eligibility is anchored by od-1; the `core.inviolable_constraints` and `active_project.phase_intent` eligibility is not anchored by a logged decision. A vision.yaml header note is not a logged decision — it states current authority assignment, not an authorization for proposal mechanisms.

**Alignment verdict: Confirmed (with constraint-3 flag on eligible field boundary).**

---

### Stage 2: Quality Review

**Does it do what it claims to do?**

Partially. The module correctly validates proposals, dispatches binary Telegram confirms with inline buttons, stores pending proposals atomically, and handles accept/decline via write to vision.yaml. The validation logic (eligible field check, field existence check, no-op check) is sound. The tests cover the five named scenarios.

However, there is a structural gap that renders the end-to-end path non-functional: **the callback handler is not wired into `route_callback_message`**. When Dan taps Accept or Decline, the dispatcher calls `route_callback_message(msg)` from `dispatcher_handlers.py`. That function currently handles only `decide_retry:` and `decide_close:` prefixes, then returns `handled=False` for everything else. The vision callbacks (`vision_accept:`, `vision_decline:`) fall through to the dispatcher's prose-based job-confirm/delete-confirm handling, which does not recognize them. No write to vision.yaml occurs.

The PR adds a `handle_vision_callback` shim to `dispatcher_handlers.py` with a usage comment showing correct integration, but the shim is never called from `route_callback_message`. This is a wiring gap, not a design gap.

**Gap 1: Callback routing not wired into route_callback_message**

The `handle_vision_callback` function in `dispatcher_handlers.py` is defined but not called by `route_callback_message`. The fix: add a branch in `route_callback_message` before the fall-through return that checks for `vision_accept:` and `vision_decline:` prefixes and routes to `handle_vision_callback`. Without this, every Accept/Decline tap by Dan silently produces "Unknown callback: vision_accept:..." as the reply, and vision.yaml is never written.

**Gap 2: yaml.dump silently destroys vision.yaml's authority model header**

In `handle_vision_accept`, the accepted field value is written back using `yaml.dump(vision_data, default_flow_style=False, allow_unicode=True)`. PyYAML's `yaml.dump` strips all YAML comments. The vision.yaml file begins with a 14-line comment block that constitutes the authority model for the entire system:

```
# Vision Object — Dan's intent at three temporal scales.
# Private: not committed to the Lobster repo.
# Authority model:
#   core.*                          — Dan only
#   active_project.phase_intent     — Dan only
...
```

On the first accepted proposal, this entire authority model is silently destroyed. The file still loads correctly as YAML, but the human-readable governance record is gone. A downstream agent or engineer reading vision.yaml for context would encounter a file with no authority model header, treating the loss as a pre-existing condition.

The fix: use a read-then-targeted-write approach (e.g., ruamel.yaml which preserves comments, or a regex-based field replacement that preserves the comment block). For Phase 1, the simplest acceptable fix is to use ruamel.yaml's round-trip loader, which is the idiomatic approach in Python for comment-preserving YAML writes.

**Gap 3: Hardcoded chat_id=6036 contradicts the established LOBSTER_ADMIN_CHAT_ID pattern**

`dispatch_vision_proposals`, `handle_vision_accept`, `handle_vision_decline`, and `handle_vision_callback` all default to `chat_id=6036`. The codebase's established pattern is `os.environ.get("LOBSTER_ADMIN_CHAT_ID", "8075091586")` — visible in `inbox_write.py`, `wos_throttle.py`, and `dispatcher_handlers.py`. The chat_id 6036 does not appear as an authoritative chat_id anywhere else in the codebase; the admin chat is 8075091586.

This means vision proposals on a canonical deploy would be dispatched to the wrong chat (6036), where they would be invisible to Dan. The default would need to be the env var pattern to be correct and portable across instances.

**Gap 4: active_project.phase_intent and core.inviolable_constraints eligibility lacks a logged prior decision**

vision.yaml's authority model header states: `active_project.phase_intent — Dan only` and `core.* — Dan only`. od-1 authorized agent updates to `current_focus.*` and `life_domains.*` with notification. It did not authorize agent-originated proposals for structural fields, even with a human confirm gate.

The PR extends proposal eligibility to these Dan-only fields without a logged decision. While the confirm gate provides a safeguard, establishing a code path that can alter Dan-only structural fields is an Encoded Orientation decision (constraint-3) that requires a logged prior. The fix: either (a) remove `core.inviolable_constraints` and `active_project.phase_intent` from the eligible set and restrict to `current_focus.*` only (anchored by od-1), or (b) log a decision in `oracle/verdicts/` explicitly authorizing proposal-plus-confirm as the mechanism for structural fields, with a vision.yaml anchor.

**Test observation (non-blocking):**

Test 4 (`test_accept_handler_writes_vision_yaml`) has a resource-management issue: it saves pending to the un-monkeypatched `vi.PENDING_JSON` (the live production path, not `tmp_data_dir`) due to the `try/finally` saving `original_pending` but calling `_save_pending({phash: proposal})` *before* the path is redirected in the `try` block. The `tmp_data_dir` fixture redirects `vi.PENDING_JSON` via monkeypatch, but the test directly calls `_save_pending` from the module (not through the monkeypatched attribute) and then reads `vi.PENDING_JSON` in the assertion. This has a subtle ordering issue: if `_save_pending` is called before `vi.PENDING_JSON` is set to `tmp_path / ...`, the file writes to production. Reading the test more carefully: `monkeypatch.setattr(vi, "PENDING_JSON", ...)` runs in the `tmp_data_dir` fixture before the test body, so the path IS redirected before `_save_pending` is called in the test. The test also stores `original_pending = vi.PENDING_JSON` and restores it in `finally` — but `monkeypatch` already handles restoration. The double-restore with `vi.PENDING_JSON = original_pending` in the `finally` block bypasses monkeypatch's cleanup order, potentially leaving production path exposed between the `finally` and monkeypatch teardown. This is a minor test hygiene issue, not a functional defect. Flag as note, not a revision-contract gap.

**What failure modes exist?**

1. Dan taps Accept → "Unknown callback: vision_accept:..." reply. No write occurs. (Gap 1 — functionally broken)
2. Dan accepts any valid proposal → vision.yaml comment block destroyed. (Gap 2 — data loss)
3. On a canonical deploy, proposals dispatched to wrong chat. (Gap 3 — wrong recipient)
4. Agent proposes change to core.inviolable_constraints → writes a new constraint with auto-generated id, rationale from the proposal's `basis` field — no review of constraint wording quality, no canonical id format check.

**What patterns does this introduce that will propagate?**

The binary confirm pattern (inline keyboard Accept/Decline with pending JSON storage) is a clean, reusable pattern for agent-originated structural changes. If the three blocking gaps are resolved, this pattern should be cited as a golden pattern candidate for other "agent proposes, Dan confirms" flows.

The inline import pattern in `_process_vision_proposals` (try `from src.harvest.vision_inlet` → fallback to `importlib.import_module("vision_inlet")`) is consistent with existing patterns in the codebase for cross-module imports in scheduled-task contexts.

---

### Revision contract

Each named gap must be resolved in one of three ways: (a) addressed — the revision shows the thing the gap named, (b) disputed — the author states why the gap does not apply, with specific reason, (c) deferred — the author acknowledges the gap and states why it is not addressed now.

**Gap 1 (Callback not wired):** Address. Add the vision callback branch to `route_callback_message`.

**Gap 2 (yaml.dump destroys comments):** Address. Use ruamel.yaml round-trip loader for comment-preserving writes, or use a targeted string-replacement approach that does not touch comment lines.

**Gap 3 (Hardcoded chat_id=6036):** Address. Replace default `chat_id=6036` with `int(os.environ.get("LOBSTER_ADMIN_CHAT_ID", "8075091586"))` in `dispatch_vision_proposals` and `handle_vision_callback`. The function signatures can keep `chat_id` as a parameter, but the default must use the env var.

**Gap 4 (Eligibility scope lacks logged prior for Dan-only fields):** Address or dispute. Either restrict eligible fields to `current_focus.*` (anchored by od-1), or log a decision in `oracle/verdicts/decision-vision-inlet-authority.md` citing a specific vision.yaml anchor and authorizing proposal-plus-confirm for structural fields.
