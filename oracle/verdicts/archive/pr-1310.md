VERDICT: APPROVED

## PR #1310 — fix: correct STATUS_ORDER in dispatcher-wos-query.md

**Oracle round:** 1
**UoW:** uow_20260525_835c08
**Coordinator task_id:** wos-pr-coord-1310
**Reviewed:** 2026-05-25

---

## Stage 1: Vision Alignment

**Adversarial prior:** Is this fixing the wrong problem or foreclosing better paths?

No. The PR targets a named defect documented in `oracle/learnings.md` (PR #1172 entry): a display-ordering list that diverged silently from the production enum. The correct fix is exactly this — synchronize the list to the canonical enum. No architectural alternative exists or is preferable; the list is intentionally a display-ordering hint, not enum enforcement. The extras fallback remains for future additions.

---

## Stage 2: Implementation Correctness

**Enum coverage check (UoWStatus, registry.py:47-82):**

Post-PR STATUS_ORDER contains 15 values. UoWStatus enum contains 15 values. Complete overlap confirmed — all enum values are present, no phantom values remain.

Removed phantom values:
- "paused" — confirmed absent from UoWStatus enum
- "completed" — confirmed absent from UoWStatus enum (correct terminal is "done")

Added real values:
- "ready-for-executor" — UoWStatus.READY_FOR_EXECUTOR confirmed
- "diagnosing" — UoWStatus.DIAGNOSING confirmed
- "awaiting-owner" — UoWStatus.AWAITING_OWNER confirmed
- "failed" — UoWStatus.FAILED confirmed
- "closed" — UoWStatus.CLOSED confirmed

**Ordering review:**
1. Active execution states: active, executing, ready-for-executor, ready-for-steward — correct
2. Escalation/blocked: diagnosing, needs-human-review, awaiting-owner, blocked — correct
3. Intake queue: pending, proposed — correct
4. Terminal (failure first): failed, done, expired, cancelled, closed — correct

Highest-signal failure state (failed) now appears in the prioritized section, not in the extras fallback. This resolves the exact defect named in learnings.md PR #1172.

**Pattern checks:**
- StrEnum golden pattern: raw strings in a task file are correct and necessary — the task file is a markdown prompt, not importable Python. Values match the enum. No violation.
- Feature bundling: single file, single purpose. No bundling.
- Mirror-constant risk: N/A — this is a display-ordering list, not a frozenset gating logic. Future enum additions fall through to the extras fallback gracefully.

**No gaps identified.**

---

## Decision

VERDICT: APPROVED — merge when ready.
