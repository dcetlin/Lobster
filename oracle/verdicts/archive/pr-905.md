---
oracle_status: approved
oracle_pr: https://github.com/dcetlin/Lobster/pull/905
oracle_date: 2026-04-24
---

VERDICT: APPROVED

## PR #905 — feat(wos): add route_callback_message for compaction-resilient callback dispatch (issue #901)

**Reviewed:** 2026-04-24 (R3 — post-gap-fix re-review)

---

## Prior Gap Tracking

Round 2 issued two blocking gaps. Status at R3:

- **Gap 1 (Merge conflict):** Addressed. PR rebased onto origin/main after PR #903 merged. `mergeStateStatus: CLEAN`, `mergeable: MERGEABLE`. The conflict on `.claude/sys.dispatcher.bootup.md` is resolved — the verbose inline chains from PR #903 (steps 7-8) are replaced by PR #905's single-step encoding.

- **Gap 2 (Bootup doc contract incoherence):** Addressed. Step 7 now calls `route_callback_message(msg)` unconditionally for all `type="callback"` messages, branches on `result["handled"]` (True path: send_reply + mark_processed; False path: fall through to step 8), and does not assert that `handled` is always True. This matches the `route_wos_message` pattern the PR was required to follow.

---

## Stage 1: Vision Alignment

**Adversarial prior:** PR #903 already solved this problem adequately via bootup prose encoding, making PR #905 redundant complexity that introduces a two-source-of-truth problem.

**Disconfirming evidence:** The prior does not hold. The learnings.md PR #894 entry ("notification surface wired before callback handling creates user-facing dead buttons") documents the exact failure class this PR closes. PR #903 recreated that failure in a slower form by placing the handler in bootup prose — which loses reliability under context compaction. The `route_wos_message` golden pattern and vision.yaml principle-3 ("determinism over judgment for conditionals — if-then logic is code, not LLM instructions") both anchor this PR structurally. The compaction-resilient dispatch function is the correct encoding form.

**Alignment verdict:** Confirmed.

---

## Stage 2: Quality Review

### Prior gap status
Both gaps resolved — see Prior Gap Tracking above.

### Code quality
- `route_callback_message` is correct: two prefix branches, lazy Registry instantiation, proper fallthrough returning `handled=False` for non-WOS callbacks.
- `CALLBACK_DATA_HANDLERS` frozenset is correctly scoped as an enumeration of handled prefixes.
- Local import of `Registry` inside the function preserves module importability without a DB connection.
- Encoded Orientation check (constraint-3): this PR re-encodes an existing behavioral decision (route decide_retry/decide_close callbacks) in a compaction-resilient form. The prior logged decision is the PR #903 oracle approval and the PR #894 learnings.md entry naming this exact gap. Vision anchor: principle-3 and operating-principle-4. Satisfied.
- Feature bundling check: 3 files changed (bootup doc, dispatcher_handlers.py, test file), all three described in PR body. Clean.

### Bootup doc (R3 state)
Step 7 now encodes: call `route_callback_message(msg)` unconditionally; branch on `result["handled"]`. This is structurally equivalent to the `route_wos_message` dispatch pattern. The false path (handled=False) correctly falls through to step 8 (unknown callback reply). The encoding is dense but correct and survives compaction — the import path and branching logic are explicit.

### Test quality
10 new tests in `TestRouteCallbackMessage` cover: status transitions (retry → ready-for-steward, close → failed), `handled` flag, `chat_id` echo, and unknown callback fallthrough (`handled=False`). 82 total tests pass. Structurally sound. Advisory notes from R2 (tautological prefix tests, latent hard-cap test risk) remain advisory — neither is blocking at current code state.

---

**Patterns introduced:** `route_callback_message` as a compaction-resilient entry point for callback dispatch — same structural form as `route_wos_message`. `CALLBACK_DATA_HANDLERS` frozenset as enumeration of handled prefixes. Both extend existing patterns rather than introducing new ones.

**What this forecloses:** Nothing material. The `handled=False` fallthrough preserves extensibility for future callback types without bootup doc changes.

**Opportunity cost note:** None — this is the correct closure of the PR #903 reliability gap.
