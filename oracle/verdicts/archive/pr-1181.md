VERDICT: APPROVED
PR: 1181
Round: 3

## Round 3 — 2026-05-16

### Prior gap status

**Gap 1 (was blocking): test_wos_uow_detail_gen.py locally re-declared pricing constants**
Status: ADDRESSED (confirmed Round 2, re-verified Round 3). Lines 29–32 of the test file import `SONNET_4_6_INPUT_PER_MTK`, `SONNET_4_6_OUTPUT_PER_MTK`, and `SONNET_4_6_CACHE_READ_PER_MTK` from `src.orchestration.wos_uow_detail_gen`. The production constants are present at module scope (lines 48–50). No local re-declaration anywhere in the test file.

**Gap 2 (was blocking): exact-match uow_id branch had zero test coverage**
Status: ADDRESSED (confirmed Round 2, re-verified Round 3). Test count is 44 (confirmed via line count on `def test_` prefixes). Three new tests cover:
- `test_exact_match_uow_id_field_includes_matching_entry` — inclusion on exact match
- `test_exact_match_uow_id_field_excludes_mismatching_entry` — exclusion when uow_id differs
- `test_exact_match_takes_priority_over_regex_fallback` — priority semantics (explicit uow_id overrides regex match on task_id)

**Gap 3 (was blocking): bare python3 in new UOW_ID extraction block**
Status: ADDRESSED. Commit dbf8247c changed line 256 from `UOW_ID_VAL=$(python3 "${TMPPY3}" ...)` to `UOW_ID_VAL=$(uv run python3 "${TMPPY3}" ...)`. Revision contract verification command (`grep 'UOW_ID_VAL=\$(python3' scripts/token-ledger-collect.sh`) returns zero results on the PR branch. The pre-existing `PROMPT_TASK_ID=$(python3 "${TMPPY2}" ...)` at line 227 remains — this was advisory-only in Round 1, declared out of scope in Round 2's revision contract, and is not re-opened by this round.

### Stage 1: Vision alignment (locked from Round 1)

The PR closes an 83% attribution miss in WOS token cost tracking by stamping an explicit `uow_id` field at dispatch time. WOS observability is explicitly named in vision.yaml `current_focus.this_week.primary`. The approach is additive-only (regex fallback preserved for non-WOS entries). Nothing is foreclosed. Alignment verdict: Confirmed.

Oracle vocabulary applied: The mirror-constant pattern (learnings.md, eleven recurrences) was the active prior entering Round 1 review — it caused the test file constants to be checked first, which surfaced Gap 1. The bare-python3 pattern (learnings.md PR #821, PR #830) caused the new UOW_ID extraction block to be flagged, surfacing Gap 3. Both patterns produced behavioral change in the review — not labels.

### Stage 2: Quality review (Round 3)

All three gaps are closed. The functional implementation is correct and clean:

- `dispatcher_handlers.py`: `uow_id: {uow_id}` is a single additive line in the executor prompt frontmatter. Minimal, correct, no side effects.
- `token-ledger-collect.sh`: The new UOW_ID extraction block parses frontmatter via a temporary Python script. It uses `uv run python3` (line 256). The block correctly handles failure via `|| true`, producing an empty `UOW_ID_VAL` for entries without the field. The jq conditional object merge (`+ (if $uow_id != "" then {"uow_id": $uow_id} else {} end)`) correctly omits the field when empty — old-format entries are not polluted with an empty string.
- `wos_uow_detail_gen.py`: The branching logic is clean. When `explicit_uow_id` is truthy, exact-match semantics are used; when absent (the key is missing or None), the regex path fires unchanged. The `or ""` guard on `task_id` prevents NoneType issues on the regex fallback path.
- `tests/`: Gap 1 and Gap 2 both resolved. The priority test (`test_exact_match_takes_priority_over_regex_fallback`) is the most structurally important — it confirms the branching semantics (`if explicit_uow_id:` exits the else path correctly when uow_id is present but mismatching).

**Encoded Orientation check:** This PR adds a `uow_id` metadata field to executor prompt frontmatter. This is observability infrastructure — it adds a data field, not a behavioral default, routing rule, or agent identity change. No Encoded Orientation issue.

No new concerns introduced by the Round 3 fix commit. The fix is a single-character substitution (`python3` → `uv run python3`) with no other changes.

---

## Round 2 — 2026-05-16

### Prior gap status

**Gap 1 (was blocking): test_wos_uow_detail_gen.py locally re-declared pricing constants**
Status: ADDRESSED. The test file now imports `SONNET_4_6_INPUT_PER_MTK`, `SONNET_4_6_OUTPUT_PER_MTK`, and `SONNET_4_6_CACHE_READ_PER_MTK` directly from `src.orchestration.wos_uow_detail_gen`. The production constants exist at module scope (lines 48–50 of the production file). Mirror-constant divergence risk is closed.

**Gap 2 (was blocking): exact-match uow_id branch had zero test coverage**
Status: ADDRESSED. Three new tests added:
- `test_exact_match_uow_id_field_includes_matching_entry` — verifies the exact-match path includes entries where `uow_id` matches the target.
- `test_exact_match_uow_id_field_excludes_mismatching_entry` — verifies the exact-match path excludes entries where `uow_id` does not match the target.
- `test_exact_match_takes_priority_over_regex_fallback` — verifies that when `uow_id` is present and points elsewhere, the regex match on `task_id` is ignored.

All three tests are structurally correct: they test the actual branch behavior (`explicit_uow_id` present → exact match; absence falls back to regex). The priority test is the most important — it confirms the branching semantics (`if explicit_uow_id:` exits the else path) match the design intent.

**Gap 3 (was advisory): bare `python3` in token-ledger-collect.sh**
Status: NEW INSTANCE — not carried forward; new code introduced by this PR reproduces the pattern and the instance was introduced by this PR, not inherited.

Round 1 flagged as advisory the pre-existing `python3 "${TMPPY2}"` at the task-id extraction block (line 227 in the current file). That pre-existing call was not fixed in Round 2 (still bare `python3`). That remains an acknowledged carry-forward advisory.

However, this PR adds a new UOW_ID extraction block. The new block's invocation reads:
```bash
UOW_ID_VAL=$(python3 "${TMPPY3}" "${INPUT}" 2>/dev/null || true)
```
This is not the pre-existing line — it is new code introduced by this PR. The learnings.md entry (PR #821, PR #830) is explicit: "any new or modified `scripts/*.sh` file that embeds Python must use `uv run python -` or `uv run -` at the invocation site. Apply at oracle review whenever a bash script contains `python3` (bare)." This PR modifies `token-ledger-collect.sh` and the new block it introduces uses bare `python3`. The detection rule applies to new code, not only pre-existing code.

An advisory finding about a pre-existing violation does not grant permission for new lines added by the same PR to reproduce the violation. Round 1 named the advisory for line 227 (pre-existing). The new block's invocation is new code with the same defect — this instance is blocking.

The failure mode is consequential: if `python3` is not in PATH (because uv manages the Python installation), `UOW_ID_VAL` is silently empty for every ledger entry, causing every new-format entry to fall back to the regex path. The feature is inert with no error signal. This is the exact silent-failure scenario learnings.md names for this pattern.

### Stage 1 (locked from Round 1)

The change solves a real observability gap: ~83% of WOS token costs were invisible to the detail generator because the regex join was lossy. The fix is a direct join key — additive-only, backward-compatible, with regex fallback retained. Vision alignment is confirmed: WOS observability directly serves the WOS Phase 1 intent. Nothing is foreclosed.

### Stage 2 quality (Round 2)

Gaps 1 and 2 are fully resolved. The functional implementation is correct: `_fetch_token_data` branching is clean (explicit `uow_id` present → exact match; absent → regex), backward compatibility is structurally sound (non-WOS entries omit `uow_id`, regex path continues unchanged). `dispatcher_handlers.py` injection is minimal and correct. The jq conditional object merge correctly conditionally includes the field.

No Encoded Orientation issues. This PR does not change behavioral defaults, agent identity, or system constraints.

One gap remains: the new UOW_ID extraction block adds bare `python3` in new code introduced by this PR.

### Revision contract

One gap must be resolved before APPROVED verdict:

**Gap 3 (blocking — new instance in newly added code):** The new UOW_ID extraction block introduced by this PR calls `python3 "${TMPPY3}"`. Change this to `uv run python3 "${TMPPY3}"` to match the existing `uv run python3` pattern at line 156 of the same script and to satisfy the CLAUDE.md convention plus the learnings.md detection rule for modified .sh files.

This is a one-line fix. The pre-existing `python3 "${TMPPY2}"` at line 227 (the task-id extraction block) is out of scope for this gap — it was advisory-only in Round 1 and remains a pre-existing carry-forward. Only the new code introduced by this PR is in scope.

Addressed vs. not addressed is decidable by: `grep 'UOW_ID_VAL=\$(python3' scripts/token-ledger-collect.sh` returns no results.

---

## Round 1 — 2026-05-16 (preserved)

VERDICT: NEEDS_CHANGES

### Stage 1: Vision Alignment

**Epistemic prior entering review:** This implementation is solving the wrong problem, or solving the right problem in a direction that forecloses better paths.

**Theory of change in vision.yaml:** The active phase is "WOS Phase 1 + Vision Object substrate." The phase intent is to build substrate so agents make intent-anchored decisions. Principle-4 is "wire what exists before building more."

**Assessment:** The PR improves token cost attribution accuracy for WOS UoWs — closing an 83.4% miss rate on the regex-based join by stamping an explicit `uow_id` field at dispatch time and joining on it directly. This is observability infrastructure in a category that vision.yaml current_focus explicitly names as priority ("WOS observability and reliability"). The change is additive-only (new field, new field in ledger, fallback preserved). Nothing is foreclosed.

The adversarial prior is not confirmed. This is the right problem and the additive approach does not foreclose better paths.

**Alignment verdict: Confirmed.**

**Oracle vocabulary applied:**

Two named patterns from learnings.md actively constrained this review before reading the diff:

1. **Feature-bundling pattern** (five recurrences): The PR description is specific — three files, three scoped changes. I counted the changed files against the description before reading any diff content. The file list matches the stated scope exactly. No bundling detected.

2. **Mirror-constant pattern** (eleven recurrences, with PR #1116 explicitly naming this same file): learnings.md PR #1116 states "when authoring a new module with named constants, the companion test file should import those constants from the module rather than re-declaring them." This pattern constrained what I looked for first in the test file — I verified the pricing constants before reading test logic. The pattern was found. This is the finding that changes the verdict from APPROVED to NEEDS_CHANGES.

### Stage 2: Quality Review

**Gap 1 (blocking):** test_wos_uow_detail_gen.py re-declared `SONNET_4_6_INPUT_PER_MTK`, `SONNET_4_6_OUTPUT_PER_MTK`, `SONNET_4_6_CACHE_READ_PER_MTK` locally rather than importing from `src.orchestration.wos_uow_detail_gen`. Mirror-constant divergence risk per learnings.md PR #800, #967, #970 pattern.

**Gap 2 (blocking):** exact-match `uow_id` branch in `_fetch_token_data` had zero test coverage. The new branch was the primary functional change in the PR; the existing tests covered only the regex-fallback path.

**Gap 3 (advisory):** pre-existing bare `python3` at line 227 of `token-ledger-collect.sh`. Not blocking for Round 1; flagged for assessment in Round 2. The new code added by this PR also uses bare `python3` for the UOW_ID extraction block — flagged but called advisory in Round 1 because the pre-existing violation was advisory and both uses are in the same script.
