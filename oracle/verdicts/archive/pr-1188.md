VERDICT: APPROVED
PR: 1188
Round: 2

---

## Round 2 — 2026-05-18

### Prior gap status

**Gap 1 (Round 1): `detection_heuristic` and `threshold` fields re-introduced without logged design decision (constraint-3)**

Status: **addressed**

Commit 2be1b714 stripped both `detection_heuristic` and `threshold` from both new smell entries. The fix is verified against the PR branch. The two new entries now contain: `id`, `name`, `description`, `severity`, `examples`, `fix_pattern`, `recurrence_count`, `first_detected`, `last_detected`, `issue_ref`, `status` — no automation-implying fields. The fix matched the revision contract's path (a): strip the deferred fields to match the schema of the five existing entries.

The `examples` and `fix_pattern` fields, which Round 1 explicitly noted were not automation-implying and were not subject to the gap, are retained. This is correct.

---

## Stage 1: Vision Alignment (unchanged from Round 1)

The Round 1 Stage 1 finding is locked and stands without modification.

The work is correctly scoped and correctly motivated. Two named failure classes with concrete production incidents are formalized in the smell registry. A sweep instruction template encodes three harness patterns that prevent these failures. The theory of change is sound: name the class, encode the harness, let the template propagate forward. The change serves vision.yaml principle-1 (proactive resilience over reactive recovery) and principle-2 (ergonomics over shortcuts). The adversarial prior was not confirmed.

---

## Stage 2: Quality Review — Round 2

### Gap resolution verification

The fix commit (2be1b714) was applied cleanly. Comparing the PR branch `oracle/smell-patterns.yaml` against the diff:

- `legibility_failure/silent-contract-violation` entry: `detection_heuristic` field absent, `threshold` field absent. `examples` and `fix_pattern` present. All tracking fields present. Schema is consistent with the existing entries' non-automation-implying subset.
- `coherence_failure/missing-state-registry-read` entry: `detection_heuristic` field absent, `threshold` field absent. `examples` and `fix_pattern` present. All tracking fields present. Same verification holds.

The PR diff confirms this: the fix commit added zero new lines to the existing entries and only deleted the two deferred fields (+0/-12 net in the smell-patterns.yaml change from the original commit, after the fix commit). The fix is surgical and complete.

### Observation on existing entries

The five existing entries on the PR branch and on main both retain `threshold` fields. This is not a gap introduced by this PR — the diff shows no modification to existing entries (+57 lines, 0 deletions). The current state of main has already normalized (or not) those entries as a result of post-PR #884 work. PR #1188 does not worsen the existing state. The two new entries introduced by this PR are clean.

### Content review (no new concerns from Round 1)

The template structure, the smell definitions' content, and the CLAUDE.md update were reviewed in Round 1 and found to be correct. The fix commit does not change any of the content findings. Specifically:

- The three harness sections in `sweep-instruction.template.md` map correctly to the two failure classes, with enforcement rules that are checkable (shell examples, Python examples, elimination-rule format).
- The Classification States section's elimination-invariant rule is structurally sound.
- The `examples` and `fix_pattern` fields in both new entries are descriptive and correctly reference the template sections.
- The CLAUDE.md update encodes the template as a mandate for new sweep instructions, not as a suggestion.

### Encoded Orientation check (OODA constraint-3)

The fix resolves the constraint-3 gap. The two new entries no longer carry automation-implying fields. The template and smell definitions are descriptive documentation. The CLAUDE.md directive ("sweep and diagnostic agents must follow...") is advisory behavioral guidance, not encoded automation. No new Encoded Orientation concern is introduced by the fix commit.

### What this introduces

The **precondition-as-harness in declarative sweep instructions** pattern is introduced cleanly. This pattern converts abstract epistemic risks into checkable harness sections, mapping each section to a named smell in the registry. The traceability (harness section → smell ID → concrete incident) is explicit and load-bearing for future sweep audits.

This pattern was added to `golden-patterns.md` in Round 1 as part of the conditional quality analysis. That addition stands.

---

## VERDICT: APPROVED

The single Round 1 gap has been fully addressed. Both deferred fields (`detection_heuristic`, `threshold`) have been removed from both new smell entries. The revision contract's path (a) was applied correctly. The template and smell definitions are high-quality, correctly motivated, and correctly scoped. The PR is approved to merge.

---

## Round 1 — 2026-05-18

---

## Stage 1: Vision Alignment

**Adversarial prior:** this implementation is solving the wrong problem, or solving the right problem in a direction that forecloses better paths.

The PR names two recurring failure classes that produced real spurious escalations — a WOS label-rename causing false zero-count reads, and a sweep that classified a deliberately-paused system as stalled. These are not hypothetical smells; the PR description cites concrete incidents for each. The implicit theory of change is: name the failure class formally, encode prevention as harness patterns in a template, and lower the probability of recurrence in future sweep instructions.

What would have to be true for this to be the wrong path? The work would be wasted if: (a) future sweeps are not written from templates, making the template dead documentation; (b) the live negentropic-sweep.md (already updated directly per the PR description) is the only meaningful consumer and it cannot benefit from a retrospective template addition; or (c) a cheaper intervention (e.g., fixing the sweep's classifier directly) would eliminate recurrence without creating a new template artifact to maintain.

The PR addresses (c) by updating the live sweep outside the PR diff. The concern about (a) is real but not disqualifying — any template faces this adoption risk. The PR takes the structurally correct approach: name the class, encode the harness, let the template propagate forward. The smell definitions and template serve vision.yaml principle-1 (proactive resilience over reactive recovery) and principle-2 (ergonomics over shortcuts — the template keeps the correction path open for future sweep authors).

**What this forecloses:** Nothing material. The template is a Markdown file; it creates no architectural commitment. The smell entries are documentation. This is a low-stakes, reversible change.

**Opportunity cost:** The vision.yaml current_focus names WOS observability and reliability as primary. Adding harness infrastructure for sweep agents that prevents spurious escalations is directly in this lane. The work is well-prioritized.

**Stage 1 finding:** The work is correctly scoped and correctly motivated. The theory of change is sound. The adversarial prior is not confirmed. However, Stage 1 surfaces one structural concern that becomes Stage 2's primary finding: two fields re-introduced in the new smell entries (`detection_heuristic`, `threshold`) were explicitly removed from this file in PR #884 because they triggered the Encoded Orientation constraint-3 check. PR #884's verdict explicitly authorized their return only "when the automated retrospective system is oracle-reviewed and a design decision is logged." That condition is not met by this PR.

**Alignment verdict: Confirmed** — the problem is real, the approach is correct, the template and smell definitions are well-constructed. A single structural gap (field reintroduction without a logged decision) blocks approval.

**Patterns active from learnings.md that constrained this analysis:**

1. **learnings.md PR #884 "oracle gap resolved-by-deferral does not close when the deferred artifact is reintroduced in a follow-on PR"** — This pattern caused me to check whether PR #1188's new smell entries re-introduced any of the fields explicitly deferred in PR #884's resolution. Without this pattern, I would have read the new entries as additive documentation. Because of it, I checked field-level content against the PR #884 gap resolution before evaluating the PR's intent. The behavioral effect: I verified `detection_heuristic` and `threshold` presence before forming any other quality judgment. This is not a label — the check changed what I looked at first.

2. **learnings.md 2026-04-26 Meta "Documentation PR oracle check sequence"** — Explicitly directed me to run the Encoded Orientation check (item 3 in the sequence) before semantic review for documentation PRs. The sequence says: "any YAML or JSON file containing fields that imply automated action (issue_template, github_label, threshold-gated operations) requires a logged design decision." `detection_heuristic` and `threshold` are precisely the fields named in PR #882/#884 as automation-implying. Running this check as a pre-semantic gate meant the constraint-3 finding did not depend on reading the template's content — it was surfaced by field inspection.

3. **learnings.md PR #882 "Data file with behavioral intent added without design decision anchor"** — Reinforced that constraint-3 applies in a data layer (YAML fields), not only in agent definitions or CLAUDE.md. This prevented me from treating the smell registry as "just documentation" without checking whether the re-introduced fields cross the automation-implying threshold.

**Pattern from golden-patterns.md cited:**

- **golden-patterns.md "adversarial prior seeding before implementation review"** — Applied structurally: Stage 1 finding was written before evaluating the template quality or the smell definitions' content. The Stage 1 question (is the Encoded Orientation gap re-fired?) was formed before reading the diff in detail. This caused the constraint-3 gap to be named as a Stage 1 finding, not a Stage 2 discovery — which is the intended behavior of the two-stage structure.

---

## Stage 2: Quality Review

### Pre-semantic mechanical checks (documentation PR sequence from learnings.md 2026-04-26 Meta)

**1. Feature-bundling:** Three changed files: `CLAUDE.md` (1 line), `memory/canonical-templates/sweep-instruction.template.md` (new, 159 lines), `oracle/smell-patterns.yaml` (72 lines appended). PR description mentions all three explicitly (smell registry, template, CLAUDE.md). The workspace-only change to `negentropic-sweep.md` is correctly noted as outside this PR's diff. No undescribed files. Feature-bundling check: **PASS**.

**2. Path reference canonicality:** Template references `~/lobster-workspace/data/wos-config.json` and `~/lobster/memory/canonical-templates/sweep-instruction.template.md`. Both are canonical paths. The `wos-config.json` path matches the location referenced in CLAUDE.md and other system documents. No stale runtime paths detected. **PASS**.

**3. Encoded Orientation check (primary finding):** The two new smell entries in `oracle/smell-patterns.yaml` include:
   - `detection_heuristic` (present in both new entries)
   - `threshold` (present in both new entries)
   - `examples` (present in both new entries)
   - `fix_pattern` (present in both new entries)

   PR #884's verdict resolved the Encoded Orientation gap from PR #882 by removing `detection_heuristic`, `threshold`, `issue_template`, and `github_label` from all five existing entries. The verdict explicitly stated: "The deferred automation fields (`detection_heuristic`, `threshold`, `issue_template`, `github_label`) can be re-added in a future PR when the automated retrospective system is oracle-reviewed and a design decision is logged."

   This condition has not been met. No oracle review of an automated retrospective system appears in `oracle/verdicts/`. No design decision is logged in `oracle/verdicts/` or `meta/premise-review.md` for the automated smell-detection system.

   The learnings.md PR #884 pattern is explicit: "Any follow-on PR that reintroduces the same artifact must be checked against the deferred gap before review proceeds. Resolution-by-absence is not resolution-by-fix." The `detection_heuristic` and `threshold` fields are the exact fields named as deferred in PR #884. Their reintroduction re-fires constraint-3.

   The `examples` and `fix_pattern` fields are new field types not present in the original set. They do not directly imply automated action; they are descriptive. However, `detection_heuristic` and `threshold` do carry the implication: `threshold` is a machine-readable trigger count, and `detection_heuristic` is written as actionable prescriptions ("Before any query... verify the resource exists... If the resource is absent, the sweep must fail loudly"). These are not descriptions of failure; they are instructions for a detection agent.

   **Encoded Orientation check: FAILS.** The `detection_heuristic` and `threshold` fields require a prior logged design decision authorizing the automated retrospective system before they can be re-added. constraint-3 applies.

**4. Resolution-by-absence check:** The PR #884 gap was closed by field removal (resolution by absence), not by providing the missing design decision. The deferred gap re-fires now that the fields are reintroduced. This is not a new finding — it is the application of the named pattern.

### Quality of the template and smell definitions (conditional on gap resolution)

The template is structurally sound and well-executed:

- The three harness sections map precisely to the two named failure classes: Dependencies → silent-contract-violation; Operational Posture + Classification States → missing-state-registry-read. The mapping is explicit and cross-referenced in the template's Notes section. This is the right structure.
- The enforcement rules within each harness section are concrete: shell examples, Python examples, elimination-rule format. These are not advisory prose — they are checkable preconditions. This is the golden-patterns.md "coherence-narrative basin as named production constraint" applied correctly: the harness turns abstract epistemic principles into observable, checkable behaviors.
- The Classification States section's "conclude via elimination — not via positive assertion" rule directly addresses the binary-classifier failure mode. The WOS throughput classifier example is concrete and correct.
- The template correctly handles the edge case: "If a section genuinely does not apply, state 'N/A — [reason]' explicitly." This prevents the harness from being silently skipped for sweeps with no GitHub dependencies.
- The CLAUDE.md update correctly gates new sweep instructions on the template via an actionable directive.

The smell definitions themselves are accurate:
- `legibility_failure/silent-contract-violation` correctly names the class with a concrete example (label rename → false zero). The note about `write_result_not_back_propagated` being an instance of the class is accurate and adds value.
- `coherence_failure/missing-state-registry-read` correctly identifies the binary classifier failure mode: a valid third state ("deliberately paused") is not in the classifier's state space. The Concrete example is accurate.
- Severity (high) for both is correct — both caused production escalation errors.

**The quality concern is not the content. It is the field schema.**

The `detection_heuristic` fields read as detection agent instructions, not documentation. The `threshold: 1` values are machine-readable trigger counts that presuppose an automated detection pass. These are structurally identical to the fields removed in PR #884 for the same reason. If the intent is documentation only, these fields should be either (a) removed from the new entries to match the existing schema, or (b) backed by a logged design decision authorizing their purpose.

### What this introduces

The template introduces a positive structural pattern: **precondition-as-harness in declarative sweep instructions**. If adopted, this pattern prevents classification errors at the instruction layer rather than the detection layer. This is worth propagating.

The smell definitions introduce concrete failure-class vocabulary. Both are well-named, well-grounded in real incidents, and correctly placed in `oracle/smell-patterns.yaml`.

### What this forecloses

Nothing material. The template and smell definitions are additive and reversible. The gap does not implicate any design direction — it is a field-schema compliance issue.

---

## VERDICT: NEEDS_CHANGES (Round 1)

**One gap, revision contract:**

**Gap 1: `detection_heuristic` and `threshold` fields re-introduced without logged design decision (constraint-3)**

The two new smell entries include `detection_heuristic` and `threshold` fields that were explicitly deferred in PR #884. The PR #884 verdict named the condition for their return: "when the automated retrospective system is oracle-reviewed and a design decision is logged." That condition is not met.

Resolution options (three paths, one required):

**(a) Remove the deferred fields from the new entries.** Strip `detection_heuristic`, `threshold`, `fix_pattern`, and `examples` from both new smell entries to match the schema of the five existing entries. The smell definitions remain complete and useful without these fields. This closes the gap without requiring a design decision.

**(b) Provide the logged design decision.** Add a decision file to `oracle/verdicts/` (or equivalent logged location) authorizing the automated smell-detection system with a traceable vision.yaml anchor. The fields then have a prior and constraint-3 is satisfied. This is the path the PR #884 verdict authorized.

**(c) Dispute the gap.** State specifically why `detection_heuristic` and `threshold` in these two new entries are not automation-implying in the sense that triggered the PR #884 removal. If the intent is purely descriptive (i.e., the `detection_heuristic` field describes what a human reviewer should check, not what an automated agent would run), make this explicit in the field name or schema comment. A field rename (e.g., `detection_guidance` instead of `detection_heuristic`) paired with a schema comment clarifying that all automation-implying fields remain deferred would constitute a credible dispute.

Path (a) is lowest friction. Path (b) is the right long-term answer if the automated system is intended. Path (c) is valid if the naming was inadvertent.

**Verifiable: addressed when (a) the new entries match the schema of existing entries (no `detection_heuristic`, `threshold`), OR (b) a decision file exists at `oracle/verdicts/` authorizing the automated system, OR (c) the PR includes an explicit schema note distinguishing these fields from the automation-implying set and renames or annotates them accordingly.**

Note on `examples` and `fix_pattern`: these fields are new to the schema and are not automation-implying by themselves. They do not independently trigger constraint-3. They may be retained under any of the three resolution paths above, provided the author chooses to keep them.

---

## Patterns applied (for golden-patterns.md)

The template's three-harness structure applies the **"coherence-narrative basin as named production constraint"** (golden-patterns.md 2026-03-26) at the instruction layer: each harness section converts an abstract epistemic risk (silent-contract-violation, missing-state-registry-read) into a checkable precondition. This is the pattern applied correctly in a new domain (sweep instruction authoring).
