VERDICT: APPROVED

PR: #1423 — docs: add wos_prescribe handler section to dispatcher bootup doc
Repo: dcetlin/Lobster
Oracle round: 1
Reviewed: 2026-06-03

## Summary

This is a docs-only PR that adds a `### wos_prescribe` section to `.claude/sys.dispatcher.bootup.md`. The section documents the async prescription dispatch path that was previously undocumented, causing the dispatcher to silently drop these messages.

## Checks performed

### 1. File scope
Single file changed: `.claude/sys.dispatcher.bootup.md`. No Python source files touched. Confirmed docs-only.

### 2. Section positioning
New section inserted after `wos_owner_required` (line 808) and before `scheduled_job_trigger` (line 838) in the PR branch — matches the PR description claim exactly.

### 3. Field list accuracy
The doc lists these payload fields: `uow_id`, `uow_summary`, `reentry_posture`, `completion_gap`, `issue_body`, `cycles`, `new_cycles`, `selected_executor_type`, `prescribed_skills`, `vision_orientation`, `dan_register`, `steward_log`, `now_iso`.

Cross-checked against `steward.py` `_write_prescription_request()` (lines 3505–3531): the listed fields all exist. Additional fields written by steward (`uow_type`, `uow_source`, `success_criteria`, `diagnosis_section`) are consumed by the handler via `.get()` with defaults — they are optional and not required by the dispatcher mental model. The doc field list exactly matches the `handle_wos_prescribe` docstring in `dispatcher_handlers.py` (lines 1366–1370), making this consistent throughout.

### 4. Handler routing
`route_wos_message` routing table (line 1278) correctly maps `"wos_prescribe"` → `"handle_wos_prescribe"`. The doc's instruction to call `route_wos_message(msg)` and not `handle_wos_prescribe` directly is correct and consistent with the pattern used by all other wos message type handlers.

### 5. Agent type and task_id pattern
Doc states spawned subagent is `lobster-generalist` with task_id `wos-prescribe-{uow_id[:8]}`. Both verified against `handle_wos_prescribe` return value (lines confirming `"agent_type": "lobster-generalist"` and `task_id = f"wos-prescribe-{uow_id[:8]}"`).

### 6. text='' and chat_id=0 behavior
Doc correctly states `text` is always `''` and `chat_id` is always `0`. Verified: the steward message dict does not include a `text` key (defaults to empty), and `"chat_id": 0` is hardcoded in steward.py line 3512.

### 7. No gaps found
The executor note asked to confirm the field list matches the actual message payload. After direct inspection of steward.py, the answer is: the doc field list matches the handler's documented interface (the "required fields" in the docstring). The additional optional fields are not relevant to the dispatcher's mental model.

## Conclusion

The PR correctly fills a documentation gap that was causing silent message drops. The content is accurate, the positioning is correct, and the field list matches the handler's interface. No changes needed.
