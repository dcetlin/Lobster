VERDICT: APPROVED

## PR #1373 — fix(wos): add 'operational' to UoWPosture enum

### Summary

Adds `OPERATIONAL = "operational"` to `UoWPosture` in `src/orchestration/registry.py`. UoWs with `posture='operational'` in the DB caused `ValueError` at the `_row_to_uow` deserialization boundary. This is a minimal, correct, additive fix.

### Checks

- Enum addition is in the right place, correct style, no downstream handler coverage gaps (confirmed: no code switches on UoWPosture values).
- `OPERATIONAL` exists in both `UoWPosture` and `UoWRegister` — different types, same string value, no confusion risk.
- Three regression tests follow the `TestClosedStatus` pattern exactly. SQL bypass correctly exercises the pre-existing DB state path.
- All 100 registry unit tests pass.
- No schema migration needed (TEXT column, no DB-level constraint).
- Note: the root cause of UoWs being inserted with `posture='operational'` (likely a register/posture conflation at insert time) is not addressed here, which is correct per scope boundary.

Safe to merge.
