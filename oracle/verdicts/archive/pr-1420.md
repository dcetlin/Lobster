VERDICT: APPROVED

## PR #1420 — Scope golden-patterns.md read to functional-engineer only

**Reviewed:** 2026-06-03
**Reviewer:** oracle-pr-1420-r1 (coordinator inline)
**Round:** 1

---

## Summary

This PR correctly diagnoses a structural misalignment: a 360-line context file being loaded by every subagent regardless of whether that agent writes code. The fix is well-scoped, minimal, and the execution matches the stated intent.

---

## Stage 1: Structural Analysis

### What the PR claims to do
- Remove the Golden Patterns mandatory-read directive from `sys.subagent.bootup.md`
- Add an equivalent directive to `functional-engineer.md` exclusively

### What the diff actually does
- Removes exactly 11 lines from `sys.subagent.bootup.md` (the Golden Patterns section)
- Adds exactly 9 lines to `functional-engineer.md` (a Golden Patterns section before `## Development Workflow`)
- No other files touched — confirmed via `gh api` file list

These match the stated intent exactly.

### Correctness checks

**Oracle agent unaffected:** `lobster-oracle.md` reads `golden-patterns.md` directly in its own definition (Stage 2 pre-read step). This PR does not touch that file — oracle behavior is preserved.

**Lobster-meta agent unaffected:** `lobster-meta.md` also reads `golden-patterns.md` via its own definition. Unaffected.

**Functional engineer receives the directive:** The new section in `functional-engineer.md` is placed before `## Development Workflow`, which fires before any code work begins. Placement is correct.

**Three load-bearing rules preserved:**
- StrEnum usage — preserved verbatim
- Dead-code analysis scope — preserved verbatim
- Canonicality / anti-staleness — preserved verbatim

**Language softening is appropriate:** The original phrasing ("mandatory, not advisory — load it before any code work begins, not as a reference to consult if something breaks") was written for a broad audience that might deprioritize the read. The functional-engineer agent is the only audience that matters here; the directive is explicit and actionable without the escalation language.

**No unintended side effects:** Non-code subagents (generalist, ops, hygiene, brain-dump, etc.) no longer carry a 360-line context file. This is the intended outcome and it is correct. The file remains available at `~/lobster/oracle/golden-patterns.md` for any agent that needs it.

---

## Stage 2: Golden Patterns Assessment

This PR applies the **canonicality / anti-staleness** golden pattern to its own context-loading configuration: a directive that belongs in the functional-engineer definition was sitting in the shared subagent bootup file. Moving it to its correct canonical home is the exact corrective action the pattern prescribes.

No new golden patterns arise from this change.

---

## Verdict Rationale

- Diff is clean and exactly matches stated intent
- No correctness issues found
- No unintended side effects
- Oracle and meta-agent golden-patterns reads are independently defined and unaffected
- Three load-bearing rules are preserved
- Placement in functional-engineer.md is correct (before Development Workflow)
- The change reduces context pressure for all non-code subagents without losing any behavioral coverage

APPROVED without changes required.
