VERDICT: APPROVED
PR: 1046
Round: 1

## Stage 1: Vision Alignment

This PR adds 8 pytest cases for `is_night4_rotation()` in `tests/unit/test_scheduled_tasks/test_decay_detector.py`. No production code changed.

**Adversarial prior (formed before seeing implementation):** Is this solving the wrong problem, or foreclosing better paths?

The prior does not hold. Test additions targeting known off-by-one regression surfaces on existing production logic serve vision.yaml principle-1 ("proactive resilience over reactive recovery") and principle-4 ("wire what exists before building more"). The decay detector is existing infrastructure; the boundary conditions at current_night == 4 and == 6 are a real regression surface given the sentinel convention (5 = post-Night-4). Adding tests to this surface is structural prevention, not accumulation of marginal coverage.

No paths are foreclosed. Nothing in vision.yaml's `what_not_to_touch` list applies. The work is not Encoded Orientation.

**Alignment verdict: Confirmed**

---

## Stage 2: Quality Review

### All 8 tests pass

```
8 passed in 0.70s
```

### Flagged Area 1: Subprocess stub pattern

The test file creates a `_subprocess_stub` `ModuleType` with `run` stubbed to a no-op lambda and `CalledProcessError` aliased to `Exception`. This stub is injected via `patch.dict("sys.modules", {"subprocess": _subprocess_stub})` inside `_load_decay_detector()` before `spec.loader.exec_module(mod)` runs.

The production module imports `subprocess` at module level (line 14) and the `run_gh()` function calls `subprocess.run(...)`. Because the stub replaces `subprocess` in `sys.modules` before `exec_module` resolves the import, the module binds to the stub. No real `gh` calls are made during test load or execution. Clean.

### Flagged Area 2: Monkeypatch target `decay_detector.HYGIENE_DIR`

`HYGIENE_DIR` is defined as a module-level constant in the production script (line 20). `is_night4_rotation()` accesses it at call time via `state_file = HYGIENE_DIR / "rotation-state.json"` (line 65) — not frozen at import time into a local variable.

`monkeypatch.setattr(decay_detector, "HYGIENE_DIR", tmp_path)` correctly patches the module attribute. The learnings.md PR #839 failure pattern ("module-level constant computed from env var at import time is not overridable by monkeypatch.setenv") does not apply here because the production function reads the module attribute at runtime, not a value frozen into a local at load time. The patch target is correct.

### Flagged Area 3: Off-by-one boundary tests (current_night == 4 and == 6)

Production: `return int(state["current_night"]) == 5`

- `test_returns_false_when_current_night_is_4`: N-1 boundary. An off-by-one error changing `== 5` to `>= 4` or `>= 5` would let this regress to True. Test correctly guards it.
- `test_returns_false_when_current_night_is_6`: N+1 boundary. An off-by-one error changing `== 5` to `<= 6` or `>= 4` would let this regress to True. Test correctly guards it.

Docstrings accurately describe the invariant: 5 is the post-Night-4 sentinel because the counter is incremented after the sweep. Boundary coverage is sound.

### Pattern checks from oracle corpus

**Mirror-constant risk (learnings.md PRs #696, #800, #967, #970, #974):** The test defines `NIGHT4_SENTINEL = 5` locally rather than importing from the production module. There is no named constant in the production script — the comparison is the inline literal `== 5`. Since no source constant exists to import, this is not a mirror-constant failure. If the production literal changes, `test_returns_true_when_current_night_is_5` fails loudly because `is_night4_rotation()` would return False for value 5. Divergence is detected by the test rather than silently. Not a blocker.

**Importlib load pattern (golden-patterns.md 2026-03-30):** The module is loaded via `spec_from_file_location`. The pattern's warning about registering in `sys.modules` before `exec_module` applies when `@dataclass` forward references need resolution. The decay detector uses no dataclasses, so the omission is correct here. Pattern applied appropriately.

**Encoded Orientation check (OODA constraint-3):** This PR introduces no behavioral defaults, agent identity changes, system constraints, or durable routing rules. Test-only. Constraint-3 does not fire.

### What this makes easier
Future modifications to `is_night4_rotation()` — sentinel value change, comparison operator change, key name change — fail loudly against 8 targeted cases. Regression surface is structurally covered.

### What this makes harder
Nothing.
