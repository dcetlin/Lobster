VERDICT: APPROVED

## PR #1063 — feat: pending-actions nudge job

### Stage 1: Right problem?

Yes. Dan needs a daily reminder about GitHub action-item issues that have been open for 3+ days. The current system has no proactive age-based nudging for pending items. This PR introduces a Type B cron-direct script that queries open `action-item` issues, buckets them by age (3d/7d/14d+), and sends a Telegram message when any bucket is non-empty.

### Stage 2: Well made?

**Script (`scheduled-tasks/pending-actions-nudge.py`):**
- Type B pattern is correctly applied: `_is_job_enabled()` gate at top of `main()` before any work — matches the reference implementation in `executor-heartbeat.py` and `steward-heartbeat.py`
- `SOURCES` list is a clean pluggable interface for future journal-fed todo sources
- `PendingAction` is a dataclass with the right fields
- GitHub query via `gh issue list` — correct; no API key dependency
- Owner filter via `owner: <name>` line in issue body — consistent with the brain-dump enrichment spec update in `.claude/agents/brain-dumps.md`
- Age bucketing (`bucket_by_age`) is pure function — correct primitive
- `compose_message` returns `None` when all buckets are empty — no message sent when nothing is pending
- Delivery via `write_inbox_message` — correct pattern for cron-direct jobs
- `--dry-run` flag disables both the job-enabled gate and Telegram delivery

**brain-dumps.md update:** Adds owner classification guidance (`owner: dan` vs `owner: lobster`) with the filter mechanism documented in-line. Correct — this is the write side of what the nudge script reads.

**systemd units (`services/lobster-pending-actions-nudge.{service,timer}`):**
- `OnCalendar=*-*-* 15:00:00` — daily at 15:00 UTC (correct)
- `Persistent=true` — fires missed executions on next boot
- `Type=oneshot` — correct for a one-shot script
- `# LOBSTER-MANAGED` marker present

**upgrade.sh Migration 95:**
- Checks `systemctl is-enabled --quiet` before installing — idempotent
- Copies both service and timer files, daemon-reload, enable --now
- Has correct warn fallback when files not found
- Migration number 95 — no collision in the sequence visible in the diff

**No concerns.** The job is correctly typed, correctly gated, and the delivery channel is consistent with other Type B jobs.

Oracle round: 1
