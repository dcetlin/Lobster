VERDICT: APPROVED
PR: 1223
Round: 2

## Round 2 — 2026-05-19

### Prior gap tracking

**Gap 1 (BLOCKING — Encoded Orientation without logged prior):** ADDRESSED. The decision record exists at `oracle/decisions/decision-wos-pr-coordinator.md` and is cross-referenced in `oracle/decisions.md`. The record names the behavioral default change, cites `vision.yaml` by field path (`core.operating_principles.principle-3`, `core.operating_principles.principle-4`), and explicitly states the routing change as a durable default. The WOS UoW citation (`uow_20260516_71b777`) is retained in the decision record as dispatch provenance — not as the authorization. The authorization is the decision record itself, which is the required logged prior for constraint-3.

**Gap 2 (ADVISORY — no unit tests for `route_wos_pr_result`):** ADDRESSED. 27 unit tests added in `tests/unit/test_orchestration/test_route_wos_pr_result.py`, organized in four test classes. All three required cases from the Gap 2 revision contract are covered (WOS task_id → spawn_subagent, non-WOS → fallthrough, None → fallthrough), plus a fourth class covering mixed-batch routing correctness. The test file is dedicated (not bundled into `test_dispatcher_commands.py`) which is a better structural decision than the one Gap 2 named.

---

### Stage 1: Vision Alignment (locked before re-reading implementation)

Stage 1 findings from Round 1 are preserved below and are not altered by Round 2 implementation review. The alignment verdict from Round 1 was **Questioned — not Misaligned**. The question was whether constraint-3 had a logged prior. The decision record resolves that question structurally.

The Stage 1 adversarial question — "is this solving the wrong problem?" — was answered in Round 1 as: the direction is sound (consistent with `principle-3`, `principle-4`, and the `route_wos_message` pattern), and the constraint-3 gap was the only structural blocker. With the decision record in place, the alignment verdict upgrades to **Confirmed**.

**Oracle vocabulary behavioral effects (Round 2):**

From learnings.md — the mirror-constant pattern (2026-04-24 onward): I checked whether `route_wos_pr_result` or the test file introduced any module-scope constants that mirror production enum values from other modules. They do not. The test file uses `_WOS_TASK_ID`, `_NON_WOS_TASK_ID`, and similar constants that are test-local input values — not mirrors of production enum members. The StrEnum golden pattern does not apply to routing predicates using string-prefix checks, but the mirror-constant risk is not present here.

From learnings.md — "feature-bundling" (2026-05-14, #1148): The Round 2 commit (12f4e063) adds a decision record and a dedicated test file — two artifacts that are both directly named in the Round 1 revision contract. Neither is from a distinct workstream. This is not feature-bundling.

From learnings.md — "WOS-artifact-as-authorization" (2026-04-24, #913): The decision record does not rely on the WOS UoW as authorization. The UoW is cited as dispatch provenance (WOS Reference field) and in the Authorization section explicitly as the oracle round 1 finding that prompted the record — not as the source of authority. The authorization is the decision record itself. The pattern is satisfied correctly.

From golden-patterns.md — "adversarial prior seeding before implementation review" (2026-03-27): Stage 1 finding held from Round 1 without alteration. The adversarial prior that constraint-3 was the structural question was confirmed by the Round 1 gap, and the Round 2 fix addresses it directly. No new Stage 1 concerns arise.

---

### Stage 2: Quality Review — Round 2

**Encoded Orientation check (OODA constraint-3):**

(a) A prior logged decision now exists: `oracle/decisions/decision-wos-pr-coordinator.md`, dated 2026-05-19, cross-referenced in `oracle/decisions.md`. The decision record satisfies all four criteria named in Gap 1's revision contract: named behavioral change, vision.yaml field path citations (principle-3, principle-4), durable default stated, and no WOS UoW cited as the authorization.

(b) The change is traceable to vision.yaml anchors: `core.operating_principles.principle-3` ("Determinism over judgment for conditionals") is the correct anchor for a deterministic routing predicate (`task_id.startswith("wos-")`). `core.operating_principles.principle-4` ("Wire what exists before building more") is the correct anchor for composing existing oracle/fix/merge agents behind a coordinator boundary rather than building new capability. Both citations are structurally sound — not boilerplate.

**Gap 1 verification complete. Constraint-3 satisfied.**

---

**Test coverage quality review (Gap 2 resolution):**

The test file `tests/unit/test_orchestration/test_route_wos_pr_result.py` contains 27 tests across four classes. Coverage assessment:

**`TestWosPrRoutesToCoordinator` (11 tests):** Covers the happy path fully. Tests verify: action value, agent_type, task_id encoding (PR number in ID, exact prefix pattern `wos-pr-coord-{number}`), prompt content (PR number, PR URL, repo, agent definition reference, YAML frontmatter), result_text truncation at 500 chars, and exact-match behavior for "wos-" prefix (underscore variant falls through). The truncation test (`test_result_text_truncated_in_prompt`) correctly tests both that the full 1000-char string is absent and that the 500-char prefix is present — this is the correct assertion form and avoids the assertion-softening defect named in learnings.md (2026-04-22, #839).

**`TestNonWosPrFallsThrough` (3 tests):** Covers the fallthrough path. Tests verify: non-WOS task_id produces fallthrough, fallthrough result has exactly one key ("action"), oracle-prefix task_id also falls through. The "no extra keys" test is a clean structural assertion that prevents accidental state leakage.

**`TestMissingOrMalformedFields` (8 tests):** Covers defensive cases. Tests verify: None task_id, empty task_id, malformed URL, non-integer PR number in URL, trailing slash in URL (correctly parses to spawn_subagent), empty result_text, integer chat_id, string chat_id (Slack). The trailing-slash case is notable — it tests a URL normalization edge case that the production code handles via `rstrip("/")`, and confirms the behavior is correct. String chat_id coverage for Slack compatibility is appropriate given the function signature accepts `int | str`.

**`TestMixedBatchRouting` (5 tests):** Covers the statelessness invariant. Tests verify that WOS and non-WOS calls in sequence produce independent results — no cross-call state leakage. The "distinct WOS PRs get distinct coordinator task_ids" test (`wos-pr-coord-999` vs. `wos-pr-coord-1000`) is a regression guard against the task_id collision scenario noted as a non-blocking observation in Round 1.

**Gap coverage assessment:** All four cases from the Gap 2 revision contract are covered. The test file is a dedicated module (not bundled), which is structurally better than the Gap 2 suggestion. The "pure function, no mocking required" design observation in the module docstring is accurate and is itself a structural signal — it confirms that `route_wos_pr_result` has no I/O side effects. The docstring explicitly states four cases from the PR description; all four are encoded as test classes. No mocking is required; no test fixtures set external state.

**Gap 2 verification complete.**

---

**Round 2 non-blocking observations:**

**Decision record authorization section:** The Authorization section states "Authorized by WOS UoW uow_20260516_71b777. The oracle PR #1223 verdict (Round 1) identified the missing decision record as a blocking gap. This document is the required logged prior for the behavioral default change under constraint-3." This is accurate. The UoW is not cited as the authorization — the oracle verdict's identification of the gap is cited as the prompt for creating the record. The authorization for the behavioral change is the decision record itself, as required. The section reads clearly and does not create ambiguity about what authorizes the change.

**`oracle/decisions.md` cross-reference:** The cross-reference entry in `oracle/decisions.md` correctly summarizes the change and points to the full decision file. It uses the exact language "durable behavioral default change anchored to vision.yaml principle-3 and principle-4" — consistent with the decision record's own characterization. No inconsistency.

**Wiring location correctness (from Round 1 non-blocking review, still sound):** The `route_wos_pr_result` call is placed before the dedup check in the dispatcher's ENGINEER → REVIEWER routing section. This means WOS-originated PRs bypass the dedup check entirely. The coordinator has its own internal state tracking (workstream status file). This is intentional and correct — the dedup check guards against spawning a second review agent for the same PR, which is the wrong concern for a coordinator that already owns the whole loop. The ordering is sound.

**Task_id collision advisory (from Round 1):** Still present as a theoretical risk. `wos-pr-coord-{pr_number}` as a task_id means two concurrent WOS subagents opening PRs against the same repository would produce the same coordinator_task_id. At current WOS drain rates, the same PR number from two different subagents is essentially impossible (PR numbers are monotonically increasing and not reused). The risk remains theoretical and does not justify a blocking concern.

---

### Patterns applied (behavioral change test, Round 2)

**From learnings.md (2026-04-23, PR #894) — "decision-file vision anchor citation can name a non-existent field path":** I grepped `vision.yaml` for `principle-3` and `principle-4` during the constraint-3 check. Both field paths resolve correctly: `core.operating_principles.principle-3` and `core.operating_principles.principle-4` are present with the stated content. The citation verification from PR #894 constrained this action — without it, I would have accepted the field path citations at face value.

**From learnings.md (2026-04-22, #839) — "assertion softening to not-legacy-value":** I checked the test assertions for the `test_result_text_truncated_in_prompt` case specifically because this is a common test-softening site. The test asserts both absence of the full string and presence of the 500-char prefix — this is the correct assertion form, not a softened form. The pattern caused me to inspect this specific test rather than accepting the test count as sufficient.

**From learnings.md (2026-04-20, PR #720 R4) — "ad-hoc caller enumeration produces per-round discovery":** In Round 2, I checked whether the PR's new test file is the only test file for `route_wos_pr_result`, and whether any sibling test files test the same function. There are no sibling test files — the dedicated test module is the only test surface. The Round 1 gap naming convention described bundling the tests into `test_dispatcher_commands.py`; the fix chose a dedicated module instead. I verified the dedicated module does not introduce a coverage gap by checking against the function's full set of stated cases.

---

### APPROVED

Both gaps from Round 1 are resolved. The implementation is structurally sound, constraint-3 is satisfied, and the test coverage is thorough and well-organized. No new blocking concerns arise in Round 2.

---

---

## Round 1 — 2026-05-19

### Stage 1: Vision Alignment (locked before implementation review)

**Adversarial prior:** This implementation is solving the wrong problem, or solving the right problem in a direction that forecloses better paths.

**Theory of change in vision.yaml:** Build a substrate where agents make intent-anchored decisions and the system routes work without requiring Dan's attention on every step. Active phase: WOS executor live, active constraints are the WOS bidirectional interface (#1118) and completion notification layer. `principle-4` ("Wire what exists before building more") and `principle-3` ("Determinism over judgment for conditionals — if-then logic is code, not LLM instructions") are directly relevant.

**What would have to be true for this to be the right path?** The 36–48 dispatcher reads per WOS burst is a real and measured problem. Context compaction from those reads causes routing errors, not just latency. The cost of a new coordinator agent (complexity, new failure mode surface) is less than the cost of the compaction problem.

**Adversarial test:** Is the compaction-from-dispatch-volume problem measured or hypothesized? The PR description says "During WOS drain bursts (6+ UoWs), the current per-stage dispatcher round-trip pattern generates 36–48 dispatcher reads in a single session, causing repeated context compaction." The causal claim (reads cause compaction which causes errors) is asserted, not evidenced. The opportunity cost of the active named priorities (bidirectional interface #1118, completion notification layer) is not assessed.

**What does this foreclose?** The coordinator internalizes oracle→fix→merge behind a new agent boundary. This makes the oracle verdict trail less observable to the dispatcher: the coordinator runs the oracle loop internally and the dispatcher only sees the final write_result. It also adds a new long-lived background agent as a structural dependency for WOS PRs. If the coordinator stalls or fails, the PR is stuck with no user-visible signal until the coordinator's escalation path fires (round 4+).

**Is there a cheaper test?** Yes: measure whether compaction actually causes routing errors in the WOS PR path before introducing a new agent tier. The OODA Orient step here is underdeveloped — the PR jumps to a structural solution without first verifying the symptom is the root cause.

**However:** The implementation is directionally sound. The `route_wos_pr_result` routing pattern is consistent with `route_wos_message` (the established compaction-resilient dispatch pattern). The coordinator eliminates N round-trips in the right way — by delegating to a background agent rather than blocking the dispatcher. The direction is not wrong; the Stage 1 concern is about whether the prior for making this a default routing behavior exists.

**Alignment verdict:** Questioned — not Misaligned. The implementation direction is sound and aligns with `principle-3` (encode in code, not prose) and the existing `route_wos_message` golden pattern. The question is whether this Encoded Orientation change has a logged prior (constraint-3). That is the gate, not the direction.

---

### Stage 2: Quality Review

**Encoded Orientation check (OODA constraint-3):**

This PR makes an Encoded Orientation decision: it changes the default routing behavior for all WOS-originated PR results from "spawn review agent" to "spawn coordinator agent which owns oracle→fix→merge internally." This is a durable behavioral default change — once merged, every WOS subagent that returns a PR URL will be routed to the coordinator rather than the review agent.

Constraint-3 requires: (a) a prior logged decision exists in `oracle/verdicts/` or `oracle/decisions.md`, and (b) the change is traceable to a `vision.yaml` anchor.

**Check result:** No prior logged decision for this behavioral default was found in `oracle/verdicts/`, `oracle/decisions.md`, or `oracle/decisions/`. The PR description cites `WOS-UoW: uow_20260516_71b777` as authorization. Per learnings.md (2026-05-01, PR #1032; 2026-04-24, PR #913): "WOS UoW artifacts are dispatch records, not decision records." This is the third or fourth recurrence of this exact pattern — WOS UoW cited as authorization for an Encoded Orientation decision. The gap fires on the same class of evidence that has been rejected in previous reviews.

**Gap 1 (BLOCKING — Encoded Orientation without logged prior):** `route_wos_pr_result` changes the durable routing default for all WOS PR results. No logged decision exists in `oracle/decisions.md` or `oracle/verdicts/` authorizing this behavioral change. The UoW artifact `uow_20260516_71b777` is dispatch provenance, not authorization. Constraint-3 is not satisfied.

**Resolution criteria:** File a decision record in `oracle/decisions.md` (or `oracle/decisions/decision-wos-pr-coordinator.md`) that: (a) names the behavioral default being changed, (b) cites `vision.yaml` by field path (suggested anchor: `core.operating_principles.principle-3`, `core.operating_principles.principle-4`, and `active_project.phase_intent`), (c) states the specific routing change as a durable default, and (d) is not itself a WOS UoW artifact. The oracle verdict for this PR serves as the structural record once APPROVED; the decision file is the explicit logged prior that constraint-3 requires.

---

**Gap 2 (ADVISORY — no unit tests for `route_wos_pr_result`):**

The PR description states "route_wos_pr_result() manual test: WOS task_id routes to coordinator, non-WOS falls through, None falls through — all pass." There are zero unit tests in `tests/` for `route_wos_pr_result`. The function is a pure function with no I/O, making it trivially testable. The PR touches `tests/unit/test_orchestration/test_dispatcher_commands.py` (130 tests) but adds no test class for the new function. Per learnings.md PR #714: "When a PR's scope is a new routing module, verify that at least one production call site (not test file) imports from it before treating the routing capability as live." More directly: a pure routing function with no tests in a codebase that has test infrastructure for this module is a regression surface with no CI guard.

**Resolution criteria:** Add a `TestRouteWosPrResult` test class in `tests/unit/test_orchestration/test_dispatcher_commands.py` covering: (a) WOS task_id → spawn_subagent action, (b) non-WOS task_id → fallthrough, (c) None task_id → fallthrough, (d) malformed PR URL → fallthrough with warning. All four cases are stated in the PR description as manually tested; encoding them as assertions closes the gap and prevents regression.

---

**Other findings (non-blocking):**

**Coordinator oracle spawning**: The coordinator spawns oracle via `subagent_type=lobster-oracle`, which is a valid agent definition in `.claude/agents/lobster-oracle.md`. The merge agent is spawned via `subagent_type="lobster-generalist"`, which is also valid. Both subagent types exist. The hook `hooks/pr-merge-gate.py` will fire on the merge agent's `gh pr merge` call and gate on the verdict file — this is correct and means the PR merge gate hook remains the structural enforcement mechanism even inside the coordinator loop.

**Verdict file archival timing**: The coordinator archives `oracle/verdicts/pr-{N}.md` to `oracle/verdicts/archive/pr-{N}.md` after merge. This is structurally correct. The `archive/` directory already exists (confirmed in PR description). The PR merge gate hook only needs the verdict file to exist before `gh pr merge` is called, not after — the archival ordering is sound.

**task_id deduplication**: The coordinator uses `coordinator_task_id = f"wos-pr-coord-{pr_number}"` as the task_id. Two concurrent WOS subagents that both open PRs with the same number would produce a coordinator_task_id collision. At current scales this is unlikely, but it is worth noting that the deduplication check uses the coordinator_task_id rather than the PR URL itself. Not a blocking issue at current scales.

**Fallthrough correctness**: The `route_wos_pr_result` function returns `{"action": "fallthrough"}` for non-WOS task_ids. The dispatcher correctly falls through to the existing review agent path without any state mutation. The existing dedup check (`reviewer_task_id = f"review-{msg.get('task_id', 'unknown')}"`) correctly handles non-WOS PRs. The fallthrough path is structurally unchanged.

**Import placement**: `from src.orchestration.dispatcher_handlers import route_wos_pr_result` is placed inside the `if pr_url_match:` block in the bootup doc pseudocode. This is a lazy import inside a conditional — consistent with the dispatcher's established pattern for cross-module imports to avoid circular dependencies. Sound.

---

### Patterns applied (behavioral change test)

**From learnings.md (2026-05-01, PR #1032) and (2026-04-24, PR #913) — WOS-artifact-as-authorization:**
This pattern constrained what I looked for first: before reading the implementation quality, I checked whether the authorization for the Encoded Orientation change was a WOS UoW artifact. It was. This caused me to flag Gap 1 before assessing implementation correctness — which is the correct ordering. Without this pattern in active vocabulary, I would likely have found the implementation sound and moved toward approval without checking the authorization form.

**From golden-patterns.md — "adversarial prior seeding before implementation review" (2026-03-27):**
Stage 1 was written before seeing the diff. The Stage 1 finding — "the key question is constraint-3" — held after seeing the implementation. It was not altered by the quality of the implementation, which is genuinely good. The adversarial prior caused me to check the authorization before the quality; the quality is good enough that without the prior, this would likely have received APPROVED.

---

### NEEDS_CHANGES — Revision Contract

**Gap 1 (BLOCKING):** Add a logged decision record in `oracle/decisions.md` or `oracle/decisions/decision-wos-pr-coordinator.md` that satisfies constraint-3: named behavioral change, `vision.yaml` field path citation (e.g., `core.operating_principles.principle-3`, `core.operating_principles.principle-4`), durable default stated. The WOS UoW citation is not sufficient.

**Gap 2 (ADVISORY):** Add `TestRouteWosPrResult` test class in `tests/unit/test_orchestration/test_dispatcher_commands.py` covering the four cases named above. Advisory means the PR may be approved without this if the author disputes it with a specific reason — but the function is pure, the test infrastructure exists, and the four cases are already manually verified. Encoding them costs one hour and closes the regression surface permanently.

Each gap must be resolved as: (a) addressed — the revision shows the thing the gap named, (b) disputed — the author states why the gap does not apply with a specific reason, or (c) deferred — the author acknowledges the gap and states why it is not addressed now.
