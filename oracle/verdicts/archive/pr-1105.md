VERDICT: APPROVED
PR: 1105
Round: 2

---

## Round 2 — 2026-05-09

### Prior gap status

**Gap 1 (BLOCKER — migration number collision):** ADDRESSED. PR #1103 merged as migration 0020 (executor_pid). PR #1105 branch now uses `0021_add_control_events.sql` and upgrade.sh Migration 104. No collision. The migrations directory on main ends at 0019; PR #1103 added 0020; PR #1105 adds 0021. Sequence is clean.

**Gap 2 (minor — raw event type string literals):** ADDRESSED. `ControlEventType(StrEnum)` is defined at module scope in `dispatcher_handlers.py` with `WOS_START`, `WOS_STOP`, and `WOS_ABORT` members. All three handler usages (`handle_wos_start`, `handle_wos_stop`, `handle_decide_close`) use `ControlEventType.*` instead of raw strings. Test file imports `ControlEventType` from production and binds local names to the imported members — no mirror constants.

---

### Stage 1: Vision alignment (carried from Round 1, unchanged)

**Theory of change:** Applies directly to `active_project.phase_intent` (WOS observability and reliability). An append-only control events log closes a genuine observability gap. Alignment verdict: Confirmed.

---

### Stage 2: Quality review — Round 2

#### Residual finding: stale internal comment in SQL file (minor, non-operative)

`src/orchestration/migrations/0021_add_control_events.sql` contains the header comment:

```sql
-- Migration 0020: add control_events table for dispatcher command timeline (issue #1104).
```

The file was renamed from 0020 to 0021 during the Round 1 fix, but the comment inside was not updated. This is a documentation defect only — the migration runner determines version from the filename prefix, not the comment text. The registry.py docstring correctly references "migration 0021." The upgrade.sh entry correctly says "Migration 104." No runtime behavior is affected.

This was not a named gap in Round 1 because the migration file content was not separately examined. It is noted here for completeness. It does not change the verdict.

#### Gap 1 confirmation details

The migration runner skips versions already in `_migrations` by numeric prefix. `0021_add_control_events.sql` is distinct from `0020_add_executor_pid.sql` (now on main). No silent-skip risk exists. The upgrade.sh sequence (102 → 104) correctly reflects that PR #1103 occupies Migration 103, which is already on main.

#### Gap 2 confirmation details

`ControlEventType(StrEnum)` is placed at module scope in `dispatcher_handlers.py`, below the imports and above the WOS execution config block — unambiguous canonical placement (golden-patterns.md "canonical placement must be obvious from first encounter" applies: the enum is at the top of the module with its type peers, not buried near its first call site). Test file imports with:

```python
from orchestration.dispatcher_handlers import ControlEventType
_EVENT_TYPE_WOS_START = ControlEventType.WOS_START
```

This is the correct pattern — the test binds a local alias to the imported constant, not a locally-declared string mirror. `_REQUIRED_EVENT_TYPES` is built from imported members, so any change to the StrEnum values propagates to tests automatically.

The three handler call sites:
- `handle_wos_start`: `ControlEventType.WOS_START` — confirmed
- `handle_wos_stop`: `ControlEventType.WOS_STOP` — confirmed
- `handle_decide_close`: `ControlEventType.WOS_ABORT` — confirmed

#### Encoded Orientation check (carried from Round 1, unchanged)

This PR adds durable behavioral writes (new side effects in existing handler code paths) but does not change behavioral defaults, system constraints, agent identity, or decision-making rules. The writes are observational and non-fatal. Constraint-3's logged-prior requirement does not apply at this threshold.

#### What the implementation does well (carried from Round 1, extended)

- `ControlEventType(StrEnum)` satisfies the StrEnum golden pattern (golden-patterns.md 2026-04-24, Rule 3: "any function that returns one of N fixed string choices should define a StrEnum"). The pattern was cited in Round 1 as imperfectly applied; it is now correctly applied.
- The `registry=` optional keyword injection in `handle_wos_start` and `handle_wos_stop` follows the testability-via-injection pattern. Local import (`from .registry import Registry as _Registry`) avoids circular import at module scope. This is structurally clean.
- Test coverage is thorough: idempotent no-op paths write no event; OSError in toggle writes no event; table-absent is handled non-fatally; payload round-trip is verified; multiple calls accumulate in order.
- The non-fatal error boundary is correctly scoped. A missing control log entry is categorically distinct from a missing UoW operation.

---

### Verdict rationale

Both Round 1 gaps are fully resolved. The stale internal comment in the SQL file is a documentation defect with no runtime consequence and no behavioral risk — it does not reopen either named gap and does not introduce a new gap that would require a Round 3 fix cycle.

---

## Round 1 — 2026-05-09

### Stage 1: Vision alignment

**Theory of change:** vision.yaml's `active_project.phase_intent` is WOS observability and reliability — building the substrate that lets agents make intent-anchored decisions. The `current_focus.this_week.primary` explicitly names "WOS observability and reliability." principle-1 ("proactive resilience over reactive recovery") favors observability infrastructure.

**Stage 1 question:** Is an append-only control events log the right path, or is this solving the wrong problem while callback handlers (decide_retry, decide_close) remain unwired?

**Finding:** The foundational assumption holds. An append-only timeline of dispatcher control actions is a genuine observability gap — start/stop/abort were previously observable only by manual correlation of three separate sources. The PR is strictly additive and forecloses nothing. The opportunity cost is low; the PR is bounded in scope. The concern about building before the pipeline is correctly wired does not apply here: observability infrastructure is appropriately early, not premature optimization.

**Alignment verdict:** Confirmed. This PR applies the `active_project.phase_intent` WOS observability axis directly.

---

### Stage 2: Quality review

#### Encoded Orientation check (constraint-3)

This PR adds a new durable behavioral write in `dispatcher_handlers.py` — every `wos start`, `wos stop`, and `handle_decide_close` now writes to the registry. This is an Encoded Orientation change in that it adds new side effects to existing handler code paths.

The change is: (a) purely observational (no routing or decision changes), (b) non-fatal by design (swallows all errors), and (c) traceable to `active_project.phase_intent` (WOS observability). It does not change behavioral defaults, system constraints, or decision-making rules. Constraint-3's logged-prior requirement applies to Encoded Orientation decisions that "change behavioral defaults, system constraints, agent identity, or decision-making rules in a durable way." This PR meets the threshold of durable behavioral addition, but its non-fatal, observational nature places it at the lower end. No prior logging is required.

#### Gap 1: Migration number collision — BLOCKER

**Both PR #1103 and PR #1105 claim migration 0020 and upgrade.sh Migration 103.**

PR #1103 adds `0020_add_executor_pid.sql` (adds `executor_pid INTEGER NULL` to `uow_registry`). PR #1105 adds `0020_add_control_events.sql` (creates the `control_events` table). The current highest migration on main is 0019. Neither PR has merged.

This is the exact pattern documented in learnings.md PR #1000: "Migration number collision between simultaneous PRs produces silent feature deletion. When the second PR merges, its migration file is silently skipped — the feature's DB table is never created." The migration runner skips versions already in `_migrations`. The backwards-compatibility `try/except` in `log_control_event` (designed for pre-migration deploys) makes the collision indistinguishable from a normal pre-migration state: no crash, no visible error, feature permanently disabled.

upgrade.sh Migration 103 is also claimed by both PRs. Whichever merges second will produce a no-op migration step with no indication the collision occurred.

**Resolution required:** This PR must renumber to 0021 (or whatever is the next available number after both open PRs are accounted for). If PR #1103 merges first, PR #1105 needs 0021. If PR #1105 merges first, PR #1103 needs 0021. The task prompt suggested 0021 as the expected number — the implementing agent's choice of 0020 was based on counting only merged migrations on main, which is insufficient when other PRs are in concurrent review. upgrade.sh must also be renumbered to Migration 104 (or next available).

Per the established learnings.md PR #1000 protocol: "When two PRs are in concurrent oracle review, the oracle should flag migration number conflicts as a NEEDS_CHANGES blocker regardless of which merges first, since merge order cannot be guaranteed."

#### Gap 2: Event type strings are raw literals with no named production constants — NEEDS_CHANGES (minor)

The event type strings `"wos_start"`, `"wos_stop"`, `"wos_abort"` appear as inline string literals in `dispatcher_handlers.py` (at the `log_control_event` call sites). No named constants are defined at module scope in production code.

The test file defines local constants (`CONTROL_EVENT_TABLE`, `_EVENT_TYPE_WOS_START`, etc.) but cannot import them from production because they don't exist there. The PR's own docstring says `CONTROL_EVENT_TYPES contains at least: wos_start, wos_stop, wos_abort` — implying these should be a defined set, but this set is test-only.

This is the StrEnum golden pattern applied imperfectly: per golden-patterns.md (2026-04-24), "any function that returns one of N fixed string choices (path selection, strategy names, mode strings) should define a StrEnum." The event_type column is N fixed string choices. The fix: define a small set of named constants (or StrEnum) at module scope in `registry.py` or `dispatcher_handlers.py` and import them at all call sites, including in the test file.

This is minor — the strings are simple and unlikely to drift — but the test file's locally-declared event type constants (`_EVENT_TYPE_WOS_START = "wos_start"`) are mirror constants that will diverge silently if the production string values ever change. The mirror-constant pattern (first-pass oracle check, learnings.md 2026-04-26) is present here in its lowest-severity form: test file re-declares string values that the production code also has as inline literals. No production named constant exists to import — the test is doing the right thing given what the production code provides, but production should provide the named constant.

#### Known concerns evaluation

**Concern 1: handle_decide_close → wos_abort mapping**

The implementing agent is correct that `handle_decide_close` is the appropriate write point given the current codebase. The semantic mapping is sound: `decide_close` transitions a UoW from `blocked → failed(user_closed)` — this is the operational equivalent of an abort on a stuck UoW. The docstring now makes the "wos abort" alias explicit.

The concern about PR #1103 is valid but not a blocker for this PR: when PR #1103 merges and adds `handle_wos_abort`, that handler should write `"wos_abort"` via `log_control_event`. At that point, two paths will write `"wos_abort"`:
- `handle_decide_close` (Dan selects "Close" on a stuck UoW via inline button)
- `handle_wos_abort` (Dan sends `wos abort <uow_id>` via Telegram command)

These are semantically distinct commands with a shared event type, which is correct — the control log records the fact that an abort occurred, and the payload's `uow_id` distinguishes the affected UoW. No double-write risk exists because `handle_decide_close` closes an already-blocked UoW (blocked → failed) while `handle_wos_abort` kills a running process (no status transition). They operate on different UoW states. This concern is resolved.

**Concern 2: Migration number — BLOCKER (see Gap 1)**

The implementing agent's claim that "0020 was the correct next slot" was based on the migrations directory on main (which goes up to 0019). This is insufficient: PR #1103 (in oracle Round 2, not yet merged) also claims 0020. The task prompt's suggestion of 0021 was the correct signal. This is the blocker.

**Concern 3: Pre-existing SyntaxError in test_registry_cli.py**

Confirmed pre-existing (line 1183, unclosed parenthesis, present in main). Confirmed that `test_control_events.py` is a separate file and will not be affected by the SyntaxError in `test_registry_cli.py`. pytest collects each file independently; a SyntaxError in one file causes that file's collection to fail but does not block other files. The 19 new tests in `test_control_events.py` can run cleanly. Not a blocker.

#### What the implementation gets right

- The `log_control_event` method's non-fatal error boundary is correctly designed. The `BEGIN IMMEDIATE` + rollback-on-exception pattern is consistent with the existing registry connection pattern. The `try/except` in the payload serialization path is correctly scoped (serialization errors are separate from DB errors).
- The `registry=` optional keyword argument in `handle_wos_start` and `handle_wos_stop` follows the testability-via-injection pattern cleanly. Existing callers with no argument continue to work. The local import (`from .registry import Registry as _Registry`) avoids a circular import at module scope.
- The test coverage is thorough: idempotent no-op paths write no event, OSError in toggle writes no event, table-absent is handled non-fatally. The dispatcher tests correctly use dependency injection rather than patching internals.
- The upgrade.sh migration entry correctly notes the auto-migration handles existing installs (no manual SQL step needed).

---

### Revision contract

**Gap 1 (BLOCKER — migration number collision):** Renumber `0020_add_control_events.sql` to `0021_add_control_events.sql` (or the next available number after accounting for all open PRs). Update all references to `0020` within the new file's comments, the docstring in `log_control_event` ("Created by migration 0021"), and upgrade.sh Migration 103 → 104 (or next available). If PR #1103 merges before this PR, renumber to 0021 relative to its 0020. If this PR merges first (unlikely given #1103 is Round 2), PR #1103 must renumber. In either case, the oracle will confirm the gap is resolved by checking that no two migration files in the merged repo share the same prefix number.

**Gap 2 (minor — raw event type string literals):** Add named string constants for the three event types in production code — either in `registry.py` or `dispatcher_handlers.py` — and update the test file's local constants to import from production. The form is simple: `WOS_START_EVENT = "wos_start"` etc., or a `StrEnum`. The test file's `_REQUIRED_EVENT_TYPES` set should then assert against the imported production constants, not locally-declared strings. This closes the mirror-constant divergence risk.
