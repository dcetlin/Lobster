VERDICT: APPROVED
PR: 1177
Round: 1

## Stage 1: Vision Alignment

**Adversarial prior:** Is this solving the wrong problem, or solving the right problem in a way that forecloses better paths?

The theory of change: subagents were making `gh pr` / `gh issue` calls without `--repo dcetlin/Lobster`, causing them to silently operate on `SiderealPress/lobster` (the upstream public fork) instead of the target repo. The fix adds an explicit reminder in `sys.subagent.bootup.md`.

**What would have to be true for this to be the right path?** The root cause must be an instruction-following gap, not a structural defect requiring mechanical enforcement.

**Foreclosure concern:** `learnings.md` (2026-05-14, #1148) explicitly names "advisory enforcement of structural invariant violates vision.yaml principle-3." The `advisory-to-hook promotion` golden pattern (2026-04-22) names the same class: if the gate condition is string-pattern-detectable on `tool_input["command"]`, it should be a hook. A `gh pr` without `--repo` is string-pattern-detectable.

**Why advisory is defensible here (not a foreclosure):** The `advisory-to-hook promotion` pattern also specifies the non-promotable boundary: "gates requiring semantic judgment cannot be promoted." The `--repo` requirement has context-sensitivity: WOS task contexts require `dcetlin/Lobster`; Lobster maintenance tasks legitimately target `SiderealPress/lobster`. A hook enforcing `dcetlin/Lobster` unconditionally would generate false positives in maintenance contexts. The prior bootup note ("Default repo: `SiderealPress/lobster`... for Lobster maintenance tasks") is the existing context that makes a blanket hook over-broad. This is the distinguishing condition from the merge gate case.

**Alignment verdict: Confirmed.** The advisory approach is the correct choice given context-sensitivity. The placement is at the canonical seam for repo-context conventions.

**Learnings.md citation behavior:** The `advisory-to-hook promotion` pattern was consulted. Its non-promotable boundary criterion — context-sensitivity requiring judgment — directly constrained the verdict: I did not flag the advisory format as a defect requiring NEEDS_CHANGES, because the pattern's own boundary condition applies here. Without that check, I would have flagged the prose format as a principle-3 violation.

---

## Stage 2: Quality Review

**Diff:** 3 lines added to `.claude/sys.subagent.bootup.md`, Tooling conventions section. No code changes.

**Does it do what it claims?** Yes. The instruction names the subcommands (`gh pr`, `gh issue`), the explicit argument (`--repo dcetlin/Lobster`), the failure mode (working directory remote defaulting to `SiderealPress/lobster`), and the scope ("WOS dispatch or any task context").

**Placement:** Immediately after the existing "Default repo" note at line 450. Correct structural seam.

**Precision gap (minor, non-blocking):** The new note says "always specify `--repo dcetlin/Lobster` in `gh` CLI commands" in "WOS dispatch or any task context." The preceding note says `SiderealPress/lobster` is the default for Lobster maintenance tasks. These two notes are in mild tension for maintenance task subagents. A subagent reading both notes can resolve the distinction, but a more precise formulation would be "when running `gh pr` or `gh issue` in WOS or user-work task contexts." Not a blocker — subagents with full bootup context have enough signal to parse the distinction.

**Encoded Orientation check (constraint-3):** This PR modifies a subagent bootup file — a durable behavioral default change. The logged prior is UoW `uow_20260514_ebc90d` (named in the PR). The vision.yaml anchor is principle-3 ("Determinism over judgment for conditionals" — the `--repo` specification removes an ambiguous default). The audit (`dispatcher_handlers.py` and task files) was confirmed in the briefing. Both conditions satisfied.

**Failure modes:**
- Advisory prose is compaction-vulnerable, but this is the correct approach given context-sensitivity (see Stage 1).
- The audit covered `dispatcher_handlers.py` and `scheduled-jobs/tasks/`. Agent definition files in `.claude/` are not audited explicitly, but the briefing's broader statement ("all task files already use --repo dcetlin/Lobster") covers the scope.

**What this makes harder:** Nothing is foreclosed.

**Patterns introduced:** Adds to the tooling-conventions bullet list in prose form, consistent with existing section style. No new structural patterns introduced.

**VERDICT: APPROVED**
