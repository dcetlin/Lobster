VERDICT: APPROVED

## Oracle Review — PR #1312
**Repo:** dcetlin/Lobster
**Branch:** fix/registry-db-path-wos1293 → main
**Reviewed at:** 2026-05-25
**UoW:** wos-pr-coord-1312

## Summary

PR #1312 corrects the default DB path in two WOS status scripts from the stale orphan `data/registry.db` to the canonical live registry `orchestration/registry.db`. The fix is a 2-line change (one per file) that aligns these scripts with every other consumer in the codebase.

## Checks

### Correctness
- `scripts/wos-status.sh`: Old default was `${HOME}/lobster-workspace/data/registry.db`, new default is `${HOME}/lobster-workspace/orchestration/registry.db`. Correct.
- `scripts/wos_active_summary.py`: Old default was `Path.home() / "lobster-workspace" / "data" / "registry.db"`, new default is `Path.home() / "lobster-workspace" / "orchestration" / "registry.db"`. Correct.
- Both scripts continue to honour `REGISTRY_DB_PATH` env var and positional argument overrides — existing override behaviour is unchanged.

### Consistency with codebase
- `src/orchestration/paths.py`, `executor-heartbeat.py`, `steward-heartbeat.py`, `registry_cli.py`, `analytics.py` all already use `orchestration/registry.db`. This PR completes the migration.
- No remaining `data/registry.db` references in any script or Python file after this PR (the one hit in grep is on the main branch pre-merge, exactly what this PR fixes).

### Scope
- Change is minimal and targeted: 2 lines changed, 0 new files, 0 deletions of other code.
- No logic changes, no interface changes, no new dependencies.

### Risk
- Low. The orphaned `data/registry.db` (72 KB, last updated 2026-05-18) would never return correct active UoWs for the live system. Pointing at `orchestration/registry.db` (645 MB, updated 2026-05-24) is strictly correct.
- The `REGISTRY_DB_PATH` override path is preserved, so any existing automation using the env var is unaffected.

### Issue closure
- Closes #1293 ("hygiene: data/registry.db is stale orphan"). The issue identified both `wos-status.sh` and the need to remove/archive `data/registry.db`. The PR description correctly defers the runtime file removal as a follow-up, which is appropriate (not a code change).

## Conclusion

The change is correct, minimal, consistent with the rest of the codebase, and closes the identified issue. No gaps found.
