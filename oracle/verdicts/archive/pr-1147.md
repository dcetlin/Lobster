VERDICT: APPROVED
PR: 1147
Round: 3

## Round 3 — 2026-05-13

### Stage 1 — Vision Alignment

**Prior entering review:** This implementation is solving the wrong problem, or solving the right problem in a direction that forecloses better paths.

The vision.yaml states `current_focus.primary` as "WOS observability and reliability" with metabolic taxonomy operationalized. Issue #1068 closes the gap between having metabolic labels (heat/shit/seed/pearl) and being able to see whether bad-outcome UoWs are also consuming disproportionate tokens — the "bloat detector" signal. This is a direct observability instrument for a system that already tracks both fields.

The implicit theory of change: if bad-outcome UoWs are also high-token-cost UoWs, that is a structural inefficiency signal worth surfacing before it compounds. The PR does not change any routing or execution behavior — it adds a read-path join of two fields already being written. The cheaper test of the underlying assumption (are there UoWs with both outcome_category and token_usage populated, and do the bad ones cluster at the high-cost end?) is exactly what this function computes. There is no cheaper test that is not this function.

What this forecloses: nothing meaningfully. The function is pure read-only analytics over an existing DB schema. It does not introduce a new table, new migration, or new agent behavior.

**Alignment verdict: Confirmed.** The work serves the stated WOS observability priority and is the minimal instrument for the signal it targets.

---

### Stage 2 — Quality Review

**91/91 tests pass** on this branch (test_analytics.py + test_wos_metrics_report.py).

**R2 blocking issues — status:**

- R2 Gap 1 (test key missing from EXPECTED_TOP_LEVEL_KEYS): ADDRESSED. `"outcome_cost"` added at line 54 of test_wos_metrics_report.py.
- R2 Gap 2 (missing unit tests): ADDRESSED. `TestOutcomeCostCorrelation` class with 8 tests present. All four oracle-specified edges verified:
  - Single-UoW-equals-median excluded: `test_single_bad_outcome_equal_to_median_excluded` — inserts 1 "heat" row, median=5000, asserts empty bloat_candidates because `5000 > 5000` is False. Correct.
  - Uniform values all excluded: `test_uniform_token_values_all_excluded_from_bloat_candidates` — 4 rows at 2000 tokens each, all equal to median, asserts empty. Correct.
  - Empty bad-outcome set: `test_empty_bad_outcome_set_returns_empty_bloat_candidates` — only "good" category rows, asserts empty bloat_candidates. Correct.
  - DB-absent path: `test_db_absent_returns_structured_error` — non-existent path, asserts structured dict with "not found" in data_gap. Correct.

**Module-level import in wos_dashboard.py:** `from src.orchestration.analytics import outcome_cost_correlation` at line 52, before any function definitions. Confirmed module-level.

**PR body closing syntax:** "Closes #1068" present in body. Confirmed.

**No duplicate class names:** `TestOutcomeCostCorrelation` appears exactly once. Confirmed.

**Mirror-constant observation (non-blocking):** The tests use raw string literals "heat" and "shit" as `outcome_category` data values in `_insert_outcome_cost_uow()` calls, rather than importing `_BAD_OUTCOME_CATEGORIES` from analytics.py. This is a partial instance of the mirror-constant pattern (learnings.md, 2026-04-26). Severity is low: the strings appear as DB insert values, not as assertions against a production constant. A change to `_BAD_OUTCOME_CATEGORIES` would cause `test_bloat_candidates_excludes_at_median_includes_above` to fail loudly (correct detection). The failure mode is loud, not silent — unlike the canonical learnings.md instances where tests passed silently against stale constants. Not a blocking issue at this severity.

**`_BAD_OUTCOME_CATEGORIES` as mutable set (non-blocking):** The constant is a plain `set` literal (`{"heat", "shit"}`) rather than a `frozenset`. The golden-patterns.md entry (PR #967) calls for module-scope gate sets to use `frozenset`. Used only once, in `_build_bloat_candidates`. Minor pattern deviation; no behavioral impact.

**Encoded Orientation check (OODA constraint-3):** This PR adds analytics read functions and wires them into existing report and dashboard payloads. No behavioral defaults, system constraints, agent identity, or decision-making rules are changed. Encoded Orientation check does not apply.

**`statistics` import:** Module-level at line 36. Confirmed.

**No schema changes, no new dependencies, no migrations required.** The function reads from the existing `uow_registry` table using fields already present.

### Verdict

All R2 blocking issues addressed. 91/91 tests pass. Four oracle-specified edge cases explicitly tested. Two non-blocking observations noted (mirror-constant severity-low, mutable set vs frozenset) — neither rises to NEEDS_CHANGES threshold at this stage given the low failure risk and the absence of silent failure modes.

VERDICT: APPROVED
