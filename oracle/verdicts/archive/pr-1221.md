VERDICT: APPROVED
PR: 1221
Round: 1

## Stage 1: Vision Alignment (formed before reading implementation)

**Theory of change check:** The oracle's correct operation — writing verdict files to the path that the merge gate reads — is a necessary precondition for the PR Merge Gate to function structurally rather than as a manual workaround. Issue #1204 reported that merge agents had to copy files between directories as a workaround. Vision.yaml constraint-2 requires that system behavior be structurally anchored, not inferred or manually patched at runtime. A path mismatch in an installer heredoc is an installation-class defect of the kind constraint-2 exists to prevent.

**Adversarial prior check:** Does this fix the wrong thing, or solve a real problem while leaving a bigger one? The prior oracle review (PR #1205) established the symlink (migration 116) as the runtime fix and called for instruction-level cleanup to follow. The PR summary correctly identifies this as the instruction-level cleanup. The adversarial concern would be: is the heredoc fix alone sufficient, or does the same error appear in other instruction files? Stage 2 must answer this.

**What would have to be true:** The user-update.sh d11 heredoc was the last remaining instruction-level reference to the wrong path. The oracle agent definition and pr-merge-gate.py already use the correct path. The actual task file was corrected on Apr 15 (commit 7684a5a4). Only the installer heredoc was stale.

**Vision alignment:** Confirmed. This is structural hygiene that enables constraint-2 and constraint-3 (oracle review path integrity is a precondition for the PR Merge Gate gate to function as described in CLAUDE.md).

**Stage 1 findings locked:** The adversarial question is scope completeness — is this the last remaining reference or one of several? Stage 2 checks this.

---

## Stage 2: Quality Review

### Pattern citations from learnings.md and golden-patterns.md

The "path fix completeness" pattern (2026-04-20, PR #727 entry) states: "Enumerate all forms a wrong path can take — not just the form observed in the failing case." This caused me to explicitly grep for `lobster-workspace/oracle` across scripts/, .claude/agents/, and scheduled-tasks/ before concluding the fix is complete. Without this pattern, I would have reviewed only the diff and taken the PR's "last remaining" claim at face value. The grep confirmed no remaining occurrences in those locations.

The "canonical vs runtime copy divergence" pattern (2026-04-21, PR #811 entry) caused me to check whether the runtime sweep-context.md at `~/lobster-user-config/memory/canonical/sweep-context.md` also needed updating when the canonical template was updated. Investigation showed the negentropic sweep task explicitly reads from `~/lobster/memory/canonical-templates/sweep-context.md` and explicitly deprecates the user-config runtime copy — so the divergence concern does not apply here.

### The path fix (user-update.sh)

**Correctness:** The fix is correct. `~/lobster-workspace/oracle/learnings.md` → `~/lobster/oracle/learnings.md` matches the canonical path established when oracle files were moved to the repo directory.

**Scope:** The "last remaining" claim holds. Grep across scripts/, .claude/agents/, and scheduled-tasks/ finds zero remaining occurrences of the wrong path. The previously missed negentropic-sweep.md line 57 was corrected in a prior session (commit 7684a5a4, April 15).

**Effectiveness on existing installations:** The heredoc is guarded by `if [ ! -f "$learnings_proposals_task" ]` — meaning it only runs on installations that haven't yet created the task file. Existing installations that already ran d11 already have the stale path in their live task file. However: this is not a defect in this PR's fix. Migration 116 created the symlink on existing installations, making `~/lobster-workspace/oracle/learnings.md` and `~/lobster/oracle/learnings.md` resolve to the same file. The runtime defect for existing installations was already addressed by the symlink. This PR closes the installer-level gap that would have affected new installations running upgrade.sh from a base state before migration 116.

**Ordering integrity:** user-update.sh d11 runs before migration 116 in upgrade.sh (d11 is sourced at line 2366, migration 116 is at line 4124). With the fix, d11 writes the canonical path — which is correct regardless of whether the symlink exists, since `~/lobster/oracle/learnings.md` is the actual file.

### The sweep-context.md addition (Night 4 pre-flight)

**Relationship to the issue:** This addition is not related to the oracle verdict path issue (Issue #1204). It is a separate feature bundled into the same PR. The PR body does not explain the connection, and the commit history shows two separate commits (one for the sweep feature, one for the path fix).

**Content quality:** The Night 4 pre-flight section (Step 1c-N4) is well-structured. It adds four checks before Night 4 begins: night agreement validation, decay-detector.py existence and runtime check, Pipeline Mode B script existence, and gh CLI authentication. The gh auth failure is a hard-abort gate that sends Ping 1 to Dan and exits. The section correctly includes an abort-output specification so the sweep file is readable even on abort.

**Encoded Orientation check:** The Night 4 pre-flight section changes how the negentropic sweep agent behaves on Night 4 (mandatory pre-flight before detection). This is a durable behavioral default change. However, the constraint-3 bar for Encoded Orientation applies to "system acts without Dan's explicit input." The sweep agent is dispatched by a cron job, not by the agent itself; the agent reads the template and follows instructions. The pre-flight section adds a hard-abort on gh auth failure (sends Ping 1, exits), which notifies Dan rather than acting silently. Vision.yaml principle-1 ("Proactive resilience over reactive recovery") is a traceable anchor: the pre-flight detects setup failures before mid-sweep discovery. The abort-then-notify pattern (Ping 1 with "Night 4 aborted") is a reactive-escalation, not an autonomous behavioral extension. Encoded Orientation constraint-3 is not triggered.

**Canonical template is the authoritative source:** The negentropic-sweep task file explicitly reads `~/lobster/memory/canonical-templates/sweep-context.md` and deprecates the user-config runtime copy. Updating the canonical template is the correct and complete action.

**Bundling concern (non-blocking):** The two commits in this PR address unrelated concerns. The path fix is a one-line correction to close Issue #1204. The pre-flight section is a 57-line feature addition. These are not related and bundling them makes the PR non-atomically revertable. This is a process note, not a blocking defect — the pre-flight section is correct, and the path fix is correct.

### Failure modes

None identified. The symlink (migration 116) provides defense-in-depth — even on installations where the task file carries the stale path, the symlink ensures both paths resolve to the same file. The PR adds correctness at the installation level.

### What this makes easier / harder

Easier: A fresh installation running upgrade.sh from a pre-d11 state will create the learnings-proposals task file with the correct canonical path. The Night 4 sweep has an early-exit path for authentication failures rather than discovering them mid-sweep.

Harder: Nothing is foreclosed or made harder.

---

## Verdict

VERDICT: APPROVED

The path fix is correct, complete, and necessary. The sweep-context.md pre-flight addition is an improvement correctly placed in the canonical template. No blocking defects found.

Process note for the record: bundling an unrelated 57-line feature addition with a single-line bug fix makes the PR scope harder to review in isolation. The fix should have stood alone. This does not affect the verdict.
