VERDICT: APPROVED
PR: 1266
Round: 1

---

## Stage 1: Vision Alignment (formed before reading implementation)

**Prior entering this review:** This implementation is solving the wrong problem, or solving the right problem in a direction that forecloses better paths.

**Theory of change interrogation.** The PR title says "remove duplicate validate_phase2_schema from steward.py." Before seeing the diff: what problem does removing a duplicate function solve? Duplication creates a divergence risk — two definitions of the same schema check with two field sets that can drift apart. The canonical pattern in this codebase (golden-patterns.md "full-repo grep as precondition for dead-code removal") requires verifying both are truly identical before removal; the "StrEnum constants in all enum-backed SQL and routing logic" pattern requires that field validation lives in one authoritative place. If the two copies diverged, fixing only one would silently perpetuate errors in callers of the other.

Applied adversarially: could this be the wrong problem? The alternative reading would be "the duplication is intentional — steward.py's copy validates steward-specific fields while registry.py's copy validates executor fields, and removing one breaks the other's semantics." If this is true, consolidation would produce a silent semantic mismatch. I will verify field identity in Stage 2.

**Opportunity cost and foreclosure check.** If the duplication is truly redundant, removing it is purely structural hygiene. It does not foreclose any downstream direction. If it is not redundant and the fields have diverged, the PR would introduce a regression. This is a binary outcome; Stage 2 resolves it.

**Vision alignment.** This is hygiene work (issue #365 from the 2026-03-30 hygiene scan). It is anchored to vision.yaml principle-4 ("Wire what exists before building more" — here: consolidate what exists before adding more schema validation paths) and principle-1 ("Proactive resilience over reactive recovery" — removing the divergence surface before the two copies drift). The PR tracks a WOS UoW (`uow_20260522_07c89d`), so it is properly registered work, not a side-channel addition. It is within-scope maintenance work, not a new feature that would conflict with `current_focus`.

**Alignment verdict:** Confirmed. The adversarial prior is not confirmed — the problem is real (duplicated schema validation), the fix is the right intervention point (remove from steward.py, import from registry.py), and it forecloses nothing.

---

## Stage 2: Quality Review

### What the PR actually does

Two files changed:

1. **`src/orchestration/steward.py`:** Removes `_STEWARD_REQUIRED_FIELDS` (frozenset, 9 fields), `validate_steward_schema()` (the duplicate function), and the `validate_phase2_schema = validate_steward_schema` alias. Replaces with an import block that brings in `validate_steward_executor_schema as validate_steward_schema` and `validate_phase2_schema` from `orchestration.registry`. All import paths switch from `src.orchestration.*` to `orchestration.*` for consistency.

2. **`scheduled-tasks/steward-heartbeat.py`:** Switches all top-level imports from `src.orchestration.*` to `orchestration.*` for the same reason (import path normalization, not functional change).

### Field identity verification

The canonical `_STEWARD_EXECUTOR_REQUIRED_FIELDS` in `registry.py` contains:
- workflow_artifact, success_criteria, prescribed_skills, steward_cycles, lifetime_cycles, timeout_at, estimated_runtime, steward_agenda, steward_log

The removed `_STEWARD_REQUIRED_FIELDS` in `steward.py` contained:
- workflow_artifact, success_criteria, prescribed_skills, steward_cycles, lifetime_cycles, timeout_at, estimated_runtime, steward_agenda, steward_log

Both frozensets are identical (9 fields, same values). The PR description's claim "Both field sets were verified identical (9 fields) before removal" is correct. The golden-patterns.md "full-repo grep as precondition for dead-code removal" pattern applies: the PR needed to verify both before removal. The identity check passes.

### Call-site preservation

The call site in `run_steward_cycle` uses `validate_steward_schema(conn)`. The PR re-exports `validate_steward_executor_schema as validate_steward_schema` in the import, so the call site requires no change. This is correct — the caller sees the same name, the function is now the canonical one.

### Test compatibility

`test_steward.py` accesses `steward.validate_phase2_schema` (two test methods in `TestSchemaValidation`). The PR re-exports `validate_phase2_schema` from the registry import: `from orchestration.registry import ... validate_phase2_schema`. This makes `steward.validate_phase2_schema` accessible as a module attribute, preserving the test reference.

`test_phase2_schema_migration.py` imports `validate_phase2_schema` directly from `src.orchestration.registry` — that path is unchanged by this PR, so those tests are unaffected.

The integration test `test_wos_pipeline.py` mentions `validate_steward_schema` in a docstring comment (line 1204: "Call validate_steward_schema at startup") — this is documentation only, not an import.

### Import path normalization

The switch from `src.orchestration.*` to `orchestration.*` in `steward-heartbeat.py` and `steward.py` is consistent with the pattern used by other modules in the heartbeat scripts (the importlib bridge pattern from golden-patterns.md applies here: heartbeat scripts add the repo root to `sys.path`, making `orchestration.*` resolvable without the `src.` prefix). The one exception in `steward.py` is `from src.ooda.fast_thorough_selector import ...` — this import retains the `src.` prefix. This inconsistency is pre-existing and is not introduced by this PR.

Also preserved as-is: `from src.utils.jobs import is_job_enabled` and `from src.utils.inbox_write import write_crash_alert` in `steward-heartbeat.py`. Both retain `src.` prefix. The PR only normalizes the `orchestration.*` imports, not all `src.*` imports. This is a scoping choice — the PR does not claim to normalize all imports, only to move the registry/steward/etc. imports. The preserved `src.utils.*` imports indicate the normalization is selective and targeted.

### Encoded Orientation check (constraint-3)

This PR removes duplicate code and normalizes import paths. No agent identity, behavioral defaults, decision-making rules, or system constraints are changed. Constraint-3 does not apply.

### Issue linking check

The PR body does not contain `Closes #365`, `Fixes #365`, or `Resolves #365`. Issue #365 ("validate_phase2_schema duplicated in registry.py and steward.py") exists, is open, and is exactly what this PR fixes. The missing close-syntax is a minor gap — the issue will not auto-close on merge. This is not a blocker for APPROVED but should be noted.

The PR body does reference the WOS UoW (`WOS-UoW: uow_20260522_07c89d`) which is the mechanically tracked reference; the GitHub issue close-syntax is an additional human-readable cross-reference that is missing.

### Feature-bundling check

Two files changed: `steward.py` (the deduplication fix) and `steward-heartbeat.py` (import normalization that was part of the same import context change). The PR description covers both changes. The import normalization in `steward-heartbeat.py` is incidental to the same fix — both files were importing `steward.py` symbols and needed the same import path correction. Not bundling across unrelated workstreams (learnings.md 2026-05-18 calibration applies: same-system, same context, not a separate UoW).

### Mirror-constant check

The removed `_STEWARD_REQUIRED_FIELDS` was itself a mirror constant — a module-scope frozenset in `steward.py` duplicating `_STEWARD_EXECUTOR_REQUIRED_FIELDS` in `registry.py`. This PR removes the mirror. The golden-patterns.md "StrEnum constants in all enum-backed SQL and routing logic" pattern and the learnings.md mirror-constant pattern (multiple entries, most recently 2026-05-11 #1211) both name this as the correct resolution. The PR is implementing the exact fix these patterns recommend.

### What this makes easier / harder

Easier: any future change to the required field list is made in one place (registry.py). No synchronization requirement between two identical frozensets. The `validate_steward_executor_schema` function's docstring and implementation are the authoritative source.

Harder: nothing foreclosed. The import normalization is additive convention, not a one-way door.

### Verdict

The fix is correct, minimal, and cleanly scoped. Fields are verified identical before removal. The call site is preserved via aliased import. Test compatibility is preserved via re-export. The import normalization is correctly scoped to the orchestration modules. The only gap is the missing `Closes #365` issue link in the PR body, which is a minor operational inconvenience (the issue will not auto-close) but not a correctness defect.

VERDICT: APPROVED
