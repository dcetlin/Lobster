VERDICT: APPROVED
PR: 1018
Round: 2

---

## Round 2 — 2026-05-23

### Prior gap tracking

All three gaps from Round 1 are accounted for before issuing a new verdict.

**Gap 1 (Encoded Orientation — step 2b-pw):** ADDRESSED. Step 2b-pw has been removed from `sys.dispatcher.bootup.md`. A full-repo grep for `principle_weight`, `2b-pw`, and `Principle Audit` across `.claude/`, `src/`, `scheduled-tasks/`, `hooks/`, and `tests/` returns zero matches outside the two new files (`src/orchestration/principle_weights.py` and `tests/unit/test_principle_weights.py`). No Encoded Orientation decision remains in this PR.

**Gap 2 (Unfalsifiable mechanism — nightly audit):** ADDRESSED. The nightly audit step, the daily-digest template section, and the dispatch-time wiring re-export from `dispatcher_handlers.py` have been removed. The PR description explicitly states: "No dispatch-time wiring — library is available but not called from any production path. No startup step — step 2b-pw has been removed; no session-init ritual. No nightly audit — deferred until a falsifiability test is designed first." The scope is unambiguously library-only. The unfalsifiable mechanism is not shipped; only the pure-function library remains.

**Gap 3 (Library-wiring inversion):** ADDRESSED. The inversion identified in Round 1 — "the mandatory gate (session start ritual) is wired, the claimed primary value is not" — is resolved by removing the mandatory gate. Both sides of the inversion are now consistent: the library is unwired, and no operational wiring exists. The PR description's "What is explicitly out of scope" section explicitly names this: no dispatch-time wiring, no production call sites.

---

### Stage 1: Vision Alignment (locked from Round 1; does not change after seeing implementation)

*Reproduced verbatim — Stage 1 findings do not change between rounds.*

**Prior entering review:** This implementation is solving the wrong problem, or solving the right problem in a direction that forecloses better paths.

**Theory of change implied by this work:** Subagent behavioral quality suffers because dispatched prompts lack structured epistemic emphasis guidance. By adding principle-weight vectors to dispatch calls, subagents will weight their reasoning differently and produce measurably better output aligned to the task type.

**What would have to be true for this work to be the right path:**
1. Current subagent quality failures are caused by absence of epistemic weight labels, not by orientation failures or prompt structure.
2. Text injected into prompts reliably and verifiably changes model output in the described direction.
3. This work is higher priority than the stated active constraint (WOS bidirectional interface #1118).

None of these preconditions are established. The Stage 1 alignment verdict remains **Questioned** — the library's theory of change is not anchored to a diagnosed failure. However, the narrowed scope (library-only, no operational wiring, no bootup additions) means the foreclosing effects identified in Round 1 are no longer present. A questioned library that ships no behavioral defaults and introduces no operational gates is a different risk profile from a questioned library that wires itself into dispatcher session initialization.

---

### Stage 2: Quality Review

**Diff scope verification:** The PR diff contains exactly two files: `src/orchestration/principle_weights.py` (137 lines, new) and `tests/unit/test_principle_weights.py` (159 lines, new). No bootup docs, no nightly consolidation, no daily digest, no dispatcher_handlers.py, no inbox_server.py. The claimed scope is confirmed by inspection.

**Encoded Orientation check (constraint-3):** No Encoded Orientation decision remains. The two new files are a pure-function library and its tests. Neither changes behavioral defaults, system constraints, agent identity, nor decision-making rules in any durable way. The library is callable but not called. Constraint-3 check: CLEAR.

**Code quality (unchanged from Round 1 assessment, now unencumbered by the structural concerns):**

The implementation is technically correct. Pure-function design: `assign_principle_weights` returns a fresh dict copy on every call — callers may mutate without corrupting shared state. Canonical iteration order via `PRINCIPLE_NAMES` tuple ensures deterministic output across Python versions. `VISIBILITY_THRESHOLD` is a named module-scope constant imported correctly in tests (no mirror-constant risk per learnings.md PRs #696, #800, #967, #970). Weight bounds are [0.0, 1.0] and enforced by test assertion. Fallback to "default" profile for unknown task types is correct and tested. The `Final` type annotations on module-scope constants are correct.

**Test quality:** 24 tests across three test classes. Tests import production constants directly (`PRINCIPLE_NAMES`, `PRINCIPLE_IMPLICATIONS`, `PRINCIPLE_WEIGHT_PROFILES`, `VISIBILITY_THRESHOLD`) — consistent with the golden pattern for avoiding mirror-constant divergence. The two local constants (`EXPECTED_PRINCIPLE_COUNT = 5`, `EXPECTED_PROFILES = {"debugging", "mobile_response", "design_inquiry", "default"}`) are spec-declared constraints, not mirrors of production values — they are independent assertions about what should exist, which is the correct use of local test constants. Copy-on-read semantics are explicitly tested with both equality and identity assertions. Canonical ordering test is correctly implemented by extracting principle names from rendered output and comparing to `list(PRINCIPLE_NAMES)`.

**What this makes easier for future work:** A future PR that wants to wire principle weights into dispatch has a clean, tested, stable library to call. The pure-function design means the wiring PR does not need to worry about shared state or initialization order. The named profiles create a vocabulary for dispatch-type classification that is independent of the wiring question.

**What this makes harder:** Nothing. A library-only PR with no operational wiring forecloses no future paths. The falsifiability question (Issue #22, identified in Round 1 as a structural gap in the theory of change) remains open — but it is now correctly deferred as future work rather than shipped as a nightly audit that cannot answer the question it claims to address.

**Patterns cited from learnings.md:**

The absorption-ceiling pattern (learnings.md 2026-03-23) drove what I looked for in this round: does any of the new content add to the always-on context pool rather than externalize? Answer: no. The two new files are importable library code, not bootup additions. The pattern's behavioral effect on this review was to confirm that removal of the bootup additions was the correct resolution path — and to verify that no equivalent accumulation snuck in through a different vector. A grep for `principle` across `.claude/` returned only existing vision.yaml operating principle references, confirming the bootup pool is unchanged.

The mirror-constant pattern (learnings.md PRs #696, #800, #967, #970, #974, #1218) constrained my test quality review: I enumerated every constant used in test assertions and verified each was imported from the production module. `EXPECTED_PRINCIPLE_COUNT = 5` and `EXPECTED_PROFILES` are the only non-imported test constants. Both encode spec-level invariants, not production value mirrors, which is the correct pattern. No mirror-constant risk found.

---

### Round 2 Verdict

**VERDICT: APPROVED**

All three Round 1 gaps are resolved via the library-only narrowing: step 2b-pw removed (Gap 1), nightly audit and all operational wiring removed (Gap 2), wiring inversion resolved by removing the wired side (Gap 3). The implementation is technically correct, the test suite is sound, no operational changes are present, and constraint-3 is satisfied.

The Stage 1 alignment concern (theory of change not anchored to a diagnosed failure) remains — it is not resolved by scope narrowing. It is tolerated in the APPROVED verdict because: (a) a pure library with no behavioral effects is a lower-stakes way to hold the theory until falsifiability is designed, (b) the PR explicitly defers wiring to a future PR that will need to address Issue #22 before oracle approval, and (c) the library introduces no patterns that propagate incorrectly. The alignment concern is recorded; it is not a merge blocker for a library-only primitive.

---

## Round 1 — 2026-05-23

### Stage 1: Vision Alignment (written before reading implementation)

**Prior entering review:** This implementation is solving the wrong problem, or solving the right problem in a direction that forecloses better paths.

**Theory of change implied by this work:** Subagent behavioral quality suffers because dispatched prompts lack structured epistemic emphasis guidance. By adding principle-weight vectors to dispatch calls, subagents will weight their reasoning differently and produce measurably better output aligned to the task type.

**What would have to be true for this work to be the right path:**
1. Current subagent quality failures are caused by absence of epistemic weight labels, not by orientation failures or prompt structure.
2. Text injected into prompts ("attunement_over_assumption: 0.9 — name causal vs. symptom layers explicitly") reliably and verifiably changes model output in the described direction.
3. This work is higher priority than the stated active constraint (WOS bidirectional interface #1118).

None of these preconditions are established.

**Pattern match from learnings.md — behavioral effect, not labeling:**

Two named patterns from learnings.md directly govern this review:

*Pattern: "absorption-ceiling response via context-expansion" (2026-03-23):* The learnings entry states: "This session produced two PRs both aimed at making principles durable across context compaction... The epistemic-principles meta-thread explicitly names 'absorption ceiling' as the problem. Neither PR reduces the document; both add to it. When the intervention for 'context gets too long and instructions recede' is 'add more context,' the pattern is self-escalating." PR #1018 adds: a new dispatcher bootup section (32 lines), a mandatory startup step (2b-pw, 6 lines), a nightly-consolidation audit step (11 lines), and a daily-digest template section (13 lines). This is the same pattern, fourth iteration. The named failure class applies. Effect on this review: I looked specifically for whether any of the added content compresses or externalizes rather than adds — it does not. The startup step 2b-pw is particularly acute: it requires the dispatcher to generate prose about its own epistemic orientation before processing any messages. This is new prose added to the dispatcher's session to address the problem that prose added to the dispatcher's session is not reliably followed.

*Pattern: "held-back issues as architectural signal" (2026-03-23):* Issue #22 names the falsifiability test for this work — "what is the verifiable test that a principle-weight vector produced a different output?" — and was held back as "underspecified." The learnings entry states: "The held-back issues may be underspecified because they point at the harder architectural problem the shipped work sidesteps. When the underspecified issues are the most structurally interesting ones, hold that signal." PR #1018 does not answer Issue #22. It ships the mechanism without addressing the falsifiability gap. The nightly principle audit in step 4b produces subjective judgments ("did the response name causal vs. symptom layers?") that have no ground-truth measure. Effect on this review: I looked for any falsifiability test in the PR — any observable outcome that would confirm or disconfirm the mechanism is working. There is none.

**Additional pattern match from learnings.md (PR #714):** "A new module with 733 additions and 0 deletions, with no import of the new module in any existing file, is a library with no wiring." PR #1018 explicitly states: "The dispatcher does not yet call assign_principle_weights automatically during routing — this PR provides the primitives; wiring into live dispatch is a follow-up." The module is unwired. The dispatcher_handlers.py re-export makes the functions importable but does not call them. The inbox_server.py field addition allows principle_weights to be stored in tasks but nothing reads them during dispatch. This is infrastructure for unspecified future wiring, with no production call site.

**Cheaper tests not run:**
- Has anyone measured whether current subagent outputs would be detectably different if principle weights were appended vs. not appended? The nightly audit step cannot answer this without a control condition.
- Does the current quality problem trace to specific dispatch call sites, or is it diffuse? Without a diagnosis, the intervention target is unspecified.
- Issue #33 (the source issue) explicitly states "This issue should remain open until principle weights are (a) stored, (b) read by subagents, and (c) demonstrably affect outputs." The PR was opened and WOS auto-closed the issue. The auto-close triggered before criteria (b) and (c) were met.

**What this forecloses:** The startup step 2b-pw ("Do not proceed to message processing until this step is complete") is a mandatory ritual that generates prose about which principles are "live" for the session — a concept with no operational definition. If this step is compaction-vulnerable (the absorption-ceiling problem), the dispatcher will skip it silently and no detection mechanism will fire. If it is not compaction-vulnerable, it adds deliberative overhead to every session start. The step forecloses the question of whether principle orientation is better served by structural encoding or by prose reflection — it answers the question by shipping the prose path.

**Encoded Orientation check (constraint-3):** The startup step 2b-pw changes the dispatcher's behavioral defaults at session start. It is a new mandatory gate in the session initialization sequence. This constitutes an Encoded Orientation decision. Check: (a) is there a prior logged decision for this class of change? No — no file in oracle/verdicts/ names principle-weight dispatch as an authorized behavioral default. The WOS UoW auto-closure does not constitute a logged decision — it is a status transition, not a decision record. (b) Is the change traceable to a vision.yaml anchor? The PR does not cite a vision.yaml anchor. Vision.yaml principle-5 ("Discriminator improvement over rule addition") applies in the opposite direction: when a behavioral rule isn't followed, improve the discriminator — do not add more rules. **Encoded Orientation decision lacks logged prior or vision.yaml anchor (constraint-3).** This is a NEEDS_CHANGES trigger on its own.

**Opportunity cost:** vision.yaml current_focus names WOS bidirectional interface (#1118) as the active constraint. The horizon field names Orient implementation (#733) as the next structural investment "after WOS execution loop is fully closed." This PR does neither — it ships an unwired library in the epistemic-primitives space while WOS execution is not yet closed.

**Alignment verdict: Questioned**

The implementation may be internally well-made. The problem is that this work is not anchored to a named failure it corrects, its central mechanism is unfalsifiable, it repeats a named failure pattern (absorption-ceiling response via context-expansion), it violates constraint-3 (Encoded Orientation without logged prior), and it ships while the stated active constraint (#1118) is unaddressed.

---

### Stage 2: Quality Review

The implementation is technically clean. The pure-function design is correct. Copy-on-read semantics are correct. Canonical iteration order for deterministic output is correct. Test coverage is thorough. The mirror-constant risk that appears frequently in this codebase is not present — tests import from the production module. The visibility threshold is a named module-scope constant, imported correctly in tests. These are all correct. The code is well-made.

**Gap 1 (BLOCKING): Encoded Orientation decision without logged prior or vision.yaml anchor**

The startup step 2b-pw ("After loading bootup context, explicitly name which principles are live for this session... Do not proceed to message processing until this step is complete") changes the dispatcher's session-initialization behavioral defaults. This is Encoded Orientation under constraint-3.

Required for this gap to be closed: either (a) a logged decision in oracle/verdicts/ naming principle-weight dispatch as an authorized behavioral default, with a traceable vision.yaml anchor, OR (b) the step 2b-pw is removed from the bootup doc and kept as advisory text in a design document rather than an operational gate.

**Gap 2 (BLOCKING): The mechanism is unfalsifiable as shipped**

The PR's stated value is that "a dispatcher can call assign_principle_weights('debugging') and get a weight vector that prioritizes attunement_over_assumption (0.9), which gets rendered into the subagent prompt as actionable behavioral guidance." No test verifies that injecting the weight block produces detectably different subagent outputs. The nightly audit (step 4b) performs subjective human-read evaluation with no ground truth. This was specifically flagged as "underspecified" in Issue #22 before this PR was written — and the PR ships without addressing it.

The source issue (#33) stated: "This issue should remain open until principle weights are (a) stored, (b) read by subagents, and (c) demonstrably affect outputs." Criteria (b) is not met (no production call site reads the weights during dispatch), and (c) is not met (no falsifiability mechanism exists). The WOS auto-closure of the issue triggered prematurely.

Required for this gap to be closed: either (a) the PR includes or cites a falsifiability test — a specific observable outcome that would confirm or disconfirm that the mechanism affects subagent outputs — or (b) the scope is explicitly narrowed to "infrastructure for future use" with no session-start step and no nightly audit, and the PR title/description is updated to reflect that wiring is deferred.

**Gap 3 (BLOCKING): Library with no production call site**

The PR description states "The dispatcher does not yet call assign_principle_weights automatically during routing." The dispatcher_handlers.py re-export is a re-export, not a call site. The inbox_server.py field addition stores principle_weights in tasks, but nothing reads this field during dispatch. No production code path invokes assign_principle_weights or format_principle_weights_block. This is the named "library with no wiring" pattern from learnings.md (PR #714). Infrastructure-only PRs create review debt and rationalization pressure toward future wiring that may never happen.

This is less severe than Gaps 1 and 2 when taken alone — incomplete wiring is acknowledged in the PR. But combined with the fact that the only code that IS wired is the session-start step (2b-pw), the PR has an inversion: the advisory block (prose injected into subagent prompts) is unwired, but the mandatory gate (session start ritual) is wired. The least-load-bearing element is operational; the claimed primary value is not.

Required for this gap to be closed: either (a) at least one production call site is wired (the dispatcher calls assign_principle_weights during task dispatch), which would require closing Gap 2 first, or (b) the mandatory startup step 2b-pw is removed (consistent with Gap 1 resolution), leaving only the library for future wiring.

**What this makes easier/harder for future work:**

Easier: future dispatchers can call assign_principle_weights with minimal boilerplate. The pure-function design is correct and reusable.

Harder: once the principle audit is in nightly-consolidation and daily-digest, it generates output on every nightly run for responses that "included principle_weights in their prompts" — which is currently zero responses, since dispatch wiring is a follow-up. The audit step will produce empty output silently until wiring happens, and there is no detection mechanism for the case where principle_weights were supposed to be included but were not.

---

### Revision contract

Each gap must be resolved one of three ways: (a) addressed — the revision shows the thing the gap named; (b) disputed — the author states why the gap does not apply, with specific reason; (c) deferred — the author acknowledges the gap and states why it is not addressed now.

**Gap 1 resolution path:** Remove step 2b-pw from the dispatcher bootup doc, or provide a logged decision in oracle/verdicts/ with a traceable vision.yaml anchor for principle-weight dispatch as a behavioral default.

**Gap 2 resolution path:** Narrow the PR scope to a library-only primitive (no session-start step, no nightly audit, no daily-digest section), with a clear statement that wiring and validation are deferred, and update the PR title to remove "first-class dispatch primitive" framing that implies operational deployment. OR provide a falsifiability test with a specific observable outcome.

**Gap 3 resolution path:** Follows from Gap 2 resolution. If scope is narrowed to library-only, Gap 3 is addressed. If scope is kept with wiring, at least one production call site is required.
