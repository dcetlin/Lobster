VERDICT: SUPERSEDED

## PR #1257 — refactor: extract is_job_enabled() shared utility

### Finding

PR #1257 is superseded by PR #1178, which was merged to main on 2026-05-15.

PR #1178 delivered the same work (both passes — all 17 scripts + test mock updates) before PR #1257 could be merged. Main branch verification:

- `src/utils/jobs.py` exists with the canonical `is_job_enabled(job_name, default=True)` implementation
- All 17 scheduled-task scripts on main use `from src.utils.jobs import is_job_enabled`
- `grep -l "def _is_job_enabled" scheduled-tasks/` returns no results for any of the 17 target scripts

### Merge Conflict Root Cause

The branch diverged from main before ~164 files of major feature work landed (dispatcher_handlers, pre_handler, slack_router, lobster_meta, many tests, etc.). The 18,664-line deletion diff from main confirms the branch cannot be rebased automatically — it would require resolving conflicts across the entire recent codebase history.

### Recommended Action

Close PR #1257 as superseded by #1178. The UoW `uow_20260512_56b580` is complete.

Do NOT attempt to merge. The work is already in production.
