VERDICT: APPROVED
PR: 997
Round: 2

## Round 2 — 2026-04-27

### Prior gap tracking (from Round 1)

**Gap 1: "executing" bucket label is semantically incorrect at production scale — ADDRESSED**

The revision implemented path (b) from the Round 1 revision contract: `_EXECUTING_STATUSES` is now used as a positive gate in `_classify_status`, and the catch-all bucket is renamed to `"in-pipeline"`. Proposed/pending/blocked/expired UoWs now map to `"in-pipeline"`, not `"executing"`. The dead-constant defect is resolved.

**Finding 3: `_EXECUTING_STATUSES` frozenset is dead code — ADDRESSED (same resolution as Gap 1)**

The frozenset is now wired as a positive gate: `if status in _EXECUTING_STATUSES: return "executing"`.

**Finding 2: `fetch_in_window` excludes UoWs with NULL timestamps — DEFERRED (stated in Round 1 as advisory)**

No change. Advisory only; acceptable deferral.

**Finding 4: N+1 audit queries in `_format_report` — DEFERRED (stated in Round 1 as advisory)**

No change. Advisory only; acceptable deferral.

---

## Stage 1: Vision Alignment

**Prior entering review:** This implementation is solving the wrong problem, or solving the right problem in a direction that forecloses better paths.

**Theory of change check (unchanged from Round 1):** vision.yaml names WOS observability and reliability as the primary focus. A time-windowed pipeline report wires existing Registry data to a queryable operational surface, consistent with principle-4 ("Wire what exists before building more"). The fix in Round 2 makes the primary summary number operationally accurate — a correct `in-pipeline: 257` vs. a misleading `executing: 257` is directly relevant to observability. The correction improves the work's alignment, not just its quality.

**Alignment verdict: Confirmed.**

**Learnings.md patterns active as priors (behavioral effect in Round 2):**

- **Dead-constant anti-pattern (PR #906):** This caused me to verify that `_EXECUTING_STATUSES` is now referenced in `_classify_status` rather than merely defined. It is — positive gate, line 4 of the hierarchy. Gap resolved.
- **Mirror-constant pattern (PRs #696, #800, #967, #970, #973, #974, #976, #980):** This caused me to re-check whether any new constants introduced in the revision use raw string literals over `UoWStatus` enum members. The four frozensets still use raw string literals. This was noted as advisory in Round 1 (consistent with `registry_cli.py`'s existing module posture). No change in assessment.
- **Test assertion softening (PR #839):** This caused me to verify that the new `test_proposed_maps_to_in_pipeline` and `test_pipeline_statuses_map_to_in_pipeline` tests use exact equality (`== "in-pipeline"`), not substring or inequality assertions. They do.

---

## Stage 2: Quality Review

### Encoded Orientation check (constraint-3)

Not triggered. Read-only CLI command. No agent definitions, bootup files, gate tables, or automated dispatch paths modified.

### Feature bundling check (mechanical)

Three files changed: `registry.py`, `registry_cli.py`, `tests/unit/test_orchestration/test_registry_cli.py`. All three explicitly named in the original PR description and unchanged in scope. Pass.

### Findings

**Gap 1 resolution — verified correct:**

`_classify_status` now implements a five-bucket hierarchy with first-match semantics:

1. `status in _COMPLETE_STATUSES` → `"complete"`
2. `status in _FAILED_STATUSES` → `"failed"`
3. `status in _ESCALATED_STATUSES` → `"escalated"`
4. `status in _EXECUTING_STATUSES` → `"executing"` (positive gate, now wired)
5. catch-all → `"in-pipeline"`

The docstring documents the hierarchy explicitly. No status can appear in two buckets (the frozensets are disjoint by inspection: `done`, `failed/cancelled`, `needs-human-review`, `active/executing/ready-for-steward/ready-for-executor/diagnosing` have no overlap). The catch-all correctly captures `proposed`, `pending`, `blocked`, `expired`.

**`_compute_summary` key normalization — verified consistent:**

The `buckets` dict uses `"in-pipeline"` (matching the string returned by `_classify_status`). The returned summary dict normalizes to `"in_pipeline"` via `"in_pipeline": buckets["in-pipeline"]`. The `_format_report` header renders `in-pipeline:` from `summary['in_pipeline']`. The normalization is deliberate and internally consistent.

**Tests — verified adequate:**

- `test_proposed_maps_to_in_pipeline`: exact equality, correctly replaces the old `test_proposed_maps_to_executing`
- `test_pipeline_statuses_map_to_in_pipeline`: covers `pending`, `blocked`, `expired` with exact equality
- `test_executing_status_maps_to_executing`: covers all five members of `_EXECUTING_STATUSES`
- `test_counts_proposed_in_pipeline_not_executing`: integration-level test in `_compute_summary`, explicitly named in the task prompt as the new test. Verifies that 4 proposed/pending/blocked rows produce `in_pipeline == 4` and `executing == 1` (for the single `active` row). This test directly demonstrates the separation the Round 1 blocking gap required.
- `test_counts_correct_for_mixed_statuses`: asserts `in_pipeline == 0` for a row set with no pre-execution statuses — correct.

No stale assertions from Round 1 remain. The old test that documented the wrong behavior is gone.

**Advisory advisories from Round 1 — no regression:**

Findings 2 and 4 (advisory, deferred) are unchanged. No new advisories introduced by the revision.

**No new issues introduced by the revision.**

---

## Summary

The blocking gap from Round 1 is cleanly resolved. `_EXECUTING_STATUSES` is a live positive gate, not dead code. The catch-all bucket is semantically accurate. Tests cover the separation with exact equality assertions. No regressions against any Round 1 advisory finding. The remaining advisories (NULL-timestamp exclusion documentation, N+1 audit queries) are acceptable deferrals for a human-invoked read-only CLI command.

**VERDICT: APPROVED**
