VERDICT: APPROVED

## PR #1065 — docs: add step 4 (write_result) to Long-Running Dispatch Preamble

### Stage 1: Right problem?

Yes. Long-running dispatches (subagents) had a 3-step preamble (mkdir, write status.md, update status.md) but no explicit reminder to call `write_result` at completion. Without this, the reconciler cannot detect task completion via the task_id→write_result path. Adding step 4 closes this gap.

### Stage 2: Well made?

Docs-only PR. Two files modified:
- `CLAUDE.md` § Long-Running Dispatch Preamble: adds step 4 with full `write_result` call signature including both `sent_reply_to_user=False` and `sent_reply_to_user=True` variants
- `.claude/sys.dispatcher.bootup.md` § Long-Running Dispatch Preamble (summary): adds compact step 4 line consistent with the summary format of that file

Both additions are minimal and precise — they add exactly the information that was missing without changing anything else.

Note: the content of this PR was already partially applied to main as part of resolving merge conflicts during PR #1061 rebase. The PR branch was rebased on main with the overlapping commit skipped (it was a no-op against current main). The merge is clean.

Oracle round: 1
