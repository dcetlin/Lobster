VERDICT: APPROVED
PR: 861
Round: 2

## Round 2 — 2026-04-23

### Prior gap tracking

**Gap 1: `hooks/pr-merge-gate.py` not updated** — Status: **Addressed.** `find_oracle_verdict()` now checks `VERDICTS_DIR / f"pr-{pr_number}.md"` first (first line read, exact string equality check), with legacy fallback to `oracle/decisions.md` prose search. The path resolves to `~/lobster/oracle/verdicts/pr-{number}.md` — consistent with where oracle agents write. The structural enforcer reads the new signal.

---

### Stage 1 — Vision Alignment (locked)

Round 2 is a targeted enforcement fix addressing the single gap named in Round 1: the hook left behind after the instruction layer was updated. The theory of change is unchanged from Round 1 and remains confirmed — per-PR verdict files with a first-line machine-readable signal serve vision principle-3 (determinism over judgment for conditionals) and principle-1 (structural prevention over reactive recovery). There is no new foundational assumption in Round 2. The adversarial prior for this round: did the fix introduce a different gap — broken legacy fallback, or a new failure mode at the hook layer? Investigation below does not confirm this prior.

**Alignment verdict: Confirmed**

---

### Stage 2 — Quality Review

**`find_oracle_verdict()` correctness:**
The new primary path is: check `VERDICTS_DIR / f"pr-{pr_number}.md"` for existence, read text, call `.splitlines()[0].strip()`, compare to exact strings. Path construction is correct — hook at `hooks/pr-merge-gate.py`, `parent.parent` resolves to repo root, `oracle/verdicts/pr-{N}.md` matches the oracle agent's write instruction in `lobster-oracle.md`. Exact string equality (not `in` or regex) is correct for a safety gate — no ambiguity about partial matches.

**Legacy fallback:**
The fallback to `oracle/decisions.md` prose search is preserved intact. The only change to the legacy path is removal of the now-unused `pr_token = f"PR #{pr_number}"` variable (no behavioral change) and updated warning messages. The legacy check continues to work for all PRs reviewed before Round 1 shipped.

**`splitlines()[0]` IndexError on empty file:**
Real but low-consequence. An empty verdict file requires the oracle agent to create the file but fail before writing any content — a degenerate write failure. If this occurs, `.splitlines()` returns `[]` and `[0]` raises `IndexError`, which crashes the hook rather than returning "UNKNOWN." In a PreToolUse hook, an uncaught exception causes the tool call to be blocked rather than silently allowed — the conservative direction for a safety gate. The fix is simple (`lines[0] if lines else ""`), but the failure mode is safe rather than dangerous. Not a blocker.

**Scope check:**
7 files changed — all named in the PR description. No undocumented scope expansion. The hook/structural-enforcer-synchronization learning (learnings.md, 2026-04-23) names this exact pattern as the detection criterion; this PR is its direct remediation.

**Remaining minor item from Round 1:**
`index.md` seed file not added (labeled "Recommended, not blocking" in Round 1). Not resolved in Round 2. Does not prevent approval — it was not in the blocking contract.

---

### Round 1 — 2026-04-23

*(Full content from prior round preserved below for audit continuity.)*

## Stage 1 — Vision Alignment

PR #861 addresses a real structural problem: the old PR Merge Gate check relied on grepping `oracle/decisions.md` for the latest entry for a given PR, which is fragile under multi-round reviews and ordering ambiguity. The proposed fix — per-PR verdict files with a machine-readable first line — is well-scoped and directly serves vision.yaml principle-3 ("determinism over judgment for conditionals") and principle-1 ("structural prevention over reactive recovery").

The adversarial prior I hold: is this solving the wrong problem? The deeper question is whether decisions.md bloat in dispatcher context is the root failure, or whether the root failure is that the PR Merge Gate is Advisory and depends on context-loaded prose. I looked for evidence that the advisory-hook promotion (PR #825) already addressed the structural enforcement gap, which would make this PR about signal quality (cleaner file vs. prose grep) rather than about the enforcement mechanism. The hook at `hooks/pr-merge-gate.py` does exist and does enforce the gate mechanically. But the hook reads `oracle/decisions.md` — so the quality of the approval signal is load-bearing for the hook's correctness. PR #861's premise is valid: a fragile grep over a growing prose file is the right thing to fix, and it matters because the hook depends on it.

**Alignment verdict: Confirmed** — this solves the correct problem in the correct direction. The file format change is a legitimate improvement to the hook's check path, not a surface-level cosmetic fix.

---

## Stage 2 — Quality Review

### Critical Gap: `hooks/pr-merge-gate.py` not updated

The PR changes the approval signal from `oracle/decisions.md` prose to `oracle/verdicts/pr-{number}.md` first-line, but `hooks/pr-merge-gate.py` still reads `oracle/decisions.md` via `DECISIONS_FILE` and parses `**VERDICT: APPROVED**` prose. After this PR ships, the hook's enforcement path is entirely disconnected from the new signal the oracle agent now writes.

This is the most consequential gap: the structural enforcement mechanism (the hook) is left reading the old signal. The three instruction-layer documents (lobster-oracle.md, sys.dispatcher.bootup.md, CLAUDE.md) are updated consistently and correctly, but they are advisory relative to the hook. The hook is what makes the gate non-optional during compaction.

**What would break:** Every post-PR-861 PR review produces a verdict in `oracle/verdicts/pr-{number}.md`. The hook still greps `oracle/decisions.md` for `**VERDICT: APPROVED**`. Since decisions.md still receives full prose appends (per the PR), the hook continues to function — but its approval signal is now the prose in decisions.md, not the per-PR file. The new per-PR file is written but never checked by the structural enforcer. The benefit of the new format (deterministic first-line read, no grep, no ordering ambiguity) is entirely unrealized at the enforcement layer.

### Format contract for oracle agents

The instruction in lobster-oracle.md is clear: "The first line MUST be exactly `VERDICT: APPROVED` or `VERDICT: NEEDS_CHANGES` (no other text on that line)." The dispatcher/merge agent check is "read first line." This is correctly specified and sufficiently precise for oracle agents.

One ambiguity: the instruction says "When writing Round 2+, keep all previous round content below a `## Round N — [YYYY-MM-DD]` header and prepend the new round at the top." This preserves the first-line invariant (Round 2 prepends a new `VERDICT:` line at the top, overwriting what was there). The word "overwrite each review round" in the section header makes the intent clear. This is adequate.

### CLAUDE.md gate register — compaction survival

The updated CLAUDE.md PR Merge Gate row is complete and specific: file path, first-line check, both branches (APPROVED→merge with archive move, NEEDS_CHANGES→fix→re-oracle→repeat), and the enforcement type (Advisory). The row is longer than the previous version but still fits the table format without prose overflow. Per the golden pattern "table-as-compaction-resistant encoding," each row should be self-contained — this row is. The flow is reconstructable from the table row alone after compaction.

### sys.dispatcher.bootup.md quick-reference

The updated row captures both branches, the file path, and the archive step. Adequate for dispatcher compaction recovery.

### Migration 85 idempotency

The `if [ ! -d "$oracle_verdicts_dir" ]` guard is idempotent — if the directory exists (as it will after the PR ships to the repo), the migration skips. The `LOBSTER_REPO` variable with fallback to `$HOME/lobster` is consistent with how other migrations reference the repo root. Migration number 85 is correctly sequenced (83: PR #851, 84: PR #856, 85: this PR).

One issue: `mkdir -p "$oracle_verdicts_dir/archive"` creates both parent and child directories. The subsequent `touch "$oracle_verdicts_dir/.gitkeep"` is redundant if the directory was just created (it's empty, no .gitkeep needed for runtime use). This is cosmetic — the behavior is correct.

### index.md append — path not specified

The instruction tells the oracle to "append one line to `~/lobster/oracle/verdicts/index.md`" but the index.md file does not exist in the repo and no migration creates it. The first oracle agent to run will need to create it. This is a minor gap — the instruction will still work (the agent will create the file on first write), but the directory structure in the repo (.gitkeep) does not include an index.md seed, making the repo state inconsistent with the instruction's implied structure. This is a documentation-level gap, not an enforcement gap.

### Scope check (learnings.md feature-bundling pattern)

6 files changed: lobster-oracle.md, sys.dispatcher.bootup.md, CLAUDE.md, oracle/verdicts/.gitkeep, oracle/verdicts/archive/.gitkeep, scripts/upgrade.sh. All are described in the PR description. No undocumented scope expansion detected.

---

## Summary of required changes

1. **Required:** Update `hooks/pr-merge-gate.py` to read `oracle/verdicts/pr-{number}.md` (first line) instead of grepping `oracle/decisions.md`. This is the enforcement gap that makes the new format hollow at the structural layer. The hook's `find_oracle_verdict()` function should be replaced with: check for existence of `oracle/verdicts/pr-{number}.md`, read first line, check for `VERDICT: APPROVED`.
2. **Recommended:** Add an `index.md` seed file to `oracle/verdicts/` or add a migration step that creates it with a header row. Not blocking but makes first-run behavior explicit.

**VERDICT: NEEDS_CHANGES** — the hook update is required before the new format delivers its stated benefits at the structural enforcement layer.
