VERDICT: APPROVED
PR: 1050
Round: 1

---

## Stage 1: Vision Alignment

**Prior entering review:** This implementation is solving the wrong problem, or solving the right problem in a direction that forecloses better paths.

**What would have to be true for this to be the right path?**

The PR claims that d8a in user-update.sh is redundant because vision_ref is now managed by the numbered .sql migration system. For this to be the right path: (1) the numbered migration system for registry.db must already be live and managing this column, and (2) the inline shell migration is genuinely the only artifact at risk — nothing else in the upgrade path still depends on d8a running.

The PR asserts both: the `_migrations` table is confirmed present (meaning Migration 56+ has run), and the only file touching WOS schema via shell is d8a. The PR body explicitly names what is not touched (d8b and subsequent steps, agent_sessions.db and memory.db blocks, no .sql files). This is the correct framing for a scoped removal.

**Is there a cheaper test of the underlying assumption?** No — the precondition check (grep for `_migrations` table) is already the cheapest possible test. The assumption is structural: if the table exists, the numbered migration system is live.

**What does this foreclose?** Nothing. Removing a redundant migration path that is already a silent no-op forecloses no viable future path. It clarifies the authorship model (WOS schema via .sql files, not inline shell).

**What is the opportunity cost relative to the vision's stated priorities?** Near zero. This is hygiene that serves `principle-4` (integration rate before new feature rate) by removing noise that would mislead future contributors about where WOS schema migrations belong.

**Alignment verdict: Confirmed.** The PR solves a real problem (misleading authorship signal in upgrade scripts) at appropriate scope. The Stage 1 prior — "this is solving the wrong problem" — is disconfirmed. The precondition-first pattern (verify `_migrations` table exists before deleting the migration that creates the column it manages) is the correct posture.

No pattern from learnings.md fires at the Stage 1 level. No founding premise tension is visible. This is a maintenance action within the established migration authorship model.

---

## Stage 2: Quality Review

### Mechanical pre-scan (from learnings.md document review checklist)

**(1) Feature-bundling check:** PR description mentions two changed files explicitly: user-update.sh (d8a removal) and upgrade.sh (explanatory comment). Diff shows exactly those two files. No undescribed changes. Pass.

**(2) Path reference canonicality:** The added comment in upgrade.sh references `src/orchestration/migrations/` — this is the canonical path for numbered migration files. Pass.

**(3) Encoded Orientation check:** No YAML/JSON files with behavioral fields. No agent definitions modified. No automated behavior changed. The removed block was already a no-op (column exists). Pass — constraint-3 does not apply.

**(4) Resolution-by-absence:** This is not resolving a prior NEEDS_CHANGES gap by removal. This is primary work: removing a confirmed-redundant migration path. No deferred gap re-fires.

### Mirror-constant check (first-pass oracle check per learnings.md 2026-04-26 Meta)

No new frozensets, tuples, or named constants introduced. Pass.

### Does it do what it claims to do?

Yes. The diff confirms:
- d8a block (lines including `ADD COLUMN vision_ref`) is deleted entirely.
- d8b and all sub-steps are untouched (confirmed by examining the diff boundary — d8b's `local registry_db` line appears immediately after the deletion with no gap).
- The explanatory comment is placed correctly above Migration 23 in upgrade.sh.

### What would break without the key decisions made here?

The precondition verification before deletion is the key decision. Without it, removing d8a would be speculative — if any install had not yet run Migration 56+, removing d8a would leave vision_ref absent from that install's registry.db. The PR correctly bounds the deletion to installs where `_migrations` is confirmed present. This is the right ordering: verify the replacement system is live, then remove the redundant path.

### What failure modes exist?

One theoretical failure mode: an install that has the `_migrations` table but somehow did not run Migration 56 (the specific migration that added vision_ref). This is structurally unlikely — the `_migrations` table is itself created as part of the migration bootstrap, so its presence is a strong proxy for "numbered migrations have run." The PR author correctly uses this as the precondition signal.

The `python3` invocation in the removed code used bare `python3` (not `uv run python`) — this is a learnings.md CLI convention violation (PR #804, PR #821, PR #830). However, since the block is being deleted rather than kept, the violation is resolved by removal, not perpetuated.

### What patterns does this introduce that will propagate?

The added comment in upgrade.sh is the most structurally significant part of this PR. It explicitly names:
- Which databases have formal migration runners (registry.db)
- Which do not yet (agent_sessions.db, memory.db)
- The correct authorship direction going forward ("do not add new WOS schema changes here")

This is the canonical-placement-obvious-from-first-encounter golden pattern applied to migration authorship: the comment makes the structural rule self-documenting at the exact boundary where violations have historically occurred (d8a being the example). Future contributors reading past Migration 23 will encounter the rule before they can violate it.

### What does this make easier? What does it make harder?

Easier: understanding where WOS schema migrations belong; auditing that no inline shell manages WOS schema; onboarding contributors to the migration authorship model.

Harder: nothing. The removed path was already a no-op on the live install. Its absence cannot break anything. The comment makes the rule explicit for the path that remains (agent_sessions.db, memory.db), which is the honest statement of current state rather than an implied claim that all databases use the numbered system.

### Encoded Orientation decision check (OODA constraint-3)

This PR does not constitute an Encoded Orientation decision. It removes a silent no-op and adds documentation. No behavioral default is changed. No agent decision-making rule is modified. Constraint-3 does not apply.

---

## Summary

PR #1050 is a clean, correctly-scoped maintenance action. The precondition-first pattern (confirm `_migrations` table before removing the migration step it supersedes) is the correct safety gate. The added comment in upgrade.sh is the highest-value part of the PR — it encodes the migration authorship rule at the boundary where it is most likely to be needed, converting an implicit convention into an explicit inline constraint. No failure modes, no bundled changes, no learnings.md patterns fire against this diff.

**VERDICT: APPROVED**
