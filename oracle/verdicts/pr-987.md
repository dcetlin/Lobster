VERDICT: APPROVED
PR: 987
Round: 2

---

## Round 2 — 2026-04-27

### Prior gap tracking

**Gap 1 (blocking) — Encoded Orientation without logged prior (constraint-3):** ADDRESSED. `oracle/verdicts/decision-github-rate-limit-gate.md` added in this round. The document names the gate behavior, the threshold value as the authorized default, both vision.yaml anchors by exact field path (`principle-1` at line 63, `constraint-3` at line 44 — both verified to resolve in `vision.yaml`), and the authorizing rationale. Structure matches the established pattern of `decision-system-retrospective-automation.md`. Constraint-3 satisfied.

**Gap 2 (advisory) — Stale test module docstring:** ADDRESSED. The module docstring at lines 10–18 of `test_github_rate_limit_check.py` now correctly states the `--jq` expression as `'.rate | {remaining, reset}'`, matching the actual implementation.

**Gap 3 (advisory) — Dead `context_managers` variable and unused `_run_main_with_clean_argv` helper:** ADDRESSED. Neither `context_managers` nor `_run_main_with_clean_argv` appear anywhere in the diff. Both have been removed.

### Stage 1 findings (locked from Round 1)

The adversarial prior entering Round 1 was: this work is solving a hypothetical problem, adding an Encoded Orientation gate without a logged decision, and consuming capacity better directed at the open oracle gaps named in `current_focus.current_constraint`. The blocking item was the missing decision document. The implementation was technically sound throughout; the gap was the Encoded Orientation audit trail, not the code.

That gap is now closed. The Stage 1 concern — that the gate introduces autonomous behavioral suppression without a logged prior — is resolved by the decision document, which places this gate in the same structural class as the steward escalation and smell-pattern automation decisions.

### Stage 2 verification

**Decision document correctness.** Both cited vision.yaml field paths resolve: `principle-1` (structural prevention is preferred over reactive recovery) and `constraint-3` (Encoded Orientation requires prior logged decision and traceable vision.yaml anchor). No fabricated paths. The learnings.md PR #894 pattern ("Decision-file vision anchor citation can name a non-existent field path while still satisfying constraint-3 at the substance level") does not fire here — paths are real and verified.

**Implementation quality** (unchanged from Round 1, confirmed clean):
- `RateLimitStatus` frozen dataclass with `ok`, `remaining`, `reset_at` fields: correct application of the classification-result-as-typed-dataclass golden pattern (golden-patterns.md 2026-04-04).
- `GITHUB_RATE_LIMIT_DISPATCH_THRESHOLD` is a named module-scope constant imported by tests, not re-declared locally — avoids the mirror-constant pattern (learnings.md PRs #696, #800, #967).
- Fail-open on all error paths: subprocess failure, non-zero exit, FileNotFoundError, unparseable JSON all return `ok=True`.
- Gate is per-cycle, not per-UoW. The test `test_rate_limit_check_called_once_per_cycle` verifies this.
- Integration tests for `main()` correctly patch `check_github_rate_limit` at module scope on a freshly-loaded module instance — avoids the module-level constant computed-at-import-time anti-pattern.

**No new issues introduced in Round 2.** The added decision document is clean, correctly scoped, and adds no new code paths.

---

## Round 1 — 2026-04-27

### Stage 1 — Vision Alignment (formed before reading implementation)

**Theory of change check.** The current project phase is WOS reliability and observability (PRs #891-#898 form the reference set; vision.yaml `current_focus.this_week.primary`). A rate limit pre-dispatch check is protective infrastructure. Before reading the diff, the adversarial prior was: *this work is solving a hypothetical problem, adding an Encoded Orientation gate without a logged decision, and consuming capacity better directed at the open oracle gaps named in `current_focus.current_constraint`.*

**What would have to be true.** GitHub API exhaustion would need to be an observed or credibly imminent failure mode — not a speculative one. Issue #984 frames it as observed ("when the rate limit is exhausted, the subagent burns its full TTL"), but does not present observed instances. The fix is structurally correct if the problem is real; if not, the gate adds behavioral complexity without corresponding reliability gain.

**What this forecloses.** The `GITHUB_RATE_LIMIT_DISPATCH_THRESHOLD = 100` constant is an Encoded Orientation decision under vision.yaml `constraint-3`: it causes the system to autonomously suppress entire dispatch cycles without Dan's explicit input. No logged decision in `oracle/verdicts/`, `vision.yaml open_decisions`, or a decision document authorizes this gate. Constraint-3 is explicit: "Encoded Orientation decisions require a prior logged decision of the same class and a traceable vision.yaml anchor. When neither exists, escalate to the Deliberative register." Neither exists here.

**Opportunity cost check.** vision.yaml `current_focus.current_constraint` names two open NEEDS_CHANGES gaps (#894, #896) requiring design-decision logging before re-oracle. This PR bypasses the same requirement it was meant to defer in those PRs, creating a consistency failure: the constraint applies to this gate exactly as it applied to needs-human-review escalation and smell-pattern automated filing.

**Alignment verdict: Questioned.** The implementation serves a real reliability concern, but the Encoded Orientation check fires on the threshold constant, and no logged decision exists.

---

### Stage 2 — Quality Review (Round 1)

#### What the PR does

Adds `check_github_rate_limit()` (pure function, subprocess-based) and `RateLimitStatus` (frozen dataclass) to `executor-heartbeat.py`. Calls the check once per dispatch cycle in `main()`, after the dry_run guard and context pressure throttle, before `run_executor_cycle()`. If `remaining < 100`, skips the cycle and returns 0. Fail-open on all error paths.

#### What works well

The implementation is technically clean. The golden pattern "classification result as typed frozen dataclass with observability fields" (golden-patterns.md 2026-04-04) is correctly applied: `RateLimitStatus` carries `ok`, `remaining`, and `reset_at` — exactly the observability fields that allow the caller to log without re-querying. The fail-open design is correct: a tooling failure is not a reason to suppress user work. The threshold is a named module-scope constant imported by tests rather than re-declared locally (avoiding the mirror-constant pattern). The test suite is well-structured: boundary case at exact threshold, fail-open paths for non-zero exit code, FileNotFoundError, and unparseable JSON, plus integration tests that exercise `main()` directly.

#### Issues found

**Issue 1 — Encoded Orientation without logged prior (constraint-3, blocks merge).**

`GITHUB_RATE_LIMIT_DISPATCH_THRESHOLD = 100` encodes a new behavioral default: the system autonomously suppresses dispatch cycles without Dan's explicit input when remaining API quota is below 100. This meets the definition of Encoded Orientation under constraint-3. No prior logged decision exists in `oracle/verdicts/`, `vision.yaml open_decisions`, or a decision document. PR #894 (needs-human-review) and PR #896 (smell-pattern automation) were both returned with NEEDS_CHANGES under this exact requirement. The same constraint applies here. Resolution: create a decision document (e.g., `oracle/verdicts/decision-github-rate-limit-gate.md`) citing the vision.yaml anchor (principle-1: "structural prevention over reactive recovery" is the relevant anchor) and logging the threshold value as the authorized default.

**Issue 2 — Module docstring in test file references stale `--jq` expression.**

The test module's top docstring (line 6) says the function "shells out to `gh api rate_limit --jq .rate.remaining`". The actual implementation uses `--jq '.rate | {remaining, reset}'` (a JSON object with both fields). The docstring is a stale copy from issue #984, which used the single-field form. The `_rate_json` helper's inline comment on line 79 correctly reflects the actual expression. The discrepancy is documentation-only and does not affect test correctness, but it will mislead future engineers reading the test file to understand the shell command. Fix: update the module docstring to match the actual `--jq` expression.

**Issue 3 — Dead code in `_run_main_with_clean_argv` test helper.**

The helper builds a `context_managers` list via a list comprehension on the first line (before `with patch("sys.argv", ...)`), then immediately builds a second `all_patches` list that supersedes it. The `context_managers` variable is never used. This is the "dead code immediately before an unconditional override" failure mode (learnings.md PR #848). The dead list comprehension also has a type mismatch: it mixes `patch.object(...)` and `patch.object(..., **kwargs)` based on whether `kwargs` is a dict — but `all_patches` always uses `patch.object(hb_module, name, new=val)`, so the dead comprehension's branching is never evaluated. Fix: remove `context_managers` entirely. Note: `_run_main_with_clean_argv` is never actually called by any test — it is defined but unused. The integration tests call `_hb_fresh.main()` directly via the `with (...)` block. Consider removing the helper or noting its status.

#### Revision contract (Round 1)

- **Issue 1 (blocking):** Add a decision document logging the threshold gate as an authorized Encoded Orientation decision with a traceable vision.yaml anchor.
- **Issue 2 (advisory):** Update the test module docstring to reflect the actual `--jq` expression used.
- **Issue 3 (advisory):** Remove unused `context_managers` variable and consider removing the unreferenced `_run_main_with_clean_argv` helper.
