VERDICT: APPROVED
PR: 1217
Round: 2

## Round 2 — 2026-05-19

**Stage 1: Vision alignment**

The adversarial prior: is this solving the wrong problem? The PR adds category-specific restart guidance to upgrade.sh, distinguishing three change classes (MCP server config, bootup files/hooks, Python logic) and their correct restart mechanisms. The prior asking "what would have to be true for this to be wasted effort?" surfaces one candidate: if the real fix is a hook that mechanically detects what changed and fires the right restart automatically, this advisory text is a compensatory layer over a missing structural enforcement. That candidate does not hold — the safe wrapper script (restart-mcp.sh) already exists as the structural gate for the dangerous MCP restart; the guidance added here is for the non-dangerous operations (bootup files, Python logic) where mechanical enforcement would require upgrade-time diff inspection, a much larger scope change not justified by the risk level. The vision.yaml principle-1 ("proactive resilience over reactive recovery") is served: filling in previously absent guidance is structural prevention, not reactive correction. principle-4 ("wire what exists before building more") is also served: the safe wrapper already exists; this PR connects the mental model of "what to do after an upgrade" to the existing infrastructure. The work is correctly scoped and is not solving the wrong problem.

**Alignment verdict: Confirmed**

---

**Stage 2: Prior gap verification and quality review**

**Prior gap tracking (Round 1 to Round 2):**

Gap 1 (Round 1): restart_services() guidance conflated restart-mcp.sh (MCP server process restart) with a dispatcher session restart (required for bootup file and hook script changes). Status: addressed. Commit 4a948d6d adds three distinct categories to restart_services():
- MCP server config changes -> ~/lobster/scripts/restart-mcp.sh, with the explicit CLAUDE.md-mandated warning not to run sudo systemctl restart lobster-mcp-local directly.
- Bootup files / hook scripts -> sudo systemctl restart lobster-claude, with a correct explanation: "these are loaded at session start and cannot be reloaded mid-session."
- Python logic / migrations / job files -> no manual restart needed.

The distinction is now unambiguous and technically correct. The earlier conflation (treating both restart types as interchangeable) is eliminated.

Gap 2 (Round 1): PR description claimed "No logic changes" but the branch included executor-heartbeat.py commits with no accurate labeling. Status: addressed. The current PR description enumerates all 4 commits explicitly, labels the first two as "no-op relative to main (identical change already merged via PR #1215)" with a parenthetical confirmation of mergeability, and identifies the primary stated scope (b19e5b0a) and the Round 1 oracle fix (4a948d6d). The description is accurate.

**Additional Stage 2 findings:**

Internal consistency: the post-upgrade checklist added to the UPGRADE COMPLETE banner mirrors the same three-category structure as restart_services(), with identical commands. No inconsistency between the two locations.

Safety notes: the restart_services() guidance correctly reproduces the CLAUDE.md-mandated warning ("Never run 'sudo systemctl restart lobster-mcp-local' directly -- use the safe wrapper.") for the MCP restart path, and correctly does not apply the same restriction to sudo systemctl restart lobster-claude (which is a safe direct invocation per CLAUDE.md).

Encoded Orientation check (constraint-3): the change adds information output in an upgrade procedure. It does not change behavioral defaults, system constraints, agent identity, or decision-making rules in a durable way. The Encoded Orientation check does not apply.

Advisory enforcement check (learnings.md 2026-05-14, #1148): this pattern asks whether guidance that could be structural should instead be a hook. Here the guidance is operational post-upgrade advice -- making it structural would require the upgrade script to inspect the diff at merge time and selectively fire restart commands, a much larger scope change that is not indicated by the risk profile of these operations. The advisory register is correct.

No residual gaps from Round 1 remain open. No new gaps introduced in Round 2.

---

## Round 1 — 2026-05-19

VERDICT: NEEDS_CHANGES

Gap 1: restart_services() guidance conflated restart-mcp.sh (MCP server restart) with a dispatcher session restart. The guidance offered restart-mcp.sh as the action for all three change categories including bootup files and hook scripts, both of which require a new Claude Code session (sudo systemctl restart lobster-claude), not an MCP server restart. The post-upgrade checklist had the same error.

Gap 2: PR description stated "No logic changes" but the PR contained executor-heartbeat.py commits (threshold change and comment fix) from the PR #1215 workstream. Description did not enumerate all commits or label the no-ops.

Full Round 1 findings preserved in the Round 1 oracle run context. The Round 1 verdict file was first created at that time with the full Stage 1 and Stage 2 analysis.
