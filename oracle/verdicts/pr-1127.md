VERDICT: NEEDS_CHANGES
PR: 1127
Round: 2

## Round 2 — 2026-05-10

### Stage 1 (unchanged from Round 1)

Stage 1 finding is locked. The alignment verdict is Questioned, not Misaligned. The PR is coherent with Lobster's overall mission (reducing friction for Dan to interact with his cognitive extension). It is not coherent with the declared `current_focus.current_constraint` ("oracle/verdict completeness"). This tension does not block the review.

---

### R1 Gap Status

**Gap 1 (routing ambiguity):** ADDRESSED. The bootup doc now specifies the correct condition verbatim — `text.startswith("/todo ") or text == "/todo"` — with an explicit comment: "Do NOT use a bare startswith('/todo') — that would also match '/todos'." Two new tests added: `test_todos_command_is_not_routed_to_route_todo_command` verifies the safe fallback; `test_dispatcher_routing_condition_excludes_todos` is a unit test that asserts the correct condition excludes `/todos` and the incorrect condition would not.

**Gap 2 (snooze trailing-number ambiguity):** ADDRESSED. `_SNOOZE_RE` now carries a multi-line comment naming the ambiguity and directing users to use the item ID. `_USAGE` includes the tip: "Tip: for items whose text ends in a number, use the item ID (e.g. '/todo snooze 42' instead of '/todo snooze call 911')." New test `test_snooze_trailing_number_in_item_text_is_consumed_as_days` documents the current behavior and verifies the usage tip is present.

**Gap 3 (NL routing Encoded Orientation):** ADDRESSED. `oracle/decisions/decision-todo-nl-routing.md` created with: (a) vision.yaml anchors (constraint-3 and principle-3), (b) explicit statement that NL routing is authorized as a durable dispatch default, and (c) risk acknowledgment that extraction errors produce silently incorrect add operations, with disable instructions.

---

### Stage 2: Round 2 Quality Review

#### New gap introduced in Round 2

**Gap R2-1: Module docstring integration example repeats the incorrect routing condition**

The module docstring in `src/los/todo_commands.py` (lines 14–17) provides an integration example for the dispatcher:

```python
if msg.get("text", "").lower().startswith("/todo"):
    reply = route_todo_command(msg)
    send_reply(chat_id=chat_id, text=reply, source=source)
```

This example uses the bare `startswith("/todo")` condition — exactly the incorrect form that Gap 1 fixed in the bootup doc. The bootup doc now correctly says `text.startswith("/todo ") or text == "/todo"`. The module docstring says `startswith("/todo")`.

Any developer reading the module to understand integration — the most natural starting point — would copy this example and reproduce the defect that Gap 1 identified and fixed. The fix was applied to one surface but the same incorrect pattern was preserved in the module's own documentation.

This is the "single-file fix scope misses sibling location with the same defect" pattern from learnings.md entry `#1122 R3`: when a gap fix corrects a routing condition in one location, verify the same incorrect form is not present in any sibling documentation within the same PR. The module docstring is a sibling location that was not checked.

**Fix required:** Update the integration example in the `src/los/todo_commands.py` module docstring to use the correct condition:
```python
# Correct condition: text.startswith("/todo ") or text == "/todo"
if text.startswith("/todo ") or text == "/todo":
    reply = route_todo_command(msg)
    send_reply(chat_id=chat_id, text=reply, source=source)
```

#### Structural observation (no action required)

The decision file was placed in a new `oracle/decisions/` subdirectory. The existing `oracle/decisions.md` is deprecated (it carries a deprecation header directing decisions to `oracle/verdicts/`). This creates a third possible canonical home for behavioral decisions: `oracle/verdicts/`, deprecated `oracle/decisions.md`, and now `oracle/decisions/`. This is a placement ambiguity that a future PR should resolve — but it is a lower-severity structural debt, not a blocking gap in this PR. The decision file's content meets the R1 contract requirements (vision.yaml anchor, authorization statement, risk acknowledgment, disable instructions), regardless of its directory location. This observation is logged here for tracking but does not constitute a NEEDS_CHANGES finding.

---

### Round 2 Revision Contract

**Gap R2-1 (module docstring incorrect routing example):** Update the integration example in the `src/los/todo_commands.py` module docstring to use `text.startswith("/todo ") or text == "/todo"` instead of `startswith("/todo")`. This is a one-line fix.

Once Gap R2-1 is addressed, all R1 gaps are closed and no other blocking issues are present. The PR is otherwise ready for merge.

---

## Round 1 — 2026-05-10

### Stage 1: Vision Alignment

**Theory of change in vision.yaml:** The system should extend Dan's cognitive reach without increasing screen dependency. The active_project phase is "WOS Phase 1 + Vision Object substrate." The `current_focus.this_week.primary` is WOS observability and reliability. The `horizon` points to Orient implementation (#733) after WOS feedback loop gaps close.

**What would have to be true for this work to be the right path?** The LOS TODO system is a live feature and this PR closes a real UX gap (no way to add/mark-done/snooze a TODO without navigating to the full list first). The work is additive and isolated — it does not touch WOS, vision infrastructure, or the feedback loop.

**Alignment tension:** Principle-4 ("Integration rate before new feature rate — wire what exists before building more") names the exact tension: the WOS feedback loop has open gaps (PR #894, PR #896 NEEDS_CHANGES rounds), and this PR ships new LOS command surface on a different track. The `current_focus.current_constraint` explicitly names "oracle/verdict completeness" as the active constraint. This work is not prohibited — the vision does not freeze all non-WOS work — but it is off the stated horizon.

**Stage 1 verdict:** Questioned, not Misaligned. The PR is coherent with Lobster's overall mission (reducing friction for Dan to interact with his cognitive extension). It is not coherent with the declared current_focus constraint. This tension does not block the review — it is noted as an alignment signal.

The oracle vocabulary check from learnings.md (PR #714 pattern: "new module with zero wiring is a library with no production callers") is relevant here — see Stage 2 for the wiring analysis.

---

### Stage 2: Quality Review

#### Pre-scan checks (mechanical, applied before diff reading)

**Feature bundling:** 4 changed files — `sys.dispatcher.bootup.md`, `src/los/__init__.py`, `src/los/todo_commands.py`, `tests/unit/los/test_todo_commands.py`. All four are described in the PR body. No unexplained files. Pass.

**Mirror-constant check:** Test file imports `PRIORITY_DEFAULT` and `SNOOZE_DEFAULT_DAYS` from `src.los.todo_commands` (production module). No local re-declarations. Pass.

**Encoded Orientation check (OODA constraint-3):** The bootup doc update adds two behavioral defaults: (1) route `/todo` subcommands synchronously via `route_todo_command`, and (2) natural language routing — construct synthetic `/todo` commands from phrases like "add X to my list" / "mark X done." Item (1) is a feature routing addition consistent with existing synchronous command handling. Item (2) is a new judgment-based dispatch pathway. See Gap 3 below.

**Schema migration:** No new DB columns. The module uses the existing LOS DB schema unchanged. No upgrade.sh migration needed. Pass.

---

#### Gap 1: Routing ambiguity — `/todos` matches the `/todo` routing trigger

The bootup doc instructs the dispatcher: `# msg.text starts with "/todo"`. The command `/todos` starts with `/todo`, so this routing condition will match both `/todos` and `/todo add|done|snooze` messages.

`route_todo_command` receives `/todos` as input, finds no regex match, and returns the `_USAGE` help text. The user who typed `/todos` expecting their item list gets a usage string instead.

The dispatcher is LLM-driven and will likely handle this contextually from the full document — but the instruction is ambiguous, the failure mode is real, and no test verifies that `/todos` is NOT processed by `route_todo_command`.

**Fix required:**
- In the bootup doc, change the routing comment to: `# msg.text starts with "/todo " (space after /todo) or msg.text == "/todo"` — making the `/todos` exclusion explicit.
- Add a test: `route_todo_command({"text": "/todos", ...})` should NOT be called at all, or if called, verify it returns the help text (documenting the safe fallback behavior). The test should assert that the dispatcher doc's routing condition correctly discriminates.

The learnings.md PR #1125 entry ("consumer PR written against producer schema that changed in a sibling PR") pattern applies structurally: the dispatcher routes `/todos` and `/todo add` through adjacent code paths that share a prefix; an ambiguous routing condition produces a silent behavioral failure.

---

#### Gap 2: Snooze regex silently misparses item text ending in numbers — no test, no doc warning

`_SNOOZE_RE = re.compile(r"^/todo\s+snooze\s+(.+?)(?:\s+(\d+))?\s*$", re.IGNORECASE)` uses a lazy quantifier that makes the trailing `(\d+)` group greedy from the right. For input `/todo snooze call 911`, the regex parses `query='call'` and `days='911'`.

If a user has an item called "call 911" and types `/todo snooze call 911`, they get: item "call" is searched (not found or wrong match), not item "call 911" snoozed for the default 3 days.

This is the learnings.md PR #980 "parse_diagnose_command returns full remainder instead of first token" pattern in reverse — here the parse is too aggressive at extracting the days argument. The ID-based form exists as the workaround (`/todo snooze 42`), but there is no warning to the user, no test covering this case, and the failure is non-obvious.

**Fix required:**
- Add a comment in `_SNOOZE_RE` naming the ambiguity: "A trailing integer is always parsed as days — use `/todo snooze <id>` for items whose text ends in a number."
- Add the disambiguation note to `_USAGE` (e.g., "Tip: for items whose text ends in a number, use the item ID.").
- Add a test that demonstrates the behavior: `route_todo_command({"text": "/todo snooze call 911", ...})` with an item named "call 911" in DB — verify the user sees an informative result (either the item is found by partial match "call" or a disambiguation/not-found message explains what happened).

---

#### Gap 3: Natural language routing instruction adds Encoded Orientation without a logged prior (constraint-3)

The bootup doc adds:
```
Natural language triggers the dispatcher should also route via route_todo_command:
- "add X to my list" / "remind me to X" → treat as /todo add X
- "mark X done" / "X is done" → treat as /todo done X

For natural-language routing, construct a synthetic /todo command from the extracted intent and pass it as msg["text"] to route_todo_command.
```

This encodes a new durable behavioral default: when the dispatcher sees these natural-language patterns, it should (a) classify intent, (b) construct a synthetic `/todo` command, and (c) route to `route_todo_command` — all without a subagent and without Dan's per-message input. This is an Encoded Orientation decision under OODA constraint-3.

The learnings.md PR #894 entry establishes the pattern: "A GitHub issue describing a design decision is not equivalent to a logged oracle/vision decision." There is no entry in `oracle/decisions.md`, no open_decision in `vision.yaml`, and no oracle verdict that authorizes natural language → synthetic command construction as a durable dispatch default.

The natural language routing also introduces a failure mode the `/todo` command path cannot handle: the dispatcher extracts intent from free text, constructs a synthetic `msg["text"]`, and calls `route_todo_command`. If the extraction is imprecise (e.g., "remind me to call Sarah tomorrow" → `/todo add call Sarah`), `route_todo_command` receives the synthetic text with no awareness that it came from NL extraction. The structured command path assumes precise input; the NL extraction path produces approximate input. There is no error surface when the extraction is wrong — the item is silently added with extracted text.

**Fix required:**
- Either remove the natural language routing section from the bootup doc for now and defer it to a follow-on PR with a logged decision, OR
- Add an entry to `oracle/decisions.md` with: (a) the specific vision.yaml anchor (principle-3 "Determinism over judgment for conditionals" is directly relevant — NL intent extraction is judgment, not deterministic), (b) explicit statement that this is authorized as a durable dispatch default, and (c) the risk acknowledgment that extraction errors will produce silently incorrect add operations.

The `/todo add|done|snooze` routing itself (items 1 and 2) does NOT require a decision record — it is deterministic routing of explicit commands. Only the NL extraction path falls under constraint-3.

---

#### What is structurally sound (no action needed)

- **Module structure:** Pure functions with injected `conn`, named constants for all spec-derived values, explicit disambiguation rather than silent first-match. Correctly applies the learnings.md golden pattern for clean LOS DB boundary design.
- **Test quality:** Constants are imported from production (not re-declared), fixtures use in-memory SQLite, each test has a clear behavioral assertion. Mirror-constant antipattern correctly avoided.
- **Dedup logic:** `handle_todo_add` correctly uses `find_duplicate` → `increment_mention_count` path. The `existing.mention_count + 1` display is correct (it adds 1 to the pre-increment snapshot to show the post-increment value).
- **7-second rule compliance:** The bootup doc explicitly notes synchronous handling is appropriate because DB writes complete in milliseconds. This is consistent with existing synchronous command routing (`route_los_callback` for todo-callbacks).
- **No DB schema migration required:** The new module builds on the existing LOS schema with no new columns.
- **No feature-bundling violations:** All 4 changed files are described in the PR body.
- **No mirror-constant violations:** Test imports production constants correctly.

---

### Revision contract

Each gap must be resolved in one of three ways:
- (a) **Addressed** — the revision shows the specific fix described above
- (b) **Disputed** — the author states why the gap does not apply, with specific reason
- (c) **Deferred** — the author acknowledges the gap and states why it is not addressed now

Generic "improvement" without tracing to a named gap does not count as resolution.

**Gap 1 (routing ambiguity):** Fix the bootup doc routing condition to explicitly exclude `/todos` from the `/todo` handler trigger, and add a test demonstrating the discrimination.

**Gap 2 (snooze regex):** Add a comment naming the ambiguity, a usage tip in `_USAGE`, and a test covering the trailing-number item text case.

**Gap 3 (NL routing):** Either remove the NL routing section and defer to a separate PR with a logged decision, or add the decision record to `oracle/decisions.md` with vision.yaml anchor and risk acknowledgment.
