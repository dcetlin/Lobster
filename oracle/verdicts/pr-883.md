---
oracle_status: approved
oracle_pr: https://github.com/dcetlin/Lobster/pull/883
oracle_date: 2026-04-23
---

# Oracle Verdict: PR #883

**PR title:** docs(wos): integrate metabolic primitives as typed stock in architecture diagram
**Date:** 2026-04-23
**Replaces:** PR #882 (NEEDS_CHANGES — superseded by clean branch)
**Verdict:** APPROVED

---

## Prior gap tracking (document review — PR #882 NEEDS_CHANGES)

Three gaps were named in the PR #882 verdict:

- **Gap 1: Feature-bundling** — `docs/assessments/2026-04-23-system-retrospective.md` and `data/smell-patterns.yaml` were present in the diff but unmentioned in the PR body. **Status: addressed.** PR #883 is a clean branch from main. The diff contains only `docs/wos-pipeline-architecture-20260422.md`. No retrospective, no smell-patterns.yaml, no data/ directory. Verified via `gh pr diff 883`.

- **Gap 2: Stale oracle path in architecture diagram** — The ORACLE node referenced `oracle/decisions.md`; PR #875 deprecated that path in favor of `oracle/verdicts/pr-NNN.md`. **Status: addressed.** All three stale references updated: the Mermaid ORACLE node label, the loop description in Section 5, and the Component Reference Table entry.

- **Gap 3: smell-patterns.yaml lacks design decision anchor** — Resolves with Gap 1. The file is absent from this PR entirely.

All three gaps are addressed.

---

## Stage 1: Vision Alignment

This PR is a clean re-submission of the diagram changes that were already confirmed as aligned in the PR #882 Stage 1 review. Stage 1 finding is unchanged: the Metabolic Output Layer subgraph extends the architecture diagram to show where pearls, seeds, heat, and shit go structurally after a UoW result is written. It references Issue #880 (outcome_refs schema spec) as the formal anchor. The architecture document has always led code in this repo — forward specification of the design layer is the established pattern.

Vision principle-4 ("wire what exists before building more") does not prohibit documentation of design decisions. The metabolic vocabulary was already present in the document; the subgraph makes the flow explicit.

**Alignment verdict: Confirmed.** The diagram update is correctly scoped, correctly anchored, and correctly sized. No directions are foreclosed.

---

## Stage 2: Quality Review

### Encoded Orientation check

No behavioral defaults, system constraints, agent identity, or decision-making rules changed. The diagram update describes a design; it does not encode new behavioral rules. Constraint-3 does not fire.

### Gap resolution assessment

**Gap 1 (feature-bundling):** The diff is exactly one file — `docs/wos-pipeline-architecture-20260422.md`. 53 insertions, 9 deletions. Everything is what the PR title describes. The feature-bundling learning from #882 is fully honored.

**Gap 2 (stale oracle path):** Three references updated:
1. Mermaid ORACLE node: `oracle/decisions.md` → `oracle/verdicts/pr-NNN.md`. The node now also correctly shows `oracle/verdicts/` as the directory (previously just `oracle/`).
2. Section 5 loop description: `oracle/decisions.md` → `oracle/verdicts/pr-NNN.md`.
3. Component Reference Table: oracle row `oracle/` → `oracle/verdicts/`, verdict path updated.

All three are internally consistent. No residual `decisions.md` references remain in the file.

**Gap 3 (smell-patterns.yaml):** Absent from this PR. Gap resolves with Gap 1.

### Diagram quality (unchanged from #882 Stage 2 assessment)

The Mermaid subgraph is well-formed and internally consistent. The METABOLIC subgraph label references Issue #880. The four outcome types correctly differentiate stock-with-address (pearl/seed) from energy-with-log (heat/shit). The generative loop `SEED_UOW -.->|generative loop| GH` is correctly represented with a dashed line. The Outcome Categories table correctly adds `Stock type`, `outcome_refs required?`, and `Downstream flow` columns. The "Key principle" note is the design principle in one sentence.

### No new findings

No new issues surfaced in this review. The PR is exactly what it describes.

---

## VERDICT: APPROVED

All three gaps from PR #882 are addressed. The diff is clean — one file, two commits, changes match the PR title. The stale oracle path references are corrected throughout. No new issues introduced.
