VERDICT: APPROVED
MODE: VERIFICATION
PR: 1331
Round: 2

**Prior gaps from Round 1:**
1. Feature-bundling (blocking) → **ADDRESSED.** PR now contains a single commit referencing only `uow_20260526_f13643`. The orphan-exit fix commits have been removed. Single file changed: `tests/unit/test_wos_health_check.py` (+273 lines).
2. No automated tests for Phase 3 monitors (blocking) → **ADDRESSED.** 16 tests added across three test classes covering all three functions (`check_dark_pipeline`, `check_ready_queue_drain`, `check_issue_rate_trend`).

**What was checked and why it passes:**

Test coverage: Each Phase 3 monitor function has tests for both healthy (returns None) and alert (returns string) paths. Boundary cases (exact threshold boundary), error cases (absent DB, unavailable gh CLI, CLI timeout), and observability details (queue depth in alert strings) are all covered.

Constant usage: All threshold constants are imported from the production module via `hc.DARK_PIPELINE_THRESHOLD_DAYS`, `hc.READY_QUEUE_DRAIN_WINDOW_MINUTES`, `hc.ISSUE_RATE_WINDOW_DAYS`, `hc.ISSUE_RATE_ALERT_RATIO`. No mirror constants — follows the learnings.md guidance from PRs #696, #800, #967, #970.

Subprocess mocking: `patch("subprocess.run", side_effect=...)` correctly patches the global `subprocess.run` which the production code calls directly. The side_effect dispatch on `--state` argument correctly routes opened vs. closed queries. Error paths (FileNotFoundError, TimeoutExpired) propagate through `_gh_count`'s exception handler to return 0, producing the expected None result.

No Encoded Orientation changes — test-only PR, no behavioral defaults or system constraints modified.

---

## Stage 1: Vision Alignment (retained from Round 1)

**Theory of change:** vision.yaml principle-1 (proactive resilience over reactive recovery) directly supports monitoring the WOS execution loop for silent failures. The current_focus confirms the WOS executor is enabled and actively running — monitoring is the correct next investment for a running pipeline. The Phase 3 monitors serve the "substrate that makes execution self-regulating" goal.

**What would have to be true:** The WOS pipeline must be observable to be manageable. Dark pipeline detection (audit_log silence), ready-queue stalls (growth without drain), and issue accumulation (open/close ratio) are genuine operational health signals.

**Alignment verdict:** Confirmed.

**Learnings.md pattern citation — feature-bundling (behavioral change):** The feature-bundling pattern (2026-05-14 #1148) constrained this review by making the first mechanical check "count distinct UoW references and commit message prefixes." Round 2 passes: single commit, single UoW.

**Learnings.md pattern citation — mirror-constant (behavioral change):** The mirror-constant pattern (PRs #696, #800, #967, #970) constrained Stage 2 by making constant-import verification a first-pass check before evaluating test logic. All constants are imported from the production module via `hc.*` — no local re-declarations.

## Stage 2: Quality Review

### Prior Gap Status

| Gap | Round 1 Status | Round 2 Status |
|-----|---------------|---------------|
| Feature-bundling | NEEDS_CHANGES (blocking) | Addressed — single commit, single UoW |
| No automated tests | NEEDS_CHANGES (blocking) | Addressed — 16 tests across 3 test classes |

### Test Quality

**TestCheckDarkPipeline (5 tests):** Covers recent audit activity (healthy), stale audit beyond threshold (alert), empty audit_log (alert), absent DB (healthy — graceful degradation), and exact threshold boundary (healthy). Alert assertions verify both the "Dark pipeline" prefix and the threshold value in the message — not substring-only.

**TestCheckReadyQueueDrain (5 tests):** Covers growth+drain (healthy), growth without drain (alert), no new entries in window (healthy — existing old entries don't trigger), absent DB (healthy), and queue depth observability in alert message. The growth/drain test correctly sets up audit entries with both `to_status` and `from_status` fields matching the production SQL.

**TestCheckIssueRateTrend (6 tests):** Covers ratio below threshold (healthy), ratio exceeds threshold (alert), opened>0 with zero closures (alert), both counts zero (healthy), gh CLI unavailable (healthy — FileNotFoundError), and gh CLI timeout (healthy — TimeoutExpired). The subprocess mock dispatch on `--state` argument is correct and matches the production `_gh_count` call signature.

### Test Infrastructure

The `_insert_audit_entry` helper follows the same pattern as the existing `_insert_uow` helper — direct sqlite3 connection to the tmp_path fixture DB, time offset via `_hours_ago` / `timedelta(minutes=...)`.

The `db_path` fixture already creates both `uow_registry` and `audit_log` tables, so no fixture changes were needed for the Phase 3 tests.

### Noted (non-blocking)

The PR description documents 3 pre-existing test failures in `TestJobsJsonGate` due to stale `_jobs_file` / `_is_job_enabled` references. These are not caused by this PR and are correctly identified as pre-existing.

---

## Round 1 — 2026-05-26

VERDICT: NEEDS_CHANGES
MODE: VERIFICATION
PR: 1331
Round: 1

**Feature-bundling (blocking):** PR contains two distinct UoW workstreams — `uow_20260522_cf8707` (SubagentStop hook orphan-exit fix) and `uow_20260526_f13643` (Phase 3 observation loop monitors) — with different commit prefixes (`fix(wos)` vs `feat(wos)`). The PR description covers only the Phase 3 monitors. Per learnings.md feature-bundling pattern (2026-05-14 #1148): distinct workstreams = NEEDS_CHANGES. Split into two PRs.

**No automated tests for Phase 3 monitors:** `check_dark_pipeline`, `check_ready_queue_drain`, and `check_issue_rate_trend` (163 lines of new implementation) have no automated test coverage. All three functions accept explicit parameters (`db_path`, `threshold_days`, `window_minutes`, `repo`, `alert_ratio`) making them directly unit-testable. The PR test plan uses manual dry-run verification only.
