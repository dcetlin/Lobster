VERDICT: APPROVED
PR: 980
Round: 2

---

## Round 2 — 2026-04-26

### Prior gap status (all 4 gaps from Round 1)

- **Gap 1 (comment/code mismatch in handle_wos_diagnose):** ADDRESSED. The block comment on `handle_wos_diagnose` (lines 91-94 of the diff) now correctly says: "Unlike wos_escalate (which has send_reply branches and runs before the spawn-gate), wos_diagnose always returns action='spawn_subagent' and runs inside the spawn-gate block. The gate enforcement check is always satisfied for this handler, making the gate redundant but not harmful." The dispatch table comment (lines 71-74) says "runs inside the spawn-gate block where the enforcement check is always satisfied." The `route_wos_message` fast-path comment (lines 317-322) was also updated to list `wos_diagnose` among the execution message types in the spawn-gate block. All three comment surfaces now agree with the code structure.

- **Gap 2 (inconsistent bootup doc code examples):** ADDRESSED. The `sys.dispatcher.bootup.md` section now has exactly one code block. It follows the correct order: `mark_processing(message_id)` → `route_wos_message(diagnose_msg)` → `Task(...)` → `send_reply(...)` → `mark_processed(message_id)`. The redundant second block with inconsistent flow ordering is gone.

- **Gap 3 (parse_diagnose_command returns full remainder):** ADDRESSED. The implementation uses `tokens = remainder.split(); if tokens: return tokens[0]` — first token only. The test `test_uow_id_token_is_first_word_after_diagnose` asserts `result == "uow_001"` (equality, not substring) for input `"diagnose uow_001 extra ignored"`. Both the function and the test assertion are correct.

- **Gap 4 (hardcoded MAX_RETRIES and HARD_CAP in prompt):** ADDRESSED. The import at the top of `dispatcher_handlers.py` is `from .steward import ReturnReasonClassification, MAX_RETRIES as _STEWARD_MAX_RETRIES, _HARD_CAP_CYCLES`. The prompt uses `{_STEWARD_MAX_RETRIES}` and `{_HARD_CAP_CYCLES}` interpolation. The test file imports `from src.orchestration.steward import MAX_RETRIES as _STEWARD_MAX_RETRIES, _HARD_CAP_CYCLES` and aliases them to `MAX_RETRIES = _STEWARD_MAX_RETRIES` / `HARD_CAP = _HARD_CAP_CYCLES`. Test assertions `assert str(MAX_RETRIES) in result["prompt"]` and `assert str(HARD_CAP) in result["prompt"]` now verify against production constants, not locally-declared mirrors.

### Fresh pass — new issues introduced by the fixes

No new defects introduced. Specific checks:

**Comment consistency across all three surfaces:** The `wos_escalate` dispatch table comment (unchanged, line 69 context) still reads "it is exempt from the spawn-gate that applies to execution message types" — this correctly refers to `wos_escalate`, which does run before the spawn-gate. The newly added `wos_diagnose` dispatch table comment correctly distinguishes it. No confusion introduced.

**Import surface:** Adding `MAX_RETRIES` and `_HARD_CAP_CYCLES` to the steward import line is additive and non-breaking. No circular import risk: `dispatcher_handlers.py` already imports from `.steward` (the existing `ReturnReasonClassification` import was on the same line). No new module dependency added.

**parse_diagnose_command edge case introduced by first-token fix:** The `diagnose   ` (diagnose with only whitespace) case: `stripped[len("diagnose "):].strip()` gives `""`, then `remainder.split()` gives `[]`, then `if tokens` is False, so `None` is returned. Correct. The `test_returns_none_for_diagnose_without_uow_id` test covers both `"diagnose"` and `"diagnose   "`. No regression.

**Purity claim vs. os.path.dirname(__file__):** `handle_wos_diagnose` uses `os.path.abspath(os.path.join(os.path.dirname(__file__), "registry_cli.py"))` to resolve the CLI path. This is effectively constant per install (same value on every call). The purity test `test_pure_function_same_inputs_same_output` holds. This is a pre-existing pattern in `dispatcher_handlers.py` (other handlers use similar path resolution). Advisory only — not a regression from the fixes.

**wos_escalate dispatch table comment unchanged from Round 1:** The line "action='spawn_subagent' (auto-retry branches) or action='send_reply' (surface-to-Dan branches) — it is exempt from the spawn-gate that applies to execution message types." was not changed. This was an existing line describing `wos_escalate`. It is accurate: `wos_escalate` is exempt; `wos_diagnose` is not. The two adjacent dispatch table entries now have consistent and non-contradictory comments. No issue.

### Stage 1 reconfirmation (locked from Round 1)

Stage 1 finding is unchanged: alignment verdict Confirmed. The feature is anchored to `active_project.phase_intent` (WOS reliability/observability) and `principle-4` (wire what exists). The adversarial prior — that `wos_diagnose` fragments observability rather than closing a real gap — was disconfirmed in Round 1 and remains disconfirmed. No new structural framing introduced by Round 2 changes.

### Learnings.md active priors — Round 2 application

- **PR #607 comment/code mismatch pattern:** Applied. All three comment surfaces verified against code placement. No residual mismatch. Pattern confirmed resolved.
- **PR #976 phantom-values/mirror-constant pattern:** Applied. Import-and-interpolate pattern correctly implemented in both production and test code. Pattern confirmed resolved.

### Encoded Orientation check (constraint-3)

Unchanged from Round 1: the autonomous `decide-retry` capability is Constrained register (user-initiated workflow), the design spec is the authorization document, and the safety constraints are encoded in the prompt. No new Encoded Orientation surface introduced by Round 2 changes.

---

## Round 1 — 2026-04-25

### Stage 1: Vision Alignment

Formed before reading the implementation.

**Theory of change:** `wos_diagnose` closes a gap in WOS observability: when a UoW stalls, Dan currently has no lightweight way to request a forensic diagnosis without a full steward escalation cycle. Adding a `diagnose <uow_id>` Telegram command lets Dan request a structured diagnosis in one message — consistent with the vision's cognitive extension model and the `current_focus` emphasis on WOS observability and reliability.

**Vision anchor:** The work is directly anchored to `active_project.phase_intent` (WOS reliability/observability phase) and `principle-4` ("wire what exists before building more") — this PR wires existing registry data (`registry_cli trace`) into a new access path, not a new pipeline stage. Constraint-4 (minimize metabolic cost of cybernetic engagement) supports it: reducing diagnosis from multi-step manual inspection to a single Telegram message lowers friction.

**Adversarial prior applied:** The scenario in which this work is wasted effort: the root cause of stalled UoWs is already surfaced by the existing escalation pipeline (wos_escalate + needs-human-review notifications), and `wos_diagnose` adds a parallel surface path that fragments observability rather than consolidating it. I tested this: the `wos_escalate` flow is T2-A (not yet wired from steward), and this PR explicitly states it is the manual-trigger half (T1-B) — it closes a real gap for UoWs that are visibly stalled but haven't escalated yet. The adversarial scenario is not confirmed.

**Alignment verdict: Confirmed.** The feature is aligned with the active WOS reliability focus, reduces cognitive load for a real operational need, and wires existing registry signals rather than adding new pipeline stages.

**Learnings.md active priors consulted:**
- PR #607 comment/code mismatch: applied — found a mismatch between the `handle_wos_diagnose` block comment and the actual placement in `route_wos_message`. Rated NEEDS_CHANGES based on this pattern.
- PR #976 phantom-values/mirror-constant: applied — found `MAX_RETRIES` and `HARD_CAP` hardcoded in prompt string; design spec explicitly says to import from production modules. Rated NEEDS_CHANGES.
- PR #970 ruling on local message type string constants: applied — `WOS_DIAGNOSE_MESSAGE_TYPE = "wos_diagnose"` in test file is acceptable per prior ruling (no named production constant exists to import). Advisory only.

---

### Stage 2: Quality Review (Round 1)

*(Retained for audit record. See Round 2 above for current status of all gaps.)*

**Gap 1 — comment/code mismatch in handle_wos_diagnose:** NEEDS_CHANGES (resolved Round 2)

**Gap 2 — inconsistent bootup doc code examples:** NEEDS_CHANGES (resolved Round 2)

**Gap 3 — parse_diagnose_command returns full remainder:** NEEDS_CHANGES (resolved Round 2)

**Gap 4 — hardcoded MAX_RETRIES and HARD_CAP in prompt:** NEEDS_CHANGES (resolved Round 2)
