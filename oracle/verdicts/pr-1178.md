VERDICT: APPROVED
PR: 1178
Round: 2

## Round 2 — 2026-05-16

### Prior gap resolution

**Gap 1 (granola_ingest.py, morning-delivery-flush.py, transcription-monitor.py still had local _is_job_enabled):**
Status: **Addressed.**
All three scripts now import from `src.utils.jobs`. `granola_ingest.py` and `transcription-monitor.py` both add `_REPO_ROOT` to `sys.path` before the import (the required bootstrapping pattern for scripts that previously could not resolve `src.*`). `morning-delivery-flush.py` already had `_REPO_ROOT` in path and needed only the import line. Local `_is_job_enabled` definitions removed in all three. Call sites updated to `is_job_enabled(JOB_NAME)`.

**Gap 2 (test files patching module._is_job_enabled — old name):**
Status: **Addressed.**
All 5 test files updated:
- `tests/los/test_todo_obsidian_sync.py` — 5 occurrences changed from `patch(f"{_MOD}._is_job_enabled", ...)` to `patch(f"{_MOD}.is_job_enabled", ...)`
- `tests/los/test_e2e_todo_pipeline.py` — 1 occurrence changed
- `tests/unit/los/test_vault_watcher.py` — 2 occurrences changed via `patch.object(_vault_watcher, "_is_job_enabled", ...)` → `patch.object(_vault_watcher, "is_job_enabled", ...)`
- `tests/unit/test_orchestration/test_github_rate_limit_check.py` — 3 occurrences changed via `patch.object(_hb_fresh, "_is_job_enabled", ...)` → `patch.object(_hb_fresh, "is_job_enabled", ...)`
- `tests/unit/test_orchestration/test_prescriptions_per_cycle.py` — 7 occurrences changed via `patch.object(steward_heartbeat, "_is_job_enabled", ...)` → `patch.object(steward_heartbeat, "is_job_enabled", ...)`

The patch strategy used (patching at the consuming module's namespace rather than at `src.utils.jobs.is_job_enabled`) is correct Python mocking practice when the consuming module does `from src.utils.jobs import is_job_enabled`. The imported name in the consuming module's namespace is the right intercept point.

### Stage 2 quality check (Round 2 scope — fixes only)

The three newly-updated scripts follow the exact pattern established in Round 1's 14 updates: remove local `_is_job_enabled`, add `_REPO_ROOT` to `sys.path` where needed, import `is_job_enabled` from `src.utils.jobs`, update call site. No new behavioral changes introduced. The complete conversion from 14-of-17 to 17-of-17 satisfies the consolidation's stated purpose.

**Known pre-existing issue acknowledged:** `test_prescriptions_per_cycle.py` contains tests failing on `_default_db_path` in `steward-heartbeat.py` (which uses `REGISTRY_DB` now). This is pre-existing before PR #1178 — not introduced by this PR — and does not block approval.

**Encoded Orientation check:** No change from Round 1 assessment. This PR remains a pure structural refactor with no behavioral change. Constraint-3 does not apply.

---

## Round 1 — 2026-05-16

## Stage 1 — Vision Alignment (formed before reading implementation)

**Theory of change:** This PR is a consolidation/DRY refactor extracting a shared gate function into a canonical utility. The implicit theory is that canonical placement reduces duplication debt and eliminates divergence risk across 14+ scripts. This directly serves operating principle-4 ("Integration rate before new feature rate — wire what exists before building more") and principle-1 ("Proactive resilience over reactive recovery — structural prevention is preferred over better correction mechanisms").

**Alignment verdict: Confirmed.** The work is bounded infrastructure hardening. It does not foreclose any direction, introduces no new behavioral defaults, and directly applies the golden-patterns.md "canonical placement must be obvious from first encounter" principle (2026-04-25).

**What the Stage 1 adversarial prior surfaced:** The primary risk entering Stage 1 was not misalignment — it was incompleteness. A consolidation that converts 14 of 17 local definitions but leaves 3 behind achieves the opposite of its stated goal for those 3 scripts: it introduces a two-source canonical state (imported utility for 14 scripts, local definition for 3 scripts) where there was previously a consistent (if duplicated) single-source pattern per script. The golden-patterns.md "full-repo grep as precondition" pattern (2026-04-25) names this exactly: the scope rule requires checking ALL files, not a subset. I entered Stage 2 specifically looking for this.

**Learnings.md patterns checked (citation with behavioral effect):**

1. **"Full-repo grep as precondition for dead-code removal" (2026-04-25):** This pattern says any consolidation that removes local definitions requires a full-repo grep to confirm completeness. Without this pattern in my prior, I would have read the diff as correct and counted 14 updated scripts as sufficient. Because of this pattern, I ran the grep explicitly and found 3 missed scripts. The pattern directly caused me to find the primary defect.

2. **"Mirror-constant pattern is now a first-pass oracle check" (2026-04-26):** The PR is the inverse of the mirror-constant pattern — it is meant to *eliminate* mirrors. The test files mocking `_is_job_enabled` (the old local name) are now the mirrors: they patch a symbol that no longer exists in the updated modules. This caused me to check the test files immediately after finding the incomplete conversion.

3. **"Interface migration without updating all callers" (PR #720):** The pattern says when a function interface changes, enumerate ALL callers before treating the migration as complete. Here the function itself is removed from the script namespace — any test that patches it by the old name is testing against a stale target.

---

## Stage 2 — Quality Review

### What the PR does

Creates `src/utils/jobs.py` with a canonical `is_job_enabled(job_name: str, default: bool = True) -> bool`. Removes local `_is_job_enabled` definitions from 14 of the 17 scripts that had them, replacing with `from src.utils.jobs import is_job_enabled`.

### What the canonical implementation looks like

```python
def is_job_enabled(job_name: str, default: bool = True) -> bool:
    workspace = Path(os.environ.get("LOBSTER_WORKSPACE", Path.home() / "lobster-workspace"))
    jobs_file = workspace / "scheduled-jobs" / "jobs.json"
    try:
        data = json.loads(jobs_file.read_text())
        return bool(data.get("jobs", {}).get(job_name, {}).get("enabled", default))
    except Exception:
        return default
```

The implementation is clean and correct. The `default` parameter is a meaningful addition over the local definitions (most of which hardcoded `True`). The exception handling correctly returns `default` on any failure path. The function reads `LOBSTER_WORKSPACE` at call time, not at module import time — avoiding the PR #839 anti-pattern.

### Gap 1: Three scripts not updated — incomplete conversion

**Scripts missed:**
- `scheduled-tasks/granola_ingest.py`
- `scheduled-tasks/morning-delivery-flush.py`
- `scheduled-tasks/transcription-monitor.py`

The pre-PR state had 17 scripts with local `_is_job_enabled` definitions (verified via `git grep`). The PR updated 14. Three remain with local definitions. This is not a minor omission — a consolidation that leaves 3 of 17 copies in place creates a two-canonical-home state that directly violates the golden-patterns.md "canonical placement must be obvious from first encounter" principle. A new contributor reading `granola_ingest.py` will see a local `_is_job_enabled` and have no signal that the canonical version lives in `src/utils/jobs.py`. The PR description's claim "Updated all 14 scheduled-task scripts" is accurate only if the count was 14 — but the actual count is 17.

**Revision contract:** Update the 3 missed scripts to `from src.utils.jobs import is_job_enabled` and remove their local definitions, matching the pattern applied in the 14 updated scripts.

### Gap 2: Test files mock the removed local function by old name

The following test files still mock `_is_job_enabled` (the old local function name) after the PR removes or renames the function in the production scripts:

- `tests/los/test_todo_obsidian_sync.py` — multiple `patch(f"{_MOD}._is_job_enabled", ...)` calls targeting `todo_obsidian_sync._is_job_enabled`, which no longer exists
- `tests/los/test_e2e_todo_pipeline.py` — `patch(f"{_MOD}._is_job_enabled", return_value=True)`
- `tests/unit/los/test_vault_watcher.py` — `patch.object(_vault_watcher, "_is_job_enabled", return_value=True)`
- `tests/unit/test_orchestration/test_prescriptions_per_cycle.py` — `patch.object(steward_heartbeat, "_is_job_enabled", return_value=True)` (multiple tests)
- `tests/unit/test_orchestration/test_github_rate_limit_check.py` — `patch.object(_hb_fresh, "_is_job_enabled", return_value=True)` (multiple tests)

After the PR, the production modules that were updated no longer have a `_is_job_enabled` attribute — they have `is_job_enabled` (imported from `src.utils.jobs`). `patch.object(module, "_is_job_enabled")` will raise `AttributeError` at test time because the attribute no longer exists. Tests that gate on the enabled-check returning True will either raise or silently fail to intercept the real function call.

The correct fix for updated scripts is to patch the canonical location: `patch("src.utils.jobs.is_job_enabled", return_value=True)`. This is the standard approach when a function is extracted to a utility module — patch the module where it lives, not the consuming module.

**Revision contract:** For each test file that mocks `_is_job_enabled` on a script that was updated in this PR, change the mock target from `module._is_job_enabled` to `src.utils.jobs.is_job_enabled`. The 3 scripts not yet updated (Gap 1) retain their local function and can continue to be patched at `module._is_job_enabled` until they are updated.

### What the PR does well

- The canonical implementation is correct and clean
- The `default` parameter is a meaningful improvement over all local definitions
- 14 of 17 conversions are correct and consistent
- The PR correctly removes `JOBS_FILE`, `_get_workspace()`, and `_jobs_file()` helper artifacts where they had no other callers
- `ralph-loop.py`'s conversion is notable: the old local definition was zero-argument (hardcoded `JOB_NAME` in closure), whereas the new call correctly passes `JOB_NAME` as an argument — matching the caller-declared identity golden pattern (2026-04-22)

### Encoded Orientation check (constraint-3)

This PR does not change behavioral defaults, system constraints, agent identity, or decision-making rules. It is a pure structural refactor with no behavioral change. Constraint-3 does not apply.

### Feature-bundling check

Changed files: `src/utils/jobs.py` (new), 14 `scheduled-tasks/*.py` files, plus `HOW-TO-MIGRATE.md`, `README.md`, and `auto-router.py` listed in the `git diff --name-only` output. I checked — those three files show no actual diff content against main. They appear to be in the git diff output due to branch lineage, not due to actual changes. The actual scope is clean: one new utility file and 14 script updates. No feature-bundling concern.

---

## Revision contract

Two gaps must be resolved before APPROVED:

**Gap 1 — Three missed scripts:** Update `granola_ingest.py`, `morning-delivery-flush.py`, and `transcription-monitor.py` to import from `src.utils.jobs` and remove their local `_is_job_enabled` definitions.

**Gap 2 — Stale test mocks:** For each test that patches `module._is_job_enabled` where the module was updated in this PR, change the mock target from `module._is_job_enabled` to `src.utils.jobs.is_job_enabled`. A gap-2 fix that patches the old name on a module that no longer has the attribute is not resolved — `AttributeError` at test time is the detection signal.

Each gap must be either (a) addressed — the revision shows the fix, or (b) disputed — the author states why the gap does not apply with specific reason. Generic improvement without tracing to a named gap does not count as resolution.
