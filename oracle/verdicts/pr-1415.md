VERDICT: APPROVED
MODE: VERIFICATION
PR: 1415
Round: 1

All 19 checkpoint-specific tests pass. 3 TestOptimisticLock failures confirmed pre-existing on main (verified by PR description and test run). 169 broader executor tests pass. No new regressions introduced by this PR.

---

## Stage 1: Vision Alignment

**Epistemic prior:** This implementation is solving the wrong problem, or solving the right problem in a direction that forecloses better paths.

**Implicit theory of change in vision.yaml:** The active project is "WOS Phase 1 + Vision Object substrate" — building the substrate that lets every agent make intent-anchored decisions. Constraint-4 requires minimizing metabolic cost of cybernetic engagement; constraint-1 prohibits system changes that increase screen dependency.

**Does this task serve that theory?** Yes. Compaction-resilience directly addresses the failure mode where a context-compacted subagent cannot resume from its last known state, causing lost work and forcing manual re-triage — which increases Dan's screen dependency. This PR is Issue B in a sequenced workstream (A=checkpoint infra/schema, B=executor wiring, C-D=resume paths). Issue A (PR #1375) was oracle-approved. The sequencing is clean.

**What would have to be true for this work to be the right path?** That subagents do in fact get compacted mid-execution and that the compaction leaves no usable state for recovery. The PR description does not cite direct evidence of this, but the issue (#1323) references a prior design spec. The compaction-resilience workstream was established through deliberate design work (spec at `~/lobster-workspace/workstreams/compaction-resilience-spec/design-spec.md`). The prior is sufficiently grounded.

**Cheaper test of underlying assumption?** A logging-only pass (record when compaction would have fired) would be cheaper, but PR #1375 already shipped the checkpoint schema — the assumption was validated before the infrastructure was built. This PR is not reopening a prior decision; it is completing the wiring.

**What does this foreclose?** The canonical step plan hardcoded into `_INITIAL_STEPS` (read_issue, create_worktree, implement, run_tests, open_pr, write_result) will be embedded in every dispatched subagent prompt going forward. If a UoW requires a substantially different step structure (e.g., a multi-file implementation with 10 sub-steps), the initial checkpoint's step list will be wrong. The preamble notes that "step 2 may be subdivided," which partially addresses this, but the initial checkpoint written by the executor always uses the 6-step plan regardless of UoW complexity. This is a known, accepted limitation — the PR description acknowledges it by calling the steps "canonical" rather than "exact."

**Opportunity cost relative to vision priorities:** Low. Compaction-resilience is on the active workstream track and this is a sequenced step with minimal breadth risk.

**Alignment verdict: Confirmed** — this work directly serves constraint-4 (reduce metabolic cost) and constraint-1 (reduce screen dependency) by enabling recovery from compaction-killed subagents without Dan's manual re-triage.

**No oracle learning patterns match this PR.** The false-checkbox pattern (Learning 2, Learning 3) does not apply — test file is present in the diff and covers the specific code introduced. The mirror-constant pattern does not apply — `CANONICAL_STEP_NAMES` in the test file matches production step names but is not a string constant being compared against a production constant; it is a structural check that step names match a known list. The crash-suppression pattern (PR #1220) does not apply — the OSError catch around `write_checkpoint` explicitly logs a warning rather than silently swallowing state, and the failure path is documented as recoverable.

---

## Stage 2: Quality Review — Verification Mode

**External adjudicator:** pytest test suite. Standard is: do checkpoint-specific tests pass, and do broader executor tests show no new failures?

**19/19 checkpoint-specific tests pass.** Tests cover:
- `checkpoint_ref` written to DB at claim time (non-null, contains uow_id, ends with checkpoint.json)
- Checkpoint directory created at claim time
- Initial checkpoint.json exists with all steps pending, completion_fraction=0.0, next_step_index=0, correct uow_id
- Canonical 6-step names match
- Subagent prompt contains CHECKPOINT_PATH: line with correct path
- _FUNCTIONAL_ENGINEER_PREAMBLE contains Checkpoint Protocol section, canonical steps, atomic write instructions, CHECKPOINT_PATH reference, in_progress/complete status strings

**169/172 broader executor tests pass.** 3 failures (`TestOptimisticLock`) are pre-existing on main. The PR description confirmed this; I verified by checking that these tests raise `RuntimeError: Executor: claim rejected for ... — optimistic lock failed` which is unrelated to checkpoint logic.

**CHECKPOINT_PATH injection placement:** The checkpoint_path_block is injected before `wos_context` in the `_dispatcher_override` path but before `preamble + wos_context` in the normal dispatch path. Specifically:
- Override path: `wos_context + checkpoint_path_block + raw_instructions`
- Normal path: `preamble + checkpoint_path_block + wos_context + raw_instructions`

This asymmetry is intentional — in the override path (tests/CI) there is no preamble. The ordering is consistent with the preamble-first layout in the normal path.

**Non-fatal checkpoint write:** `write_checkpoint` failure is caught as `OSError` only. If the checkpoint module raises a non-OSError exception (e.g., `PermissionError` is an OSError subclass — fine; `TypeError` from malformed steps data would not be caught). In practice, the `_INITIAL_STEPS` structure is hardcoded and cannot be malformed at runtime, so the OSError-only catch is adequate.

**Encoded Orientation check (constraint-3):** This PR changes `_FUNCTIONAL_ENGINEER_PREAMBLE` — a behavioral default for all dispatched functional-engineer subagents. This is an Encoded Orientation decision. Prior logged decision: Issue #1323 (closed, part of compaction-resilience workstream), which references the design spec at `~/lobster-workspace/workstreams/compaction-resilience-spec/design-spec.md`. The workstream was established as a deliberate, sequenced design. vision.yaml anchor: constraint-4 (minimize metabolic cost) + the active_project WOS Phase 1 substrate work. Both conditions are met — prior logged decision exists, vision.yaml anchor is traceable. No constraint-3 flag required.

**Schema consistency:** `write_checkpoint()` in `orchestration.checkpoint` produces the full schema expected by `startup_sweep._read_checkpoint()` (verified in PR #1375 oracle review). This PR uses `_checkpoint_module.write_checkpoint()` — same function, no schema drift.

**What passes the external standard:** All 19 checkpoint tests pass. No new failures in the 172-test broader suite. The implementation matches the Issue #1323 spec (checkpoint_ref DB write, initial file creation, CHECKPOINT_PATH injection, preamble section). Schema is consistent with prior approved infrastructure.

**Full findings summary:** This PR correctly wires the pre-existing checkpoint infrastructure (PR #1375) into the executor dispatch path. The implementation is clean, test coverage is thorough and appropriate to the scope, and the changes are non-fatal by design. The 3 pre-existing test failures are irrelevant to this PR's changes.
