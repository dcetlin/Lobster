---
oracle_status: approved
oracle_pr: https://github.com/dcetlin/Lobster/pull/884
oracle_date: 2026-04-23
---

# Oracle Verdict: PR #884

**PR title:** fix(hooks): relocate smell-patterns.yaml from data/ to oracle/
**Date:** 2026-04-23 (re-review after fix)
**Closes:** Issue #879
**Verdict:** APPROVED

---

## Prior gap tracking

**Gap 1 from PR #884 NEEDS_CHANGES: smell-patterns.yaml Encoded Orientation without prior design decision**

Status: **addressed** — via revision path (b). Automation-implying fields (`detection_heuristic`, `threshold`, `issue_template`, `github_label`) removed from all five smell pattern entries in commit `a1706545`. The file is now a documentation artifact (named smells with descriptions, severity, recurrence tracking, and issue refs). No fields imply or require automated action. The constraint-3 gap is closed.

---

## Stage 1: Vision Alignment

The adversarial prior: this implementation is solving the wrong problem, or solving the right problem in a direction that forecloses better paths.

The PR's stated motivation (pre-commit hook blocked `data/` files) is factually wrong — the hook enforces execute bits only on `scripts/*.sh`, `scripts/*.exp`, and `hooks/*`. Data files are not in scope. However, this misdiagnosis does not undermine the action: `oracle/` is the semantically correct home for this file alongside `golden-patterns.md`, `learnings.md`, and `patterns.md`. The relocation is correctly motivated by the semantic argument regardless of the hook claim.

What this forecloses: nothing material. The relocation is reversible and low-stakes. The semantic argument is sound.

Vision.yaml current_focus names WOS execution health as primary. This is housekeeping that closes a debt from the PR #882 NEEDS_CHANGES cycle without touching execution-pipeline components. Correctly prioritized as low-friction cleanup.

**Alignment verdict: Confirmed** — correct action; the stated motivation (hook) is wrong but the actual motivation (semantic home) is correct.

**Patterns active prior check:**
- learnings.md PR #884 entry "resolution-by-absence is not resolution-by-fix" applied: PR #883's resolution by removing the file was flagged as deferral, not a fix. This review correctly treated the gap as open when the file reappeared.
- learnings.md PR #884 entry "misdiagnosed root cause does not invalidate a correct action" applied: confirmed the relocation without requiring the hook claim to be true. The misdiagnosis was noted but did not cause the oracle to reject a correct action on wrong grounds.

---

## Stage 2: Quality Review

### Prior gap resolution

Gap 1 (Encoded Orientation without prior): **addressed**. The stripped file removes all four automation-implying field types. The descriptive fields retained (`id`, `name`, `description`, `severity`, `recurrence_count`, `first_detected`, `last_detected`, `issue_ref`, `status`) have no implied automated action. Header comment documents the deferral explicitly.

### Quality findings

- The relocation from `data/` to `oracle/` is structurally correct and complete. All three path references in `docs/assessments/2026-04-23-system-retrospective.md` are updated. No residual `data/smell-patterns.yaml` references exist in the diff.
- Feature-bundling check: two files changed, both described in the PR body. Clean.
- The retrospective document still contains narrative prose describing the proposed automated retrospective system (detection heuristics, thresholds, issue filing). This is a future-design description, not operative configuration. The Encoded Orientation check does not fire on prose descriptions of proposed systems — it fires on data files with operative fields. No issue.
- The `severity` field retained in stripped file is purely descriptive. Not automation-triggering.
- The deferred automation fields (`detection_heuristic`, `threshold`, `issue_template`, `github_label`) can be re-added in a future PR when the automated retrospective system is oracle-reviewed and a design decision is logged.

### Encoded Orientation check (constraint-3)

**Pass.** No Encoded Orientation fields remain in `oracle/smell-patterns.yaml`. The file is a documentation registry. The automated system it will eventually serve requires a separate design decision and PR.

### What this makes easier

The `oracle/` path is discoverable alongside other oracle artifacts. Future work adding the automated retrospective can find and extend this file without a path change. The deferred field set is clearly documented in the file header.

### What this makes harder

Nothing. The stripped fields can be re-added atomically in a future PR when the design decision is ready.

---

## VERDICT: APPROVED

The single gap from the prior NEEDS_CHANGES verdict has been addressed. The relocation is correct. The stripped file is a documentation artifact with no Encoded Orientation behavior. Authorized to merge.
