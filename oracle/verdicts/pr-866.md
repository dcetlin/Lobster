---
oracle_status: approved
oracle_pr: https://github.com/dcetlin/Lobster/pull/866
oracle_date: 2026-04-23
---

APPROVED
PR #866 — feat: WOS pipeline fixes — dynamic burst batch, sweep schedule, result enrichment, legacy DB
Reviewed: 2026-04-23

## Summary

Stage 1 alignment finding: the adversarial prior (is this solving the wrong problem?) found a real attack surface — dynamic BURST_BATCH_SIZE is a throughput fix, and the vision.yaml current_constraint names "44% failure rate" as primary. The resolution: PRs #862/#863 address the failure rate; this PR addresses the dispatch-level starvation that prevented any prescriptions from firing regardless of failure rate. These are distinct failure modes. The sweep schedule fix (15m → 2h) is the most clearly aligned item.

Stage 2 key issues (all advisory, none blocking):
- Bare `python3 -c "..."` in upgrade.sh Migration 86 — third recurrence of named learnings.md pattern. Safe fallback (migration skips, not corrupts), but the pattern must stop accumulating.
- Duplicate `import json; import tempfile` inside `_enrich_result_file` — dead import redundancy, move to module top-level.
- Path-derivation logic for result file is inlined rather than imported from `result_writer` — synchronization debt, gracefully handled by file-existence check.
- Fix 3 (result enrichment) not anchored to current_focus in vision.yaml — incremental scope, bounded and non-fatal.

No Encoded Orientation concerns. No structural correctness failures. Tests passed (115 passed per PR test plan).
