VERDICT: NEEDS_CHANGES
MODE: VERIFICATION
PR: 1473
Round: 2

- `scripts/health-check-v3.sh` lines ~1199-1224 (ordering-guard comment, in `check_dispatcher_heartbeat()`): the comment claims the guard is "provably cadence-independent" and that "the graceful SESSION_AGE_LIMIT_SECONDS restart is guaranteed to fire within WFM_SUPPRESSION_MARGIN_SECONDS more seconds regardless of cron cadence (it does not depend on the heartbeat at all, only on the dispatcher PID being alive)." Independently re-deriving this: `check_session_age()` (lines 1792-1858) only verifies `kill -0 "$dispatcher_pid"` succeeds (PID exists) before sending `kill -TERM`, then **unconditionally** deletes `DISPATCHER_SESSION_START_FILE` and returns 1 (script exits, line 2260-2263) — it never confirms the SIGTERM actually produced a completed restart. For a dispatcher that is genuinely frozen (not just idle) since near its own session start — the same "real, not rare, pattern" the PR body itself names for the benign case — `session_age` and `heartbeat_stale_age` track 1:1, exactly as in the idle case, so the ordering guard suppresses RED throughout `[WFM_SUPPRESSION_MAX_SECONDS, SESSION_AGE_LIMIT_SECONDS)` for this malignant case too (it cannot distinguish "idle since start" from "frozen since start" using only these two derived timestamps — that ambiguity is inherent and not a defect by itself). The claimed guarantee ("only depends on the dispatcher PID being alive") is not what actually closes this case: a live-but-truly-frozen dispatcher (e.g. an asyncio-loop deadlock of the kind that caused "the May 2026 2-day outage" the file's own top-of-file comment references) may never actually process a SIGTERM-triggered graceful shutdown despite the signal being deliverable. What actually bounds the risk is a *different*, unstated mechanism: `check_session_age()` deletes the start-file unconditionally the moment it delivers the signal, regardless of whether the restart completes — so on the *next* cron tick, `DISPATCHER_SESSION_START_FILE` is absent, the ordering guard's own absent-file branch does NOT suppress, and RED correctly fires. I confirmed this fallback exists and is real (traced through the code), so the design is not unsafe — worst-case added detection latency for a frozen-since-start dispatcher is bounded to roughly one cron cycle past `SESSION_AGE_LIMIT_SECONDS` (a small, bounded increase, not the unbounded-suppression failure mode the file's own top comment warns against). But the code's stated justification attributes safety to the wrong mechanism (SIGTERM delivery succeeding) rather than the real one (unconditional file deletion regardless of restart outcome), which is exactly the species of overclaim Round 1 flagged (a code comment asserting a stronger/differently-grounded invariant than what is actually proven). This matters operationally, not just rhetorically: a future engineer who reads "the restart is guaranteed because the PID is alive" and later "fixes" `check_session_age()` to delete the start-file only *after confirming* the restart succeeded (a very plausible-looking correctness improvement, since today's unconditional delete looks unmotivated on its own) would silently reintroduce unbounded suppression for the frozen-since-start case — the exact failure class this whole cap exists to prevent.
- Fix: rewrite the ordering-guard comment (and, if the same framing was echoed in the PR description, that too) to name the actual load-bearing invariant explicitly: safety here does not come from "PID alive implies restart succeeds" — it comes from `check_session_age()` unconditionally deleting `DISPATCHER_SESSION_START_FILE` the moment it delivers a SIGTERM, regardless of whether that SIGTERM produces a completed restart, which re-enables the ordering guard's own absent-file branch (no suppression → RED) on the very next health-check cycle even if the restart silently failed. State the bound precisely: worst case, detection for a dispatcher frozen since near its own session start is delayed to approximately `SESSION_AGE_LIMIT_SECONDS` + one cron interval, not indefinitely. Add a regression test exercising this specific two-tick handoff (not just the single-tick point-in-time checks in tests 17-20): (1) session-start file present, `session_age` at/just past `WFM_SUPPRESSION_MAX_SECONDS` → GREEN (already covered); (2) simulate `check_session_age()`'s side effect by deleting the session-start file (representing "SIGTERM delivered, restart outcome unknown/failed") while heartbeat stays stale at the same age → assert RED on the next `check_dispatcher_heartbeat()` call. This is the mechanism that actually proves the residual risk is bounded, and it is currently asserted only in prose, not enforced by any test — the same gap-shape Round 1 named ("a threshold's real-world semantics are only as reliable as ... a comment asserting the relationship holds is not the same as an enforced invariant," oracle/learnings.md 2026-04-08 PR #700, generalized).

Everything else in Round 2 is independently verified and correctly resolved — see Stage 2 detail below. This is a narrow, single, well-scoped ask (fix the comment's causal claim + one additional test); it does not require redesigning the ordering guard itself, which is behaviorally sound.

---

## Round 2 — Stage 1: Vision alignment (written before re-reading the diff)

Same anchors as Round 1: constraint-4 (minimize metabolic cost — the restart storm is a direct attention tax), principle-1 (structural prevention over reactive correction), and current_focus's explicit naming of "sweep false-positive resolution" as active priority work while WOS execution is paused. Round 1 already established Stage 1 verdict as **Confirmed** — this is the right problem, correctly scoped, not invented work. Round 2 does not reopen that question; the task at hand is narrower: did the fix agent's response to Round 1's specific finding actually close the gap, or relabel it again? I entered Round 2 with the explicit instruction to "independently re-derive" the ordering-guard math rather than accept the fix agent's claim, which is the same epistemic posture Round 1 required for the original margin-only fix. The prior (from Round 1, still active): a correctable-but-real pattern exists in this codebase of code comments/test docstrings asserting a stronger invariant than the code actually proves, specifically around this exact mechanism (WFM-suppression cap timing). Holding that prior, I did not accept "provably cadence-independent" and "guaranteed ... regardless of cron cadence" at face value — I traced `check_session_age()`'s actual precondition and side effects line-by-line rather than trusting the PR's restatement of its own reasoning. That produced the finding above: the same overclaim shape recurring in new code written specifically to fix the old overclaim.

Golden pattern and learning applied (with concrete effect on this review): the golden pattern "named constants dict + fallback constant for per-type dispatch configuration" and its sibling on derived-vs-magic-literal thresholds (cited in Round 1) primed me to check whether `WFM_SUPPRESSION_MARGIN_SECONDS`/`WFM_SUPPRESSION_MAX_SECONDS` are genuinely single-sourced now (they are — confirmed via `grep -nE` against the production file, same lines the test now extracts) rather than re-diverging. Learning `oracle/learnings.md` 2026-04-08 PR #700 ("a threshold's real-world semantics are only as reliable as the monitoring cadence, and a comment asserting the relationship holds is not the same as an enforced invariant") is the one that most directly shaped Stage 2 here: it is what made me look for whether the *specific new claim* in the ordering-guard comment ("guaranteed... only depends on PID being alive") was backed by a test or only by prose — it is only prose, and once I traced the actual mechanism I found the prose names the wrong invariant entirely (not just an untested one). Without holding this learning as an active prior I would very likely have read "guaranteed... regardless of cron cadence" as adequately justified by the code directly above it (the `if [[ -n "$ordering_guard_session_age" ]] ...` block) and moved on, since the block's *behavior* is in fact correct — the gap is specifically in the stated *justification* for that behavior, which requires tracing a different function (`check_session_age()`, ~600 lines away) to discover is inaccurate.

## Round 2 — Stage 2: Quality review (verification mode)

**Item 1 — session_store.py (#1472) untouched:** Confirmed via `git diff` between the first commit (b2a0c9e, Round-1-approved) and the current head (544ef4f) restricted to `src/agents/session_store.py`: zero diff. Byte-identical. This half remains APPROVED-quality per Round 1's independent read.

**Item 2 — ordering-guard math:** See the NEEDS_CHANGES finding above. To summarize the verification performed: I read `check_dispatcher_heartbeat()`'s new guard block (lines ~1196-1230) and `check_session_age()` in full (lines 1792-1858), and `main()`'s call-and-exit sequencing (lines 2254-2264) to independently confirm the "runs earlier in main() and exits the script if it fires" claim — confirmed true (return 1 → `exit 0` at line 2262, before `check_dispatcher_heartbeat()` is ever reached on that invocation). The ordering guard's *behavior* is correct and closes the specific race Round 1 identified for the benign idle-since-start case, and — via the file-deletion side effect I traced independently — also bounds (does not eliminate, but bounds to ~1 cron cycle) the detection delay for a session frozen since near its own start. The gap is that the code's own stated reasoning for *why* this is safe cites the wrong mechanism, which is a real, forward-risk-bearing documentation defect, not merely a style nit (see "what a future refactor could break" argument above).

**Item 2b — genuinely-frozen (absent-file) case preserved:** Confirmed both by code trace and by test execution. `run_check_with_wfm_and_session $(( WFM_SUPPRESSION_MAX_SECONDS + 900 )) ""` (test 20) asserts RED (exit 2) when `DISPATCHER_SESSION_START_FILE` is absent — this passed when I ran the suite directly (see below). The guard's `[[ -n "$ordering_guard_session_age" ]]` check correctly requires a valid, parseable timestamp to suppress; an absent or malformed file leaves `ordering_guard_session_age=""`, which fails the `-n` test and falls through to RED. Verified this is not merely asserted but actually exercised.

**Item 3 — test-mirror-constant fix:** Confirmed genuinely resolved, not relabeled. `tests/test-health-check-dispatcher-heartbeat.sh` now runs `unset LOBSTER_SESSION_AGE_LIMIT_SECONDS LOBSTER_WFM_SUPPRESSION_MARGIN_SECONDS LOBSTER_WFM_SUPPRESSION_MAX_SECONDS` (clearing ambient overrides for hermeticity) followed by `eval "$(grep -E '^(SESSION_AGE_LIMIT_SECONDS|WFM_SUPPRESSION_MARGIN_SECONDS|WFM_SUPPRESSION_MAX_SECONDS)=' "$HEALTH_SCRIPT")"`. I independently confirmed via `grep -nE` against the actual production script that these three assignments appear in file order `SESSION_AGE_LIMIT_SECONDS` (line 137) → `WFM_SUPPRESSION_MARGIN_SECONDS` (line 188) → `WFM_SUPPRESSION_MAX_SECONDS` (line 189, which references both prior variables in its own default expression) — `grep -E` preserves file order, so `eval` executes them in the correct dependency order. This is a genuine fix of the "locally-declared mirror of a production constant" antipattern named in `oracle/learnings.md` 2026-04-20 (PR #800) and reflagged in Round 1 — the test now has zero hand-copied literals for these three thresholds.

**Item 4 — new tests (16-20) actually encode the invariant and pass:** I ran the test suite directly (not trusting the PR description's claim) after restoring an unmodified working tree state: `bash tests/test-health-check-dispatcher-heartbeat.sh` → **17/20 passed, 3 failed** (tests 12-14: WFM-active-stale/absent/tombstone). All of tests 9-11, 15-20 (the tests directly relevant to this PR's changes, including the new regression test 16 and ordering-guard tests 17-20) pass. Test 19 ("ENFORCED INVARIANT") samples 4 points across the gap window (`offset` 0, 1, `gap_width/2`, `gap_width-1` → probes 6900, 6901, 7050, 7199 for the default 300s margin / 7200s session limit) and requires GREEN at every one — this is a genuine enforced-invariant test, not a prose claim, and it passed.

**Pre-existing failures 12-14 — confirmed not worsened by this PR:** I checked out `origin/main`'s versions of both `scripts/health-check-v3.sh` and `tests/test-health-check-dispatcher-heartbeat.sh` in isolation (verifying first that my local `main` ref was stale — `git merge-base origin/main origin/fix/2074-...` resolves to `origin/main`'s own tip, confirming this PR branch is fully up to date with main) and ran the baseline suite: **12/15 passed, 3 failed**, with the identical three failing test names ("WFM-active stale/absent/tombstone → RED: expected exit 2, got 0"). This PR does not introduce, worsen, or mask these three pre-existing failures — confirmed identical failure signature on baseline vs. PR branch. Per the task's explicit instruction, these are not held against this PR.

**Encoded Orientation check (constraint-3):** unchanged from Round 1 — this PR recalibrates an operational health-check threshold and closes a DB-query consistency gap. Not an Encoded Orientation decision.

**What this forecloses / propagates:** if merged with the current comment language unchanged, the next engineer to touch `check_session_age()` has a plausible, comment-endorsed reason to "clean up" the unconditional file-deletion-before-restart-confirmation as if it were an oversight — silently reintroducing unbounded suppression for the frozen-since-start case. This is the single reason this finding is gating rather than advisory: it is not a nitpick about phrasing, it is a comment that currently points a future maintainer at the wrong safety mechanism, in code whose entire purpose is preventing a repeat of a 2-day production outage.

---

## Round 1 — [2026-07-08]

VERDICT: NEEDS_CHANGES
MODE: VERIFICATION
PR: 1473
Round: 1

- `scripts/health-check-v3.sh` line ~161: `WFM_SUPPRESSION_MARGIN_SECONDS="${LOBSTER_WFM_SUPPRESSION_MARGIN_SECONDS:-300}"` — the 300s default margin is NOT reliably larger than the health-check cron cadence (`*/4 * * * *` = 240s, per `install.sh` and live crontab) by any real safety factor (300/240 = 1.25x). Because `main()` calls `check_session_age()` (fires graceful SIGTERM at `session_age >= SESSION_AGE_LIMIT_SECONDS = 7200s`) strictly before `check_dispatcher_heartbeat()` (fires RED at `heartbeat_stale_age >= WFM_SUPPRESSION_MAX_SECONDS = 6900s`) in the same script run, and a session that goes idle at/near its own start has `heartbeat_stale_age ≈ session_age`, the 300-second window `[6900s, 7200s)` during which RED-but-not-yet-graceful-restart is possible is *larger* than one 240s cron tick. By pigeonhole, that window is guaranteed to contain at least one cron check for any phase alignment — meaning the "idle since near session start" case (a real, not rare, pattern — e.g. dispatcher restarts into an empty inbox overnight) will still trip the forced-restart/alert path (`do_restart` + Telegram alert, confirmed at health-check-v3.sh lines ~2537-2567) *before* the graceful SIGTERM path ever gets a chance to run. This directly falsifies the code comment's claim ("a legitimately idle session... is now always reaped by the graceful SESSION_AGE_LIMIT restart before this cap can fire") and the regression test's docstring claim ("proving the false-positive path is actually closed, not just relabeled") — it is not closed, it is delayed from firing every ~45min to firing once per idle-from-near-start session at ~115min. That is a large, genuine improvement in frequency, but it is not the invariant the PR asserts, and the PR's own "Root-cause answer" section already concedes a residual risk exists — the code comment and test docstring should not claim more certainty than that section does.
- Fix: either increase `WFM_SUPPRESSION_MARGIN_SECONDS` to a value with a real safety factor over the cron cadence (e.g. 900-1200s, several cron ticks of buffer, chosen and documented explicitly against the 240s cadence — not just "less than SESSION_AGE_LIMIT_SECONDS"), or add an explicit ordering/suppression rule (e.g. skip the WFM-cap RED check entirely when `session_age` is already within N seconds of `SESSION_AGE_LIMIT_SECONDS`, since the graceful restart will fire imminently regardless), and add a unit test that asserts the margin-vs-cadence relationship as an enforced invariant rather than a prose assumption.
- `tests/test-health-check-dispatcher-heartbeat.sh` lines ~86-91: two NEW hardcoded mirror constants (`SESSION_AGE_LIMIT_SECONDS=7200`, `WFM_SUPPRESSION_MARGIN_SECONDS=300`) are declared independently of the production script's env-overridable computation in `scripts/health-check-v3.sh`, rather than sourced/derived from it. This is the "locally-declared mirror of a production constant" antipattern named in `oracle/learnings.md` (2026-04-20, PR #800): if the production defaults ever change, these test mirrors silently go stale and the test keeps passing against the wrong relationship. The PR's own stated goal was to replace "an unexplained magic literal" with a derivation "tied structurally to actual session lifetime" — the test file recreates exactly the kind of untied magic-literal duplication the PR's own framing argues against, and doubles the existing mirror surface (the file already mirrors `WFM_SUPPRESSION_MAX_SECONDS`; this PR adds two more mirrored inputs instead of reducing the mirror to one).

Confirmed via independent read of the diff and the surrounding files (not just the PR description): `session_store.py`'s two new guard sites are correct, minimal, and consistent — see Stage 2 detail below for what was checked and why that half passes cleanly.

---

## Stage 1 — Vision alignment (written before reading the diff/code)

Vision anchors: `core.inviolable_constraints.constraint-4` ("Minimize metabolic cost of cybernetic engagement... not maximize output volume or feature density") and `core.operating_principles.principle-1` ("Proactive resilience over reactive recovery: structural prevention is preferred over better correction mechanisms"). `current_focus.this_week.secondary` explicitly names "sweep false-positive resolution" as active work, and `current_focus.this_week.primary` names WOS-adjacent hygiene as the priority while execution is paused.

A restart storm producing 16 forced restarts/day (3x the by-design rotation rate) plus false "task(s) disappeared" alarms is a direct, measurable tax on Dan's attention every time it fires — exactly the "metabolic cost" constraint-4 exists to bound, and exactly the kind of noise principle-1 says should be structurally prevented rather than reactively soothed. This is squarely the right *class* of problem to fix, and the issue traces to a concrete, logged prior (#1472, #2074) rather than being invented scope. I entered Stage 2 expecting to test one specific thing: whether "structural prevention" is what's actually being delivered, or whether the fix is itself a reactive threshold tweak wearing structural-prevention language — because principle-1 explicitly prefers the former, and a threshold recalibration is inherently closer to the latter unless it can be shown to eliminate the failure class rather than relabel its boundary. The task's explicit prompt ("be skeptical that this PR actually removes the false-positive path rather than relabeling it") sharpens exactly this Stage 1 question into a falsifiable Stage 2 check.

That check is what produced the Round 1 finding above: the PR *substantially* reduces the false-positive rate (a real, valuable improvement, and honestly scoped — the PR body's "Root-cause answer" section explicitly declines to claim a full root-cause fix and names the residual risk as structural), but the code comment and regression-test docstring overclaim relative to what the math actually delivers. Vision constraint-2 ("every agent decision... traceable to a specific field... not inferred from conversational texture") extends naturally here: a fix whose in-repo documentation asserts an invariant stronger than what was verified is a fluency/structural-anchor gap of the same shape constraint-2 warns about, even though the PR body itself is more careful. This is a correctable documentation/calibration gap, not evidence the PR is solving the wrong problem — Stage 1 verdict is **Confirmed** (right problem, right class of fix, correctly scoped against `current_focus`), with the Stage 2 finding being about calibration and claim-accuracy, not direction.

Golden pattern applied: "named constants dict + fallback constant for per-type dispatch configuration" (2026-06-03, `golden-patterns.md`) and its predecessor pattern on derived vs. magic-literal thresholds shaped what I looked for in Stage 2 — specifically, whether the new derived constant is actually importable/single-source-of-truth or merely *presented* as such while a shadow copy exists elsewhere. That is precisely what the test-file mirror finding above surfaces: the production side follows the golden pattern (named, env-overridable, derived from two other named constants), but the test side reintroduces the antipattern the golden pattern exists to prevent. Without holding that pattern as an active checklist item, I would likely have read the test file's constants as "test setup, not a design concern" and missed the divergence-risk framing.

Learning applied: `oracle/learnings.md` (2026-04-08, PR #700) — "count-based cadence-dependent starvation threshold vs. time-based starvation threshold" / "'N consecutive readings at 30-min cadence = X hours' is a documentation claim, not an enforced invariant." This PR's mechanism is timestamp-based, not count-based, so the specific count-vs-time distinction doesn't transfer directly — but the generalized form of that learning ("a threshold's real-world semantics are only as reliable as the monitoring cadence, and a comment asserting the relationship holds is not the same as an enforced invariant") transfers exactly. Holding this learning as a prior made me check, specifically, whether the margin-vs-cron-cadence relationship was asserted anywhere as code (a test, a runtime assertion) versus only as prose in a comment — it is only prose, which is what produced the Round 1 finding rather than my accepting the code comment's claim at face value.

## Stage 2 — Quality review

**session_store.py (issue #1472 half) — clean, APPROVED-quality on its own:**
- The added predicate `AND (agent_type IS NULL OR agent_type NOT IN ('dispatcher', 'hook'))` in `cleanup_stale_running_sessions()` and `get_unnotified_completed()` is verified to be byte-identical to the existing predicate already present in `get_active_sessions()` (added in PR #781/#782) — this is reuse of an established, already-reviewed literal, not new invented scope, and avoids creating a fourth divergent copy.
- On the specific review target ("do hook sessions need dead-reaping via these paths?"): independent repo-wide grep (`src/`, `hooks/`, `scripts/`, `scheduled-tasks/`) found no current call site that registers `agent_type='hook'` sessions — the exclusion is currently a no-op against live data. That is not a defect: it mirrors the precedent already accepted in `get_active_sessions()`, keeps the three query sites in lockstep (preventing a future divergence if a hook-type session is ever registered), and costs nothing. Correct, minimal-risk scope discipline.
- Four new tests (`test_cleanup_stale_running_excludes_dispatcher_session`, `test_cleanup_stale_running_excludes_hook_session`, `test_cleanup_stale_running_still_reaps_real_subagent`, `test_get_unnotified_completed_excludes_dead_dispatcher_session`, `test_get_unnotified_completed_still_returns_real_subagent`) cover both new guard sites and, critically, include a same-file regression test proving the fallback heuristic is not globally disabled for real subagents — this is exactly the right test shape (positive exclusion + negative-control that the general case still works).
- No side effects: pure additive `WHERE` predicate, read-only queries, no new control flow branches, no writes.

**health-check-v3.sh (issue #2074/#1472 half) — the margin/cadence finding above is the substantive gap.** Everything else about this half checks out: `SESSION_AGE_LIMIT_SECONDS` is defined (line 134) before it's referenced in the new derivation (line ~161), so there's no bash ordering bug; both new values are env-overridable (`LOBSTER_WFM_SUPPRESSION_MARGIN_SECONDS`, `LOBSTER_WFM_SUPPRESSION_MAX_SECONDS`) consistent with the rest of the file's override convention; the new Test 16 regression test correctly locks in that the *old* fixed 2700s cap value now returns GREEN, which is a real and valuable assertion — it's just not sufficient to establish the stronger claim the docstring makes.

**On the named test gap** ("threshold change was NOT verified against a live dispatcher — is that acceptable?"): yes, that specific gap is acceptable — a live-dispatcher smoke test would need to observe ~2 hours of idle-from-start behavior to exercise the exact boundary in question, which is not a reasonable CI/manual-test bar for this change. What was actually missing is not live verification but a targeted unit assertion (margin > cron_interval × safety_factor, or an explicit ordering test simulating two consecutive cron ticks straddling the `[cap, graceful_threshold)` window) — a fast, deterministic check that would have caught the gap found in this review without needing a live dispatcher at all. Recommend adding that instead of (not in addition to the impracticality of) a live-dispatcher observation requirement.

**Encoded Orientation check (constraint-3):** this PR does not change behavioral defaults, agent identity, or decision-making rules — it recalibrates an operational health-check threshold and closes a DB-query consistency gap. Not an Encoded Orientation decision; constraint-3's logged-prior/vision-anchor requirement does not apply.

**What this forecloses / propagates:** if merged as-is, the code comment and test docstring become the de facto documentation of "this problem is solved," which will discourage anyone from re-investigating the next time a false RED fires in the wild during a long quiet stretch — exactly the failure mode the task prompt was worried about with the SiderealPress/lobster#2090 lineage ("a prior upstream cap 'fix' reintroduced this bug rather than fixing it"). Overclaiming resolution in comments/tests is itself a variant of that same lineage risk: the next engineer reads "closed, not relabeled" and "always reaped before this cap can fire" as settled fact and stops looking, even though the underlying margin-vs-cadence relationship was never actually verified as an invariant.
