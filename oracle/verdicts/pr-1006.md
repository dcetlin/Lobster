VERDICT: APPROVED

## PR #1006 — fix(audit): surface lifetime_cycles in completed_uow_durations

### Stage 1 — Right Problem?

The `lifetime_cycles` column was added to `uow_registry` in migration 0007 and is the binding lifetime cap counter (not reset on decide-retry, unlike `steward_cycles`). The `completed_uow_durations()` function was omitting it from the SELECT, so callers (metrics reports, WOS health checks) could not see the true cumulative effort. The fix is directly traceable to the schema — the column exists, it carries semantic meaning distinct from `steward_cycles`, and not returning it was a gap.

The PR also includes a docs/ directory restructuring (reference/, design/, wos/current/, etc.) and reference updates in cultivator.py, executor.py, germinator.py. This is in-scope housekeeping: the doc moves are renames only (similarity 100%), and the source file reference updates are correct updates to match the new paths.

### Stage 2 — Well Made?

Code changes:
- `audit_queries.py`: one-line addition to SQL SELECT (`lifetime_cycles`), one-line addition to the returned dict, docstring updated. Minimal, correct.
- `_insert_done_uow` test helper: `lifetime_cycles` parameter added with default 0, inserted correctly in the INSERT. The existing 36 tests remain valid because the default is backward-compatible.
- New test `test_lifetime_cycles_field_included`: seeds `lifetime_cycles=7`, asserts `result[0]["lifetime_cycles"] == 7`. Directly verifies the fix.

Doc changes: All renames confirmed as pure renames (100% similarity) plus three internal reference string updates in source files that correctly reflect the new paths.

No concerns. Change is correct, tight, and tested.
