VERDICT: NEEDS_CHANGES
MODE: VERIFICATION
PR: 1375
Round: 1

1. **Merge conflict in registry.py** — PR branch `wos/9765e7-checkpoint-infra` is CONFLICTING with main. PR #1374 (checkpoint-aware orphan classification) merged to main after this branch was cut, modifying the same `record_startup_sweep_executing` function. GitHub reports `mergeStateStatus: DIRTY, mergeable: CONFLICTING`. Rebase onto main required. After rebase, the `record_startup_sweep_executing` changes in this PR become no-ops (already on main); the surviving diff should reduce to: UoW dataclass `checkpoint_ref` field, deserialization line, checkpoint.py, paths.py, schema.sql, migration, and test file.

2. **Schema mismatch between `write_checkpoint()` and already-merged reader** — `CheckpointData` TypedDict defines `{step_name, status, artifacts, written_at}`. The already-merged startup sweep (PR #1374, line 235) reads `checkpoint.get("next_step_index", 0)` and iterates `checkpoint_data.get("steps", [])` for artifact verification. A file written by this PR's `write_checkpoint()` would never trigger the `orphan_kill_during_execution_with_checkpoint` classification (no `next_step_index` field) and would produce zero verified/missing artifacts (no `steps` array). If `write_checkpoint()` is intended as the canonical write path (its name and module location imply this), it must produce files compatible with the reader. If subagents will write the full schema directly (bypassing this function), document that `write_checkpoint()` is a simplified helper and name the TypedDict accordingly to prevent future callers from treating it as the canonical schema.

---

## Stage 1: Vision Alignment

**Alignment verdict:** Confirmed

The checkpoint protocol serves vision.yaml `principle-1` (proactive resilience over reactive recovery) and is directly on the critical path to resuming WOS execution. Infrastructure-before-behavior sequencing matches the golden pattern "Governing-structures-first sprint sequencing" — this PR installs plumbing (Issue #1322) before the execution behavior (Issue #1323) that depends on it. The theory of change is sound: compaction-induced orphaning is an observed operational problem causing restart-from-scratch waste; structured progress records on disk are a structurally correct response.

What would have to be true: checkpoint files must actually be written by subagents at step boundaries (Issue #1323), and the Steward/reconciler must read them to resume from the last completed step (Issue #1324). This PR installs only the first layer. The nullable `checkpoint_ref` column and disk schema are low-risk and easily reversible.

Nothing is foreclosed. The design is additive. The migration is compatible with the existing pipeline.

**Learnings.md patterns checked:**
- **Migration number collision** (PRs #999/1000, #1356, #1105): Checked — 0031 is the next sequential number. Only one open PR (this one). No collision.
- **Feature-bundling** (PRs #712, #717, #882, #1148): 6 files changed, all directly related to checkpoint infrastructure. No undescribed scope expansion.
- **No-op commit bundling** (#1217): N/A — single-purpose branch.

**Golden pattern applied:** "Governing-structures-first sprint sequencing" — this constrained what I looked for in Stage 1. Rather than asking "is the checkpoint code complete enough to be useful," I asked "is the sequencing correct — does this install the governing structure before the behavior that depends on it?" It does.

## Stage 2: Quality Review

### What works well

1. **Atomic write** via `.tmp` + `os.replace()` — correct for single-writer crash safety. Matches the design spec requirement.
2. **Path centralization** — `CHECKPOINTS_DIR` added to `paths.py` using the established `LOBSTER_WORKSPACE` derivation pattern. Avoids the path-divergence bug class (learnings.md #1034).
3. **UoW dataclass field** — `checkpoint_ref: str | None = None` with default None is backward-compatible. Deserialization via `d.get("checkpoint_ref")` correctly handles pre-migration rows.
4. **Migration 0031** — clean `ALTER TABLE ADD COLUMN` with `DEFAULT NULL`. Correct for nullable TEXT.
5. **schema.sql** — both the CREATE TABLE and executor_uow_view are updated consistently.
6. **Test coverage** — 13 tests covering write, read, roundtrip, replace, null-artifacts, and missing-file cases. Monkeypatch redirect of `CHECKPOINTS_DIR` is the correct test pattern (avoids the module-level-constant-frozen-at-import anti-pattern from learnings.md #839).

### Blocking issues

See findings 1 and 2 above.

### Revision contract

- **Finding 1 (merge conflict):** Rebase `wos/9765e7-checkpoint-infra` onto main. After rebase, the `record_startup_sweep_executing` changes should disappear from the diff (already on main). Verify with `gh pr view 1375 --json mergeable` → `mergeable: MERGEABLE`.
- **Finding 2 (schema mismatch):** Either (a) update `write_checkpoint()` and `CheckpointData` to produce the full design-spec schema (with `steps` array, `next_step_index`, `completion_fraction`, etc.) compatible with the startup sweep's `_read_checkpoint()` reader, OR (b) add a docstring to `write_checkpoint()` explicitly stating it is a simplified per-step helper not intended to produce startup-sweep-compatible checkpoints, rename `CheckpointData` to something like `StepCheckpointData` to distinguish it from the full schema, and add a TODO referencing Issue #1323 for the full schema writer. Option (b) is acceptable if the subagent preamble (Issue #1323) will write the full schema directly.
