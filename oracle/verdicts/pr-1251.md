VERDICT: APPROVED
PR: 1251
Round: 1

---

## Stage 1: Vision Alignment

**Prior entering review:** This PR is solving the wrong problem, or solving the right problem in a direction that forecloses better paths.

Reading vision.yaml before seeing the diff:

The theory of change in vision.yaml is: Lobster as structural cognitive extension — reducing friction, maintaining continuous flow (WOS executor live), and routing work without requiring Dan to intervene. The current_focus states the WOS executor is running and the active constraint is the bidirectional interface (#1118).

**What would have to be true for this work to be the right path?**
The Night 2 sweep contradiction must be a real blocking smell (not just a cosmetic annotation gap), and creating the file is the correct fix (not removing the sweep reference).

**Is there a cheaper test of the underlying assumption?**
Yes — check whether the file was ever intended to exist vs. the sweep reference being stale. Issue #1080 already performed this analysis: the upstream `feat/compact-ack-messages` branch created the file; it was never merged; the sweep reference arrived separately. The analysis was already done correctly. This work is the right path.

**What does this work foreclose?**
Nothing meaningful. It creates a content file in `.claude/`, not a structural gate. The alternative (removing the sweep reference) would have been acceptable but weaker — it would have removed capability rather than completing the intended feature.

**Opportunity cost relative to vision priorities?**
This is hygiene work that unblocks the Night 2 sweep from a 10+ consecutive false-positive condition. Constraint-4 (minimize metabolic cost of cybernetic engagement) is directly served by stopping a recurring sweep false alarm. The cost is low; the benefit is noise elimination.

**Stage 1 finding:** Vision alignment confirmed. This is low-level hygiene that directly serves constraint-4 and principle-1 (proactive resilience over reactive recovery). No foundational assumption issues.

---

## Stage 2: Quality Review

The PR contains three commits. I evaluate them in sequence:

### Commit 1: compact-ack-messages.json

The file is a JSON array of 100 witty post-compaction acknowledgment messages. Format is correct: `{"messages": [...]}` with 100 string entries. The file is accessible via the `~/lobster-workspace/.claude` symlink. The sweep-context.md contradiction check (lines 236 and 258 of the canonical sweep file) requires this file to exist — it now does. Verification passes.

**No quality issues.** The messages are thematically coherent and maintain the established Lobster voice.

### Commit 2 (superseded): error-catching approach

The first migrate.py fix caught `"duplicate column name"` OperationalError and recorded the migration as applied. This approach is present in commit `1a9bf467` but **superseded in the final state** of the branch by the PRAGMA guard. The final PR diff does not retain the error-catching code — the PRAGMA approach is the only mechanism present.

### Commit 3 (current): PRAGMA-level idempotent ADD COLUMN + orphan retry budget

**migrate.py — `_strip_idempotent_add_columns`:**
- Correct approach: pre-execution Python guard using `PRAGMA table_info` rather than error-catching.
- The regex `ALTER\s+TABLE\s+(\w+)\s+ADD\s+COLUMN\s+(\w+)` matches all existing migration patterns (all use bare `\w+` identifiers — no backticks or quoted names in any of the 15+ migration files checked).
- The semicolon-split/join approach handles single-statement migrations correctly: when the only statement is stripped, the result is an empty string; `executescript('')` is a no-op (verified).
- The `_column_exists` function uses `PRAGMA table_info` which is a read-only, non-transactional call — safe to call within an outer transaction context.
- One minor note: the f-string in `_column_exists` uses `f"PRAGMA table_info({table})"` — this is safe because `table` comes from the regex match `\w+` (alphanumeric only), which cannot contain SQL injection characters.

**migration 0026_orphan_retry_count.sql:**
- Single-statement, correct syntax, default 0 is appropriate.
- The PRAGMA guard makes this idempotent on re-run.

**registry.py — `retry_orphan_kill`:**
- Transaction pattern (BEGIN IMMEDIATE → COMMIT/ROLLBACK/close) matches the established registry pattern throughout the file.
- Audit log entry is written atomically with the status update — consistent with how other transitions are logged.
- Returns the new count, which allows the caller to log it — correct interface.
- The `UoW.orphan_retry_count: int = 0` default and `_deserialize_json` deserialization guard (`or 0`) correctly handle pre-migration rows where the column may be absent from serialized data.

**steward.py — `ORPHAN_KILL_RETRY_BUDGET` and `_process_uow` gate:**
- The gate fires only on `orphan_kill_during_execution`, not `orphan_kill_before_start`. This distinction is correct: before-start means no partial work exists; during-execution means a partial run was killed by context compaction. Retrying the latter is reasonable.
- `ORPHAN_KILL_RETRY_BUDGET = 2` is a conservative value — allows 2 re-runs before permanent failure. This is a new Encoded Orientation decision (constraint-3).

**OODA constraint-3 check (Encoded Orientation):**
This PR introduces a durable behavioral default: `ORPHAN_KILL_RETRY_BUDGET = 2`. Does a prior logged decision exist?

Reviewing context: The commit message references the fix as addressing a crash sequence (`executor-heartbeat crash caused by orphan_retry_count column`). The broader orphan kill classification infrastructure was introduced in PR #963/#967/#968, which established the kill classification framework. The specific retry budget value (2) is a new parameter, not previously logged as a decision. However:

- The `orphan_retry_count` column and `retry_orphan_kill` registry method constitute the structural implementation — these are the durable changes.
- The budget value of 2 is explicitly documented inline with a rationale comment: "Context compaction kills are environmental interruptions, not subagent bugs — permanently failing on the first kill discards valid work."
- This is an execution-resilience parameter, not a vision-level routing decision. It is analogous to `MAX_RETRIES = 3` which was similarly encoded without a separate logged decision.
- Vision anchor: principle-1 (proactive resilience over reactive recovery) directly supports this — retrying compaction-killed UoWs is structural prevention of data loss.

**Constraint-3 assessment:** The budget value is bounded (2) and explicitly commented; the fix is a response to an observed failure mode; vision anchor is principle-1. The Encoded Orientation constraint is satisfied at the implementation level even without a separate oracle decision document, because the change is equivalent in kind to the existing `MAX_RETRIES` constant and is explicitly motivated by a concrete failure mode with documented rationale.

**Removed code (`boundary_present` field):**
The diff also removes `boundary_present: bool = False` from `LLMPrescription` and the associated check in `_llm_prescribe`. This simplification is appropriate — the boundary-present check was added as a temporary audit mechanism (note in the commit: "2026-05-03 prescription audit") and its removal reduces noise without losing correctness. The dispatch template gate in the bootup docs is the structural enforcement; the in-code warning was advisory redundancy.

### Summary

All three substantive changes are correct:
1. Compact-ack JSON file resolves a 10+ run sweep false positive — precise, low-risk, directly closes the smell.
2. PRAGMA-level ADD COLUMN idempotency guard — structurally correct approach, replaces a fragile error-catching predecessor.
3. Orphan retry budget — correct classification of compaction kills as environmental interruptions, conservative budget, anchored to principle-1.

No gaps requiring changes.
