VERDICT: APPROVED
PR #875 — refactor: remove decisions.md write target; archive to oracle/legacy/
Reviewed: 2026-04-23

---

## Stage 1 — Vision Alignment (locked before seeing diff)

PR #875 removes `oracle/decisions.md` as an oracle write target and archives the file. PR #861 already established `oracle/verdicts/` as canonical — this PR is completing the cleanup leg of that migration. The adversarial prior: is this the wrong problem, or does `decisions.md` still carry load anywhere? Investigation required: does any automated reader still depend on `decisions.md`? If yes, archiving it is premature and introduces a silent gap. If no, the prior does not survive.

**Alignment verdict: Confirmed** — `verdicts/` is already canonical (PR #861 merged, hook updated). Removing the dual-write instruction eliminates a confusion vector. No foreclosure of future paths.

---

## Stage 2 — Quality Review

**All decisions.md references removed from lobster-oracle.md:**
Five references updated consistently: (1) agent description line, (2) document-review invocation-mode reference ("Use the document review format in decisions.md"), (3) Encoded Orientation check path (now checks `oracle/verdicts/`), (4) oracle_status approved note (trailing "without consulting decisions.md" removed), (5) the `Append to ~/lobster/oracle/decisions.md` write block with its full template deleted. The WOS spiral gate line (After writing to `decisions.md` and `verdicts/...`) is correctly simplified to `verdicts/` only. No stray `decisions.md` references remain.

**Archive header accuracy:**
The archive header states "this file is no longer read by any automated system." For instances with PR #861 deployed but upgrade.sh not yet run, the hook's legacy fallback path still reads `oracle/decisions.md`. When the file is moved by this PR's git rename, that fallback returns `None` → warns but allows merge (safe direction — not a hard block, no regression). The claim is mildly overstated but the failure mode is safe. Not blocking.

**No upgrade.sh migration:**
No migration to move the live file on running instances. The gate reads `verdicts/` first post-#861, legacy fallback None is safe. Consistent with instruction-layer-only scope. Not blocking.

**Encoded Orientation check (constraint-3):**
PR modifies `lobster-oracle.md` (agent definition) — this is an Encoded Orientation decision. Prior logged decision: PR #861 verdict (`oracle/verdicts/archive/pr-861.md` — APPROVED). Vision anchor: vision.yaml principle-3 (determinism over judgment for conditionals). Both conditions satisfied. Check passes.

**Scope check (feature-bundling pattern):**
2 files changed: `lobster-oracle.md` and `oracle/decisions.md` (renamed to `oracle/legacy/decisions.md.archive`). Both named in PR description. No undocumented scope expansion.

---

**VERDICT: APPROVED**
