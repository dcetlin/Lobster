VERDICT: NEEDS_CHANGES
PR: 1238
Round: 1

---

## Stage 1 — Vision alignment

**Formed before reading implementation.**

The theory of change: the executor-heartbeat sidecar writes `heartbeat_at` for all in-flight UoWs every 3 minutes, regardless of whether the agent is alive. This means a dead agent looks live to the observation loop. The fix is a discriminating signal that only an alive agent can write — `token_usage IS NOT NULL` in `uow_heartbeat_log`. §2 of the multiposture spec (oracle-approved in #1237) defines this approach.

**What would have to be true for this to be right?** Agents must reliably pass `token_usage`. The discriminator works iff every alive agent writes a heartbeat with a non-NULL token_usage value. The PR makes this "REQUIRED (not optional)" in the prompt text, but the enforcement is advisory — an agent that writes a heartbeat without token_usage will be misclassified as dead regardless of its actual state. This is a known tension the spec acknowledges, and Change 1c (sidecar demotion) is the structural completion. The discriminator choice is correctly anchored to the approved spec decision.

**Does this foreclose any direction?** Change 1c (deferred) will demote the sidecar, which means the current discriminator is a transitional mechanism, not the final state. This PR's design is consistent with that trajectory.

**Alignment verdict: Confirmed.** The problem being solved is the real problem. The discriminator is the one the spec prescribes. The constraint in vision.yaml principle-3 ("if-then logic is code, not LLM instructions") applies to the advisory enforcement in the prompt — this tension is noted but is not a Stage 1 blocker given the spec's phased approach.

---

## Stage 2 — Quality review

### Gap 1: Zero test coverage for all new Change 1b code paths

The PR's 50 passing tests cover prior functionality — sidecar writes, stuck detection, heartbeat locking. None of the new functions introduced in this PR have any test:

- `Registry.get_sidecar_masked_uows()` — zero tests
- `Registry.record_sidecar_masked_stall()` — zero tests
- `detect_sidecar_masked_uows()` in `steward-heartbeat.py` — zero tests

The PR description checks "[x] All 50 heartbeat-related unit tests pass" — these are pre-existing tests. The test plan items "confirm UoW classified as sidecar_masked within 10 minutes" are manual observability checkpoints, not automated tests. The new SQL query, the atomic state transition, the audit log write, the dry-run path, and the race-condition handling are all untested in the diff. This is the highest-severity gap.

The "false completed-test checkbox" pattern from learnings.md (2026-05-19, PR #1011) applies: the PR description checked a unit-test box and cited passing tests, but no test file for the introduced functions appears in the diff.

### Gap 2: Hardcoded `'executing'` as `from_status` in audit log INSERT — incorrect for `'active'` UoWs

In `record_sidecar_masked_stall`:

```python
INSERT INTO audit_log
    (ts, uow_id, event, from_status, to_status, agent, note)
VALUES (?, ?, 'stall_detected', 'executing', 'ready-for-steward',
        'observation_loop', ?)
```

The preceding UPDATE transitions from `status IN ('active', 'executing')`. If the UoW is in `'active'` status at transition time, the audit trail records `from_status='executing'` — factually incorrect. The parallel function `record_heartbeat_stall` uses `'active'` as its hardcoded from_status (line 3125), which raises the same category of concern (neither derives from actual state), but the sidecar-masked version gets the value wrong for the `'active'` case. Several existing functions (e.g., `write_heartbeat` at line 2093, `close_uow` at line 2307) derive `from_status` from the DB row before transitioning — this is the correct pattern.

### Gap 3: SQL hardcodes `+300 seconds` grace window in NOT EXISTS subquery, mismatched with `min_age_seconds` parameter

The outer query age check uses the `min_age_seconds` parameter:

```sql
AND CAST((julianday(?) - julianday(r.updated_at)) * 86400 AS INTEGER) > ?
```

But the NOT EXISTS subquery hardcodes `+300 seconds`:

```sql
AND l.recorded_at > datetime(r.updated_at, '+300 seconds')
```

The two 300-second values are semantically intended to be the same threshold (the startup grace window from the spec). When called with a non-default `min_age_seconds` (e.g., in tests or if the threshold is tuned), the outer and inner windows become desynchronized. A UoW 400s old with a heartbeat at second 350 passes the NOT EXISTS check (heartbeat is after the hardcoded 300s mark) even when the outer check says it's past the startup window. The constant `SIDECAR_MASK_MIN_AGE_SECONDS = 300` is defined but not used in the SQL — it should be passed as a parameter to the `datetime()` modifier rather than hardcoded. This is also an instance of the learnings.md pattern "any time-window comparison in an orphan classifier must use a named constant, not an inline literal" (2026-04-26, PR #968).

### What works well

- The discriminator logic is correct: `token_usage IS NOT NULL` correctly distinguishes agent-written heartbeats from sidecar-written `heartbeat_at` updates.
- The `SidecarMaskedResult` frozen dataclass follows the classification-result-as-typed-dataclass golden pattern.
- The prompt template upgrade (Change 1a) is well-structured: startup checkpoint is first-action, 90s is named as hard-maximum, token_usage is declared required with the consequence explained.
- The optimistic-lock + audit pattern in `record_sidecar_masked_stall` correctly follows the same structure as `record_heartbeat_stall`.
- Pre-0017 deploy safety: the `get_sidecar_masked_uows` exception handler correctly returns `[]` on DB errors (e.g., table not found), preventing crashing on older deploys.
- Race handling: `rows_affected == 0` is logged at DEBUG level (not warning), correctly treating concurrent advancement as expected.
- The `detect_sidecar_masked_uows` dual isinstance check (`uow.id if not isinstance(uow, dict) else uow["id"]`) is defensively written for compatibility with dict returns.

### Encoded Orientation check

Change 1a modifies the agent prompt template — this is an Encoded Orientation decision (changes behavioral defaults of dispatched agents). The spec PR #1237 was oracle-approved and anchored to vision.yaml principle-3 and the WOS active_project. The prior logged decision exists. Check passes.

---

## Revision contract

Each gap must be resolved in one of three ways: (a) addressed, (b) disputed with specific reason, (c) deferred with stated reason.

**Gap 1 (No tests for new Change 1b code paths) — REQUIRED:**
Add at minimum:
- One test for `get_sidecar_masked_uows()` against a real SQLite registry (consistent with the test_heartbeat_locking.py pattern): a UoW in `'executing'` status older than 300s with no uow_heartbeat_log rows → returned; a UoW with a token_usage row → not returned; a UoW newer than 300s → not returned.
- One test for `record_sidecar_masked_stall()`: verify the status transition to `ready-for-steward`; verify the audit log entry is written with `stall_type='sidecar_masked'`; verify race returns 0 without an audit entry.
- One test for `detect_sidecar_masked_uows()`: dry_run mode returns skipped_dry_run count without state transitions; live mode calls `record_sidecar_masked_stall`.

**Gap 2 (Hardcoded `'executing'` from_status) — REQUIRED:**
Either: (a) derive `from_status` from the actual row status before the UPDATE (consistent with `write_heartbeat` and `close_uow`), or (b) change `'executing'` to `'active'` to match the `record_heartbeat_stall` convention (lower fidelity, but at least not wrong for the `'active'` case), and document the limitation.

**Gap 3 (Hardcoded `+300 seconds` in NOT EXISTS subquery) — REQUIRED:**
Pass `min_age_seconds` into the NOT EXISTS subquery using SQLite's `datetime(ts, '+'||?||' seconds')` modifier, or compute the datetime bound before executing and parameterize it. The hardcoded constant produces desynchronized behavior when the parameter differs from the default.
