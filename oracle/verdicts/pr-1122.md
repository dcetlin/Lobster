VERDICT: NEEDS_CHANGES
PR: 1122
Round: 3

---

## Round 3 — 2026-05-10

### Prior gap tracking

**Gap 5a (stale test fixtures in test_cc_usage_poller.py):** ADDRESSED. `STANDARD_API_RESPONSE` updated to `five_hour.utilization` / `seven_day.utilization` top-level shape. Stale tests for `used_percentage` and `pct` fallback keys removed. `test_parse_response_raises_when_expected_keys_absent` updated to match the actual error message pattern. Commit a5301288.

**Gap 5b (urllib/requests exception mismatch in test_cc_usage_poller.py):** ADDRESSED. `test_auth_error_returns_0_not_1` and `test_network_error_returns_0_not_1` now use `requests.exceptions.HTTPError` with a mock response (`mock_response.status_code = 401`) and `requests.exceptions.ConnectionError`. Production auth detection path fires correctly in these tests. Commit a5301288.

---

### Stage 1 (locked from Round 1)

The alignment verdict is Confirmed. This work addresses a genuine problem (138h stale CC quota data) with a bounded fix. The finding does not change after seeing the implementation.

---

### Stage 2: Quality Review — Round 3

#### Pattern application from learnings.md — active priors

**PR #1122 Round 2 learning (exception interface mismatch between test and production):** Round 2 established this exact pattern for this PR: "when a test injects a specific exception class for an error path, verify the exception class matches the exception class the production code's error handling was designed for." Entering Round 3, I applied this pattern as a first-pass check on both test files, not just the one modified in the Round 2 fix. This caused me to check `test_cc_usage_poller_expiry_alert.py` for the same mismatch before reading any other content.

**PR #720 caller-enumeration pattern (learnings.md):** "when a PR changes a public function's return type, the oracle's first action before reading the diff is to enumerate all files." Applied here as: when a gap is fixed in one test file, enumerate all test files for the same gap before accepting the fix. The Round 2 fix was scoped to `test_cc_usage_poller.py`. I checked whether `test_cc_usage_poller_expiry_alert.py` — the second test file in the PR — received the same fix.

---

### New gap identified

**Gap 6: `test_cc_usage_poller_expiry_alert.py` still uses `urllib.error.HTTPError` — the same exception interface mismatch that was fixed in `test_cc_usage_poller.py`**

The Round 2 fix (commit a5301288) updated `test_cc_usage_poller.py` to use `requests.exceptions.HTTPError` with a mock response for auth error tests. However, `test_cc_usage_poller_expiry_alert.py` was not updated. This file contains a `_http_error()` helper:

```python
def _http_error(code: int) -> urllib.error.HTTPError:
    return urllib.error.HTTPError(
        url="https://claude.ai/api/usage",
        code=code,
        msg=f"HTTP {code}",
        hdrs=None,
        fp=None,
    )
```

Production code extracts the HTTP status via:
```python
http_code = getattr(getattr(exc, "response", None), "status_code", None)
```

`urllib.error.HTTPError` has `.code` but not `.response`. The `getattr` chain returns `None`. `None in (401, 403)` is `False`. The `_write_cookie_expiry_alert` branch is never entered.

**Failing tests (confirmed by running `pytest` against the branch):**

- `TestMainAlertOnAuthError::test_main_calls_alert_on_401`: `mock_alert.assert_called_once_with(401, dry_run=False)` fails — alert was called 0 times.
- `TestMainAlertOnAuthError::test_main_calls_alert_on_403`: `mock_alert.assert_called_once_with(403, dry_run=False)` fails — alert was called 0 times.

**Test that passes for the wrong reason:**
- `TestMainAlertOnAuthError::test_main_does_not_call_alert_on_500`: passes because `http_code` is always `None` with `urllib.error.HTTPError` — the branch never fires regardless of status code. The test does not verify that the 500 guard works; it only confirms that a broken code path produces no side effects.

**Pytest output (actual):**
```
FAILED test_cc_usage_poller_expiry_alert.py::TestMainAlertOnAuthError::test_main_calls_alert_on_401
FAILED test_cc_usage_poller_expiry_alert.py::TestMainAlertOnAuthError::test_main_calls_alert_on_403
2 failed, 10 passed
```

**Additional note on the Round 2 revision contract:**

The Round 2 revision contract specified: "(c) Verify tests pass. `uv run pytest tests/unit/test_scheduled_tasks/test_cc_usage_poller.py -v` must show all tests passing before re-oracle." This command runs only `test_cc_usage_poller.py` — not `test_cc_usage_poller_expiry_alert.py`. The commit message says "All 21 tests pass" — this count matches the 21 tests in `test_cc_usage_poller.py` only. The 12 tests in `test_cc_usage_poller_expiry_alert.py` (10 pass, 2 fail) were not included in the stated verification. The revision contract should have specified both files. This is noted to prevent recurrence in the fix: both files must be in scope for verification.

---

### What remains correct (unchanged from prior rounds)

- `test_cc_usage_poller.py`: 21 tests, all passing. Fixtures correct. Auth exception interface correct.
- Atomic write pattern (`tempfile.mkstemp` + `os.replace`) is correct.
- `_is_job_enabled` follows the Type B reference implementation.
- `discover_cc_org_id` scoring heuristic is defensible.
- Migration 107 sequence correct — no collision.
- `_inbox_dir` correctly reads `LOBSTER_MESSAGES` env override.
- Encoded Orientation prior satisfied by this oracle review history.

---

## Revision contract (Round 3)

**Gap 6 must be resolved as follows:**

**(a) Fix `_http_error()` helper in `test_cc_usage_poller_expiry_alert.py`.** Replace `urllib.error.HTTPError` with the same pattern used in `test_cc_usage_poller.py` after the Round 2 fix: a `requests.exceptions.HTTPError` with a mock response carrying `.status_code = <code>`. Remove the `urllib.error` import from this file.

**(b) Verify `test_main_does_not_call_alert_on_500` actually tests the guard.** After fixing the exception type, confirm that the 500 test passes because the auth branch correctly does not fire on a non-auth status code — not because the code path is broken. The corrected `_http_error(500)` will produce a `requests.exceptions.HTTPError` with `.response.status_code = 500`; `500 in (401, 403)` is `False`; alert not called; test passes for the right reason.

**(c) Run both test files and report.** `uv run pytest tests/unit/test_scheduled_tasks/test_cc_usage_poller.py tests/unit/test_scheduled_tasks/test_cc_usage_poller_expiry_alert.py -v` must show all 33 tests (21 + 12) passing before re-oracle.

Generic exception-type replacement without verifying that both files are covered does not count as resolution.

---

## Round 2 — 2026-05-10

VERDICT: NEEDS_CHANGES

### Prior gap tracking

All four Round 1 gaps are reviewed for status before issuing the new verdict.

**Gap 1 (ADMIN_CHAT_ID hardcoded):** ADDRESSED. `ADMIN_CHAT_ID: int = int(os.environ.get("LOBSTER_ADMIN_CHAT_ID", "8075091586"))` at line 117 of `cc-usage-poller.py`. Matches the established env-var pattern.

**Gap 2 (cookie file chmod 600):** ADDRESSED. Migration 107 includes `chmod 600 "$_cookie_config"` after the `cat >` block. Cookie file is created as mode 600 on first run.

**Gap 3 (mirror constants in test):** ADDRESSED. `test_cc_usage_poller_expiry_alert.py` now has:
```python
ADMIN_CHAT_ID = cp.ADMIN_CHAT_ID
SENTINEL_PREFIX = cp.COOKIE_EXPIRY_SENTINEL_PREFIX
```
With an explanatory comment. Imports rather than re-declares.

**Gap 4 (test file placement):** ADDRESSED. Both test files are now in `tests/unit/test_scheduled_tasks/`.

---

### Stage 1 (locked from Round 1)

The alignment verdict is Confirmed. This work addresses a genuine problem (138h stale CC quota data) with a bounded fix. The finding does not change after seeing the implementation.

---

### Stage 2: Quality Review — Round 2

#### Pattern application from learnings.md

**PR #827 stale-assertion pattern:** The debug-agent commit (1064998d) updated `parse_usage_response` to use the new `five_hour.utilization` response shape but did NOT update the test fixtures in `test_cc_usage_poller.py`. The learnings.md PR #827 entry names this exactly: "When a PR removes a behavior, delete or invert any test that asserts the removed behavior before merge." The commit message explicitly states "New response shape: five_hour.utilization / seven_day.utilization (was: rate_limits.five_hour.used_percentage)" — the tests are precisely the stale-assertion case this pattern exists to catch. I applied this pattern before reading the test files, which caused me to look specifically for the fixture update. Finding it missing is the result of that prior.

**PR #1000 migration number collision check:** Applied mechanically before reading the diff. Migrations 103–106 are on main. Migration 107 in this PR follows in sequence. No open PR uses migration 107. No collision.

---

### New gap identified

**Gap 5: Test fixtures use old API response shape — tests are stale assertions for removed behavior**

The debug-agent commit (1064998d) changed the production `parse_usage_response` function to expect `five_hour.utilization` / `seven_day.utilization` at the top level of the response. The old shape (`rate_limits.five_hour.used_percentage`) no longer matches the endpoint. However, the test fixtures in `tests/unit/test_scheduled_tasks/test_cc_usage_poller.py` were not updated. Specific failures:

**`STANDARD_API_RESPONSE` fixture (stale shape):**
```python
STANDARD_API_RESPONSE = {
    "rate_limits": {
        "five_hour": {"used_percentage": 24.5, "resets_at": 1712345678},
        "seven_day": {"used_percentage": 31.2, "resets_at": 1712987654},
    }
}
```
The production function does `data.get("five_hour")` — returns `None` on this fixture. Both keys missing → `ValueError` raised.

**Tests that fail because of this:**
- `test_parse_standard_rate_limits_response`: passes the stale fixture directly; `parse_usage_response` raises `ValueError` instead of returning the expected dict. Test fails.
- `test_parse_response_with_used_percentage_key`: uses the stale `rate_limits` wrapper and `used_percentage` key; both are no longer parsed. Test fails.
- `test_parse_response_with_pct_key_fallback`: same structural problem; uses `rate_limits` wrapper. Test fails.
- `test_parse_response_raises_when_rate_limits_absent`: expects `pytest.raises(ValueError, match="rate_limits")` but the production error message is `"Expected 'five_hour' or 'seven_day' keys..."` — no "rate_limits" substring. The match pattern fails.
- `test_parse_response_tolerates_missing_five_hour`: wraps in `rate_limits`; both `five_hour` and `seven_day` return `None` when extracted from top-level of the response dict. Raises `ValueError` instead of returning `None` for `five_hour_pct`. Test fails.
- `test_successful_run_writes_state_file`: passes `STANDARD_API_RESPONSE` to the `fetch_usage` mock → `parse_usage_response` raises → parse error path executes → returns 0 without writing. `assert state_path.exists()` fails.

**Secondary gap: auth error detection mismatch between production and tests**

Production code extracts the HTTP status code via the `requests`/`cloudscraper` exception interface:
```python
http_code = getattr(getattr(exc, "response", None), "status_code", None)
```

Tests inject `urllib.error.HTTPError` which does not have a `.response` attribute — it has a `.code` attribute. The `getattr` chain returns `None`. `None in (401, 403)` is `False`. The alert path is not triggered. The tests `test_main_calls_alert_on_401` and `test_main_calls_alert_on_403` mock `_write_cookie_expiry_alert` and assert it was called once — but because `http_code` is `None`, the alert branch is never entered and `mock_alert.assert_called_once_with(401, dry_run=False)` fails.

The same mismatch affects `test_main_does_not_call_alert_on_500` — it would produce the correct behavior (no alert called) but for the wrong reason (http_code is always None, so the branch never fires regardless of status code). The test passes silently regardless of whether the auth detection logic is correct.

**Fix required:**

Update `STANDARD_API_RESPONSE` and all related test fixtures to use the new shape:
```python
STANDARD_API_RESPONSE = {
    "five_hour": {"utilization": 24.5, "resets_at": "2026-05-10T18:00:00+00:00"},
    "seven_day": {"utilization": 31.2, "resets_at": "2026-05-17T00:00:00+00:00"},
}
```

Update `_pct` assertions to use `"utilization"` key. Update `test_parse_response_raises_when_rate_limits_absent` to reflect the actual error message pattern (`"five_hour" or "seven_day"`).

For auth error tests: replace `urllib.error.HTTPError` with a mock object whose `.response.status_code` matches the `requests`/`cloudscraper` exception structure, or use `requests.exceptions.HTTPError` with a mock response attached.

---

### What remains correct (unchanged from Round 1)

- Atomic write pattern (`tempfile.mkstemp` + `os.replace`) is correct.
- `_is_job_enabled` follows the Type B reference implementation.
- `discover_cc_org_id` scoring heuristic is defensible for bootstrap discovery.
- Migration 107 sequence is correct — no collision in open PRs.
- `_inbox_dir` correctly reads `LOBSTER_MESSAGES` env override.
- Encoded Orientation assessment: cron polling + alert behavior is bounded scope; this oracle round serves as the logged prior.

---

## Revision contract (Round 2)

**Gap 5 must be resolved as follows:**

**(a) Update test fixtures to use the new API response shape.** Every test that constructs a response dict with a `rate_limits` wrapper or `used_percentage` key must be updated to use the `five_hour.utilization` / `seven_day.utilization` shape documented in the module docstring and used by the production function.

**(b) Fix auth error test injection.** Replace `urllib.error.HTTPError` with a mock or real `requests.exceptions.HTTPError` whose `.response.status_code` attribute is set correctly, so the auth detection path in production code fires during tests.

**(c) Verify tests pass.** `uv run pytest tests/unit/test_scheduled_tasks/test_cc_usage_poller.py -v` must show all tests passing before re-oracle.

Generic fixture updates without tracing to the named test failures do not count as resolution.

---

## Round 1 — 2026-05-10

VERDICT: NEEDS_CHANGES

### Stage 1: Vision Alignment

**Prior entering review:** This implementation is solving the wrong problem, or solving the right problem in a direction that forecloses better paths.

The problem being addressed (138h staleness in CC quota data) is real. The `statusLine` hook only fires during active CC sessions, so idle weekends — including quota reset cycles — leave `state.json` with pre-reset numbers for hours or days. Any executor dispatch gate or usage display built on this file reads garbage after an idle period. This is a direct violation of vision constraint-4 ("Minimize metabolic cost of cybernetic engagement") — stale quota data causes surprise exhaustion during AngelList sessions, which are the primary income source per `vision.yaml`.

**What would have to be true for this to be the right path:** The undocumented `claude.ai/api/usage` endpoint must remain stable enough to be worth maintaining. The session cookie approach must not become a recurring maintenance friction item that costs more cognitive load than the staleness problem it solves. If Anthropic adds an official quota API, the correct path flips.

**What does this foreclose:** Once a poller is in production, the pressure to find a more durable solution (official API, Anthropic SDK quota endpoint) decreases. The cookie-dependency is a recurring maintenance item with no retirement path. However, the scope is small and the problem is genuine. Principle-4 ("wire what exists before building more") supports fixing the staleness path before layering executor dispatch gates on top of it.

**Alignment verdict:** Confirmed. The problem is real and the fix is bounded in scope.

### Stage 2: Quality Review

[Full Round 1 findings as originally written above — gaps 1–4, all addressed in Round 2.]
