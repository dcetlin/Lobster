VERDICT: APPROVED
PR: 1152
Round: 1

## Round 1 — 2026-05-23

### Stage 1: Vision Alignment

The PR suppresses a per-cycle warning in `executor-heartbeat.py` that was firing ~480 times/day because Migration 86 (removing `wos-registry.db`) had not been applied on the live instance. Issue #1112 named two distinct gaps: (1) Migration 86 not applied, and (2) noisy per-cycle warning. This PR addresses both: manually removes the file out-of-band and adds a module-level sentinel to gate subsequent firings.

The adversarial prior question: is the sentinel the right solution, or is it masking a structural problem? The structural problem (migration not applied) is resolved by the manual removal. The sentinel is correctly framed in the PR as "belt-and-suspenders for any future recurrence" — not as a substitute for the migration. The WOS executor is the current operational substrate (vision.yaml `current_focus.primary`). Log noise reduction in the executor heartbeat is directly in service of operational health. This work does not foreclose any better path.

**Alignment verdict: Confirmed**

No premise-review observation warranted.

### Stage 2: Quality Review

**Files changed:** `scheduled-tasks/executor-heartbeat.py` (+7/-2) and `src/orchestration/dispatcher_handlers.py` (+1/-1).

**executor-heartbeat.py:** The sentinel `_LEGACY_REGISTRY_WARNED: bool = False` is placed at module scope, correctly between the `_is_job_enabled` function and the `_warn_if_legacy_registry_exists` function. The `global _LEGACY_REGISTRY_WARNED` declaration and early return are placed at the top of the function, before any filesystem or SQLite work. The `_LEGACY_REGISTRY_WARNED = True` assignment is placed after the SQLite block's `except` handler but before the `if uow_count == 0:` log dispatch — this is correct: the sentinel fires on first detection regardless of whether the SQLite query succeeded or failed, and logging only happens after the sentinel is set. The PR description's claim ("placed before the log calls so even if logging raises, later cycles remain silent") is confirmed by the diff.

Subtle note: a zero-byte file returns early via `if size == 0: return` *before* reaching the sentinel assignment. This means a zero-byte legacy file will be rechecked each cycle. This is acceptable: the `stat().st_size` check is cheap (no SQLite connection), and a zero-byte file is not the expensive warning path. The sentinel only suppresses the expensive path (SQLite connection + UoW count query + log emission).

Two comments were removed: the zero-byte comment and the UoW count comment. The docstring preserves the function's intent explanation, so the removal is not a correctness issue.

**dispatcher_handlers.py:** `from orchestration.wos_uow_detail_gen import generate_and_upload` converted to `from .wos_uow_detail_gen import generate_and_upload`. This is a correct relative import for a function inside `src/orchestration/`. The change is unrelated to the legacy registry warning but is labeled as "reviewer r1" fix and tagged to the same UoW (`uow_20260513_484e94`). Per the feature-bundling calibration from learnings.md 2026-05-18 (#1161): same-UoW reviewer-requested corrections bundled with the primary fix are same-workstream incidental maintenance — note only, not NEEDS_CHANGES.

**Encoded Orientation check:** This PR does not change behavioral defaults, system constraints, agent identity, or decision-making rules. No Encoded Orientation decision. Constraint-3 does not apply.

**No tests:** The test plan uses manual verification checkboxes (all unchecked). The sentinel logic is simple enough (global bool checked at function entry, set before log calls) that inspection provides sufficient confidence. Adding a test that calls the function twice with a mocked legacy path would be straightforward, but absence is not a blocker at this complexity level.

**Issue closing syntax:** Issues #1112 and #1071 are already in CLOSED state. The PR has no formal `Closes #N` in the body, but the issues were already resolved. Not blocking.

**Patterns checked from learnings.md:**
- Feature-bundling (2026-05-14, #1148): Two concerns present; same UoW, reviewer-requested fix — calibrated as note only per #1161.
- False test checkbox (2026-05-19, #1011): Checkboxes are unchecked — not a false claim of completion.
- Mirror-constant pattern: Not applicable — no constants duplicated from another module.

No new learnings.md or golden-patterns.md entries warranted for this PR.
