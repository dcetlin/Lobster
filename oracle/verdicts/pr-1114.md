VERDICT: APPROVED
PR: 1114
Round: 3

## Round 3 — 2026-05-24

### Stage 1: Vision alignment (written before seeing diff)

The adversarial prior entering this review: this implementation is solving the wrong problem, or solving the right problem in a direction that forecloses better paths.

The PR introduces HTML dashboard generation from live registry data — a read-only, command-triggered operation. The vision's `current_focus.current_constraint` names the WOS bidirectional interface (#1118) as the active constraint: "the HTML dashboard needs to support user-initiated actions... not just read-only status." The adversarial question is whether a read-only dashboard generator is investment in the wrong direction when the stated constraint is interactivity.

The alignment holds on closer inspection. A generated dashboard (HTML built from live DB state) is structurally upstream of a bidirectional dashboard — you cannot attach action buttons to a static file served from disk. The generator in this PR provides the rendering layer that #1118's interactivity work needs as a prerequisite. The work is correctly ordered, not orthogonal.

The `canonical placement must be obvious` golden pattern (2026-04-25) constrained my evaluation of the new `pricing.py` module: the creation of a dedicated pricing constants module is exactly the pattern that golden pattern endorses. The `mirror-constant pattern` (learnings.md, PRs #696, #800, #967, #970, #974) was the primary detection framework for Round 2 Gap 1 — and drove what I looked for first in the Round 3 diff.

**Alignment verdict: Confirmed.** This work serves vision principle-4 (integration rate before new feature rate) by wiring the dashboard to live data, and it is correctly positioned as prerequisite infrastructure for #1118.

No premise-review item triggered.

---

### Round 2 gap status (prior to Round 3 verdict)

**Gap 1 (mirror-constant divergence): ADDRESSED.** `wos_dashboard_gen.py` and `wos_uow_detail_gen.py` independently defined `SONNET_4_6_*` pricing constants. Fix: `src/orchestration/pricing.py` created as the single canonical source. `wos_dashboard_gen.py` now imports from `pricing.py` and re-exports via `__all__`. Verified: `pricing.py` exists on the PR branch with all three constants; `wos_dashboard_gen.py` imports from `pricing.py` at line 33–37; no inline pricing literals remain in `wos_dashboard_gen.py`. Note: `wos_uow_detail_gen.py` on `main` still carries inline constants (lines 48–50) — this pre-existing condition is not introduced by this PR, and the canonical source now exists for it to migrate to separately.

**Gap 2 (dead routing): ADDRESSED.** `handle_wos_dashboard()` and `parse_wos_dashboard_command()` existed in `dispatcher_handlers.py` but `sys.dispatcher.bootup.md` had no routing section for `/wos dashboard`. Fix: a "WOS Dashboard Command" section was added to `sys.dispatcher.bootup.md` (at line 1009) with a code snippet showing the detection-and-routing pattern. The module docstring routing table in `dispatcher_handlers.py` was also updated (line 15). Verified: both present on the PR branch.

---

### Stage 2: Quality review

**Files changed:** `.claude/sys.dispatcher.bootup.md`, `docs/wos-architecture.md`, `src/orchestration/dispatcher_handlers.py`, `src/orchestration/pricing.py`, `src/orchestration/wos_dashboard.py`, `src/orchestration/wos_dashboard_gen.py`, `tests/unit/test_orchestration/test_wos_dashboard_gen.py`.

**Does it do what it claims?** Yes. `pricing.py` provides the three constants. `wos_dashboard_gen.py` imports from `pricing.py`. `parse_wos_dashboard_command()` correctly matches `wos dashboard` and `/wos dashboard` (case-insensitive). `handle_wos_dashboard()` calls `wos_dashboard_gen.generate_and_upload()` via a function-body local import. The `sys.dispatcher.bootup.md` WOS Dashboard Command section provides a working routing snippet. Tests cover `_build_cc_data`, `generate_html`, `_enrich_uow_tokens`, `parse_wos_dashboard_command`, and the pricing constants (28 tests).

**Encoded Orientation check (OODA constraint-3):** The `sys.dispatcher.bootup.md` change encodes a new dispatcher routing behavior — this is an Encoded Orientation decision. Prior logged decision: the WOS command surface is an established pattern in `sys.dispatcher.bootup.md` (existing sections for `/wos start`, `/wos stop`, `/wos unblock`). The vision anchor: `vision.yaml current_focus.this_week.primary` names WOS dashboard as active work. The addition follows the same pattern as sibling routing sections. The Encoded Orientation check passes: the prior is established by pattern (same structural category as existing sections), and the vision anchor is traceable to `current_focus`.

**Failure modes present:**
- `wos_uow_detail_gen.py` on `main` still carries inline `SONNET_4_6_*` constants — the mirror is half-resolved. If pricing changes, `pricing.py` and `wos_uow_detail_gen.py` would diverge silently. This is a pre-existing condition outside this PR's scope, but the canonical source now exists and the migration path is clear.
- The `_bisque_base_url()` function in `wos_dashboard_gen.py` has a fallback chain ending with `http://localhost:9101` — if no IP configuration is available and the `curl ifconfig.me` call fails, the returned URL will be unreachable externally. This is an acceptable graceful degradation with no silent failure.
- `handle_wos_dashboard()` is documented in the bootup as "fast enough for the main thread" but there is no timeout guard on the `generate_and_upload()` call, which does SQLite queries and potentially a `curl` call. For a command-handling path on the dispatcher main thread, this is a latency risk if the registry is large or the network is slow. Not a blocking issue at current scale.

**Patterns introduced:**
- `pricing.py` as a dependency-free constants module is a clean application of the canonical-placement golden pattern. Correctly structured for import by multiple consumers.
- Re-export via `__all__` in `wos_dashboard_gen.py` for backward compatibility is sound, though it creates an alias layer where callers importing pricing constants from `wos_dashboard_gen` are technically depending on a re-export rather than the canonical source. The docstring should note this.
- The `parse_wos_dashboard_command()` / `handle_wos_dashboard()` split follows the established parser/handler pattern in `dispatcher_handlers.py` — consistent with `parse_wos_abort_command`, `handle_wos_status`, etc.

**What this makes easier:** Future pricing changes require only `pricing.py` to be updated for `wos_dashboard_gen.py`. Future `/wos dashboard` routing is documented in the authoritative bootup location. The `generate_and_upload()` entry point is the correct surface for `#1118` to build interactivity on top of.

**What this makes harder:** Nothing structurally foreclosed. The half-migrated `wos_uow_detail_gen.py` remains a technical debt item, but a separate PR can migrate it cleanly to `pricing.py` without blocking this merge.

**Test quality:** 28 new tests in `test_wos_dashboard_gen.py` cover the pure and near-pure functions. The `TestPricingConstants` class verifies the constants import from `wos_dashboard_gen` (the re-export path) — acceptable but slightly weaker than asserting they come from `pricing.py` directly. The `test_ec_uses_named_pricing_constants` test correctly imports the pricing constants for use in the expected-value computation, not local literals — this matches the golden pattern standard.

**Verdict basis:** Both Round 2 gaps are genuinely closed. No new structural issues rise to the level of NEEDS_CHANGES. The remaining items (half-migrated `wos_uow_detail_gen.py`, main-thread latency risk) are acknowledged limitations with clear migration paths, not blockers.

---

## Round 2 — 2026-05-24

VERDICT: NEEDS_CHANGES

### Stage 1: Vision alignment (written before seeing diff)

The PR adds WOS Dashboard HTML generation — a feature that generates and serves a live dashboard from registry data. From the vision: `current_focus.this_week.primary` identifies the WOS bidirectional interface (#1118) as the active constraint. A dashboard generator is prerequisite infrastructure for the bidirectional interface. The work is correctly ordered.

**Alignment verdict: Confirmed.**

---

### Round 1 gap status

**Gap 1 (DB path re-derivation): ADDRESSED.** `wos_dashboard._default_registry_path()` now delegates to `paths.REGISTRY_DB`.

**Gap 2 (pricing magic numbers): ADDRESSED IN FORM, NOT IN SUBSTANCE → NEEDS_CHANGES (re-filed as Round 2 Gap 1).** Round 1 required naming the pricing constants as module-level names. The fix added named constants to `wos_dashboard_gen.py` — but `wos_uow_detail_gen.py` also defines the same three constants independently. This is the mirror-constant pattern (learnings.md, PRs #696, #800, #967, #970, #974). Two modules independently defining the same pricing constants is the same defect at a different level.

**Gap 3 (dead routing): NEEDS_CHANGES (re-filed as Round 2 Gap 2).** `handle_wos_dashboard()` and `parse_wos_dashboard_command()` were added to `dispatcher_handlers.py` but the dispatcher's routing authority document (`sys.dispatcher.bootup.md`) was not updated. The functions exist but are unreachable from the dispatcher's documented routing path.

---

### Round 2 gaps

**Gap 1 (mirror-constant divergence):** `wos_dashboard_gen.py` and `wos_uow_detail_gen.py` independently define `SONNET_4_6_INPUT_PER_MTK`, `SONNET_4_6_OUTPUT_PER_MTK`, and `SONNET_4_6_CACHE_READ_PER_MTK`. The correct fix: create `src/orchestration/pricing.py` as the single source of truth and have both modules import from it. Adding named constants to only one of the two modules that share them does not resolve the mirror-constant defect. Addressed: when either module's pricing constants need updating, only `pricing.py` requires a change.

**Gap 2 (dead routing):** `handle_wos_dashboard()` exists in `dispatcher_handlers.py` but `sys.dispatcher.bootup.md` does not route `/wos dashboard` to it. The dispatcher's routing authority is the bootup document, not the module docstring. Until the bootup document routes this command, the handler is not reachable by the dispatcher. Fix: add a `/wos dashboard` routing section to `sys.dispatcher.bootup.md` with the detection and handling pattern.

---

## Round 1 — [2026-05-10]

VERDICT: NEEDS_CHANGES

### Stage 1: Vision alignment

The PR fixes a DB path re-derivation in `wos_dashboard.py` and adds `wos_dashboard_gen.py` — a live HTML dashboard generator from registry data. From the vision: WOS dashboard work is explicitly in `current_focus.this_week.primary`. The work is within scope.

**Alignment verdict: Confirmed.**

### Round 1 gaps

**Gap 1 (DB path):** `wos_dashboard._default_registry_path()` re-derived the registry path inline rather than delegating to `paths.REGISTRY_DB`. Silent divergence risk if `paths.py` env-var resolution logic changes. Fix: delegate to `paths.REGISTRY_DB`.

**Gap 2 (pricing magic numbers):** `wos_dashboard_gen.py` used inline numeric literals (`3.0`, `15.0`, `0.30`) at multiple computation sites. Named module-level constants are required.

**Gap 3 (dead routing):** `handle_wos_dashboard()` wired in `dispatcher_handlers.py` but not routed in `sys.dispatcher.bootup.md`. The handler is effectively dead until the bootup document routes it.
