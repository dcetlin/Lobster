VERDICT: APPROVED
MODE: VERIFICATION
PR: 1396
Round: 1

Checked: wos_preflight.py module correctness, executor.py wiring, test coverage (18 new + 106 regression), fail-open contract, registry isolation in tests, heat short-circuit path contract compliance (result.json + trace.json + complete_uow). All pass. No OODA constraint-3 violation — default-True extension point, no existing routing behavior changed.

---

## Stage 1: Vision Alignment

**Prior entering review:** this implementation is solving the wrong problem, or solving the right problem in a direction that forecloses better paths.

**Vision alignment check:**

- `current_focus.this_week.primary` places this PR squarely within authorized Stage 1/2 substrate work. `what_not_to_touch` excludes "Stage 1/2 WOS implementation — separate agents handling this" — uow_20260522_6b3a1c is one of those authorized separate agents.
- `current_constraint` ("Do not resume execution_enabled until Stage 1/2 substrate is stable") — this PR contributes directly to stabilization. The fail-open default (`True`) ensures no regression risk.
- `principle-3` (Determinism over judgment): the registry-dispatch design converts a per-type judgment call into a deterministic if-then check at the executor boundary.
- `principle-4` (Integration rate before new feature rate): wires an existing architectural gap (no shared "is this still needed?" protocol) rather than adding a new feature category.
- `principle-1` (Proactive resilience over reactive recovery): the heat data (40%) is the signal; this is structural prevention, not a better post-hoc detection mechanism.

**Stage 1 finding:** CONFIRMED. The work directly serves current phase intent (WOS executor substrate stabilization). The problem being solved (40% heat from lack of shared "is this still needed?" protocol) is real and measured. The extension point design (registry dispatch, fail-open default) eliminates regression risk and satisfies principle-3 and principle-4. No cheaper test of the underlying assumption exists — the heat accumulation data IS the test that was run.

**Golden pattern match:** "table-as-compaction-resistant encoding" — not directly applicable here. More relevant: the registry-dispatch extension point is a structural application of principle-3 (Determinism over judgment). This pattern — converting a per-type judgment call to a registered deterministic check — is worth noting in golden-patterns.md.

---

## Stage 2: Quality Review (VERIFICATION mode)

**Mode rationale:** The test suite is the external adjudicator. 18 new unit tests + 106 regression tests passing per UoW description. Stage 2 checks structural correctness, coverage of edge cases, and completeness.

**wos_preflight.py:**
- `_REGISTRY` as module-level dict: correct. Module-level registry is appropriate for a globally-shared extension point.
- `register_preflight_check`: idempotent (overwrites), raises `TypeError` on non-callable, logs debug on registration. All correct.
- `is_still_needed`: `getattr(uow, "type", "") or ""` handles missing `.type` attribute correctly. `_REGISTRY.get(uow_type, _default_check)` is correct. Exception handling is fail-open (`return True`), logged at WARNING. All correct.
- `PreflightCheck` Protocol is `runtime_checkable` — enables `isinstance` checks if needed.
- Deferred import of `is_still_needed` inside `_run_execution` body — slightly unconventional but no functional issue; avoids circular import risk at module load time.

**executor.py wiring:**
- Fresh `registry.get(uow_id)` before calling `is_still_needed` — correct. The hook receives a fully-hydrated UoW with all fields.
- `if uow_for_preflight is not None and not _is_still_needed(uow_for_preflight)` — correct guard. Registry read failure → `uow_for_preflight is None` → dispatch proceeds (fail-open).
- Heat short-circuit path: writes `output_ref` content, writes `result.json`, validates it (`_validate_result_json_written`), writes `trace.json`, inserts corrective trace, calls `complete_uow(uow_id, output_ref, outcome_category="heat")`. This exactly mirrors the normal completion path. Contract compliance confirmed.

**Test coverage:**
- `TestIsStillNeededDefault`: covers no-check-registered, unknown type, empty type, missing `.type` attribute. Complete.
- `TestIsStillNeededRegistered`: covers True/False paths, UoW object passed correctly, type isolation (check for other type not called). Complete.
- `TestIsStillNeededFailOpen`: raising check returns True. Complete.
- `TestRegisterPreflightCheck`: callable enforcement, idempotent re-registration, non-callable raises TypeError. Complete.
- `TestExecutorPreflightDefaultPath` / `TestExecutorPreflightFalsePath`: integration tests via monkeypatch. Covers: default True dispatches normally, registered True dispatches normally, False does not dispatch, False marks heat outcome_category, False writes result.json, injected dispatcher not called on False. Complete.
- `TestPreflightProtocol` in `test_executor.py`: separate integration suite with autouse `_isolate_preflight_registry` fixture. Four required paths verified. Complete.

**OODA constraint-3 (Encoded Orientation check):**
- Does this PR constitute an Encoded Orientation decision? The module creates a shared extension point with fail-open default (`True`). All existing UoW types proceed unchanged — no behavioral default changes. The actual gate logic for specific UoW types arrives in issues #927 and #928 (separate PRs, each will require their own oracle review).
- The `register_preflight_check` call is an Encoded Orientation interface — any per-type registration changes the system's behavior for that UoW type. The PR itself registers no checks; it only defines the interface and wires it into the executor.
- **Verdict on constraint-3:** No violation in this PR. The per-type registrations in #927 and #928 must each confirm a traceable vision.yaml anchor and logged prior decision when submitted for oracle review.

**What this forecloses:** Nothing significant. The registry-dispatch pattern is the most open extension point possible — per-type checks are opt-in, fail-open by default.

**What this makes easier:** Issues #927 and #928 can now register their checks without touching executor.py or the dispatch core. Future per-type idempotency gates have a clear home.

**Patterns introduced:** Registry-dispatch with fail-open default for pre-flight hooks. This pattern carries high reusability for any cross-cutting executor concern.
