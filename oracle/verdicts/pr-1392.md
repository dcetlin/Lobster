VERDICT: APPROVED
MODE: VERIFICATION
PR: 1392
Round: 1

The PR adds 7 lines to `scripts/upgrade.sh` that source `user-update.sh` at the end of `main()`, after `run_migrations()` completes and before `health_check()`. The change closes the documented-contract gap: CLAUDE.md and user-update.sh's own header both state the file is "sourced at the end of upgrade.sh." The implementation uses the canonical `$LOBSTER_DIR` variable, a `[ -f ]` existence guard, and `source` for variable inheritance — all correct.

One structural observation noted but not blocking: the existing mid-migration sourcing block at line 2362-2366 (inside `run_migrations()`, after Migration 63) remains in place. After this PR, user-update.sh is sourced twice per upgrade run. The double-execution is harmless because every migration in user-update.sh uses idempotency guards (`[ ! -f ]`, `jq -e`, `crontab -l | grep -q`). The `migrated` counter increments in main()'s scope during the second sourcing, but that value is never used. No correctness defect results.

---

## Stage 1: Vision Alignment

Formed before reading the implementation.

The implicit theory of change: fix the implementation to match the documented contract so instance-specific migrations run at the correct point in the upgrade sequence (after all core migrations complete). This is a hygiene/correctness fix, not a new capability.

What would have to be true for this to be the wrong path: if the documentation was wrong — i.e., the intent was always to run user-update.sh mid-migrations — then making the implementation match the documentation would be incorrect. But the PR description explicitly names "Migration 90 (written to user-update.sh) were silently skipped or ran out of order," confirming the documentation reflects intent, not just aspiration.

Is there a cheaper test of the underlying assumption? The assumption is that "at the end of main()" is the right position, not "mid-migrations." The existing sourcing at line 2366 ran mid-migration. The cheaper test is simply reading CLAUDE.md and user-update.sh's header — both say "end of upgrade.sh." The PR is the right fix.

What does this foreclose? Nothing. 7 additive lines with zero deletions. No direction becomes harder.

Vision alignment: vision.yaml principle-1 ("proactive resilience over reactive recovery") supports closing implementation-documentation gaps. Principle-4 ("wire what exists before building more") applies — user-update.sh exists and its contract was unwired. This is exactly the kind of "missing arrow between existing systems" principle-4 names as a velocity multiplier.

**Alignment verdict: Confirmed**

---

## Stage 2: Quality Review

**Structural correctness:** Placement is correct — after `run_migrations` completes, before `health_check`. This ensures all core migrations have run before instance-specific d-steps execute. The `$LOBSTER_DIR` variable is the canonical repo-root variable used throughout upgrade.sh. The `[ -f ]` guard prevents errors on installs without user-update.sh. `source` (not `bash`) is correct because user-update.sh uses `local` declarations and inherits variables like `substep`, `warn`, `success`, `migrated`.

**Failure modes examined:**
- Variable scope for `migrated`: When sourced from main(), `migrated` is unset in main()'s scope. user-update.sh does `migrated=$((migrated + 1))` which evaluates to 1. This variable in main()'s scope is never used, so it's harmless.
- Double-execution: The existing sourcing block at line 2366 is NOT removed by this PR. user-update.sh will be sourced twice. All d-step migrations use idempotency guards, so no steps are re-applied. The second pass prints "already present" substep messages that appear in the upgrade output — cosmetically noisy but not incorrect.
- LOBSTER_DIR availability: `$LOBSTER_DIR` is set by the script's preamble (not by run_migrations) so it's available in main()'s scope.

**Patterns introduced:** The main() sourcing block is the canonical position per the documented contract. Future d-steps added to user-update.sh will execute after all core migrations complete, which is the correct order for steps that may depend on core migration outputs (new DB tables, new directories, etc.).

**What this makes easier:** Instance-specific migrations can now safely depend on all core migrations having completed. The CLAUDE.md contract is now structurally enforced rather than aspirational.

**What this makes harder:** Nothing. The residual mid-migration sourcing at line 2366 technically creates a two-phase execution, but since all steps are idempotent, this is silent noise rather than a structural problem.

**Encoded Orientation check (constraint-3):** This PR modifies `scripts/upgrade.sh` — a deployment script, not an agent definition, bootup file, gate table, or CLAUDE.md. No behavioral orientation is being encoded. Constraint-3 does not apply.

**Feature-bundling check:** 1 file changed, 7 lines added. PR description accurately describes the full change. No bundling.

**Pattern citations from learnings.md:**
- The "advisory-to-hook promotion" / structural-enforcement direction (golden-patterns.md, 2026-04-22) applies inversely here: this is a case where a documentation promise needed a code implementation, not the reverse. The gap between doc and code is the same class of structural inconsistency.
- The "documentation PR oracle check sequence" learning (2026-04-26 Meta) was consulted: (1) Feature-bundling: clean; (2) Path reference canonicality: `$LOBSTER_DIR/scripts/user-update.sh` is canonical; (3) Encoded Orientation: N/A for a deployment script; (4) Resolution-by-absence: no prior NEEDS_CHANGES gap for this PR.
