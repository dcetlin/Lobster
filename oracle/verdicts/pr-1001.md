VERDICT: APPROVED
PR: 1001
Round: 2

## Stage 1: Vision Alignment

**Prior:** This implementation is solving the wrong problem, or solving the right problem in a direction that forecloses better paths.

**Assessment:** The task — persisting `outcome_category` (metabolic taxonomy: heat/shit/seed/pearl) to the `uow_registry` DB column — directly serves the vision's WOS Phase 1 substrate. The metabolic taxonomy was introduced in PR #759 and has been live as a JSONL-only signal since then. Moving it into the registry makes it DB-queryable for pipeline visibility, routing decisions, and the `registry_cli report` command. This is a "wire what exists before building more" move (principle-4) that closes a real observability gap without introducing new behavioral logic.

**What would have to be true for this to be the wrong path:** The metabolic taxonomy itself would have to be wrong as a classification scheme, or the DB column would have to foreclose a better observability pattern. Neither is present: the taxonomy is already validated and written at `write_result` time; the DB column is additive and doesn't constrain alternative approaches.

**What this forecloses:** Nothing significant. The `TEXT NULL` column with `COALESCE` write semantics leaves all existing behavior unchanged for callers that don't pass `outcome_category`.

**Encoded Orientation check:** No new behavioral defaults are introduced. The migration adds a column; `complete_uow` writes it when provided. No automated routing decisions read this field. Constraint-3 does not apply.

**Alignment verdict:** Confirmed

---

## Round 2 — 2026-04-27

### Prior Gap Status (from Round 1)

**Gap 1 (migration collision — BLOCKING):** ADDRESSED. Migration is now named `0018_add_outcome_category.sql`. PR #999 claims 0016 (`0016_add_prescription_confidence.sql`); PR #1000 claims 0017 (`0017_heartbeat_token_log.sql`, confirmed merged to main). 0018 is verified as the correct next slot.

**Gap 2 (PR #997 merge conflict — BLOCKING):** ADDRESSED. PR #997 merged at 2026-04-27T19:58:00Z. The PR diff now contains only outcome_category additions on top of PR #997's merged report infrastructure. The deletions in `registry_cli.py` (-9 lines) are narrowly scoped: updating parameter signatures to thread `outcome_category`, fixing the f-string literal `f"WOS Pipeline Report"` → `"WOS Pipeline Report"`, and adjusting column header widths. No full report-command backport remains in the diff.

**Finding 1 (_EXECUTING_STATUSES dead code — advisory):** ADDRESSED as a side-effect of the rebase. `_classify_status` now explicitly uses `if status in _EXECUTING_STATUSES: return "executing"` at line 536, making the frozenset active. The dead code gap is closed.

### Stage 2: Round 2 Quality Review

**Flow verification:** The outcome_category propagation chain is correct and complete:
- `handle_write_result` in `inbox_server.py` extracts `outcome_category` (validated by `VALID_OUTCOME_CATEGORIES` membership before call) and passes to `_maybe_complete_wos_uow`
- `_maybe_complete_wos_uow` forwards to `maybe_complete_wos_uow` in `wos_completion.py`
- `maybe_complete_wos_uow` forwards to `registry.complete_uow`
- `complete_uow` writes via `COALESCE(?, outcome_category)` — existing values are preserved for callers that omit the field
- `audit_log` execution_complete note includes outcome_category when present (conditional dict unpack)
- `_compute_summary` counts per-label, filtered to `_VALID_OUTCOME_CATEGORIES` (NULL and invalid values excluded)
- `_format_report` emits `Outcomes: pearl: N  seed: N  heat: N  shit: N` only when at least one category is set

The COALESCE pattern is correct: passing `None` as the first arg to `COALESCE(?, outcome_category)` causes SQLite to fall through to the existing column value, which is the no-op semantics intended.

**New advisory findings:**

1. **Stale comment in migration file.** `0018_add_outcome_category.sql` contains the internal comment `-- Migration 0016: add outcome_category column...`. The filename was correctly renumbered to 0018 but the comment still says 0016. This does not affect migration runner behavior (which keys on filename), but creates cosmetic inconsistency. Advisory — no merge gate.

2. **Mirror-constant `_VALID_OUTCOME_CATEGORIES` (continued from Round 1 Finding 2).** `_VALID_OUTCOME_CATEGORIES` in `registry_cli.py` mirrors `VALID_OUTCOME_CATEGORIES` in `inbox_server.py`. Test files also declare local `VALID_LABELS = ("heat", "shit", "seed", "pearl")` — a third mirror. The registry_cli mirror is justified by circular import prevention (inbox_server cannot be imported in orchestration modules, established pattern per learnings.md PR #848). The test-file mirrors are the standard mirror-constant pattern from learnings.md (PRs #696, #800, #967, #970) — not ideal but the vocabulary is stable (four values, not expected to change without a design decision), and no gap from this round is blocking. The comment in registry_cli.py names the canonical source (`VALID_OUTCOME_CATEGORIES in inbox_server`), which is the minimum required mitigation.

3. **`fetch_in_window` lacks direct unit tests (continued from Round 1 Finding 3).** The new `fetch_in_window` method is tested only indirectly through `cmd_report` integration tests. The SQL `wall_clock_seconds` computed column (`COALESCE(completed_at, ...) - started_at`), the OR condition on the window predicate, and ordering behavior have no direct assertions. This is advisory — the indirect tests cover the happy path adequately and the SQL is straightforward.

**Patterns this work extends:**
- `seam-first abstraction` (golden-patterns.md 2026-03-30): `outcome_category` is placed at the `complete_uow` boundary, the natural seam between execution infrastructure and registry state, without changing the transition protocol around it.
- `principle-4 (integration rate before new feature rate)`: wiring an already-live taxonomy into queryable storage rather than building new features on top of an unqueryable one.

**What this makes easier:** Querying metabolic taxonomy from the registry without joining the JSONL ledger. `registry_cli report --since 24` now surfaces pearl/seed/heat/shit breakdowns inline. Future steward logic that reads outcome_category can do so via SQL query.

**What this makes harder:** Nothing is foreclosed. The COALESCE write pattern is conservative and non-destructive.

---

## Round 1 — 2026-04-27

VERDICT: NEEDS_CHANGES
PR: 1001
Round: 1

### Stage 1: Vision Alignment

(Preserved — same as Round 2 Stage 1 above.)

### Stage 2: Quality Review

#### Blocking Issues

**Gap 1: Migration number collision with PR #999 (BLOCKS MERGE)**

This PR added `src/orchestration/migrations/0016_add_outcome_category.sql`. PR #999 also added `src/orchestration/migrations/0016_add_prescription_confidence.sql`. Both claimed 0016. The migration runner skips versions already in `_migrations` — when the second merges, its migration is silently skipped. This is the exact failure mode named in learnings.md PR #1000.

**Required fix:** Renumber to `0017_add_outcome_category.sql` (or `0018` if PR #1000 also claims 0017).

**Gap 2: PR #997 merge conflict — report code bundled in both PRs (BLOCKS MERGE)**

PR #997 merged at 2026-04-27T19:58:00Z. This PR carried the full `registry_cli report` command as a backport with outcome_category integrated, creating a merge conflict. The branch needed rebase.

**Required fix:** Rebase against main. Post-rebase diff should contain only outcome_category-specific additions.

#### Non-Blocking Findings

**Finding 1:** `_EXECUTING_STATUSES` declared but not referenced in `_classify_status` (dead code, PR #997 learnings pattern). Advisory.

**Finding 2:** `_VALID_OUTCOME_CATEGORIES` is a mirror constant. Advisory — circular import rationale valid.

**Finding 3:** `fetch_in_window` lacks direct unit tests. Advisory.

#### Revision Contract

Gap 1: Address by renumbering migration. Gap 2: Address by rebase against main.
